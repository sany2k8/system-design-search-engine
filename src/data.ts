export interface Project {
  id: number;
  title: string;
  cat: string;
  stack: string;
  prompt: string;
}

export const CATEGORIES = {
  "Infrastructure": "t-infra",
  "Database": "t-db",
  "Auth & Security": "t-auth",
  "Performance": "t-perf",
  "Distributed Systems": "t-dist",
  "Messaging": "t-msg",
  "Observability": "t-obs",
  "API Design": "t-api",
  "Storage Systems": "t-storage",
  "Networking": "t-net",
  "Concurrency": "t-conc",
  "Compiler & Runtimes": "t-compiler",
  "ML Systems": "t-ml",
  "Cloud Native": "t-cloud",
  "Data Engineering": "t-de",
  "Testing & Reliability": "t-test"
};

export const CATEGORY_COLORS: Record<string, string> = {
  'Infrastructure': 'from-blue-500/20 to-blue-500/5 text-blue-400 border-blue-500/20',
  'Database': 'from-emerald-500/20 to-emerald-500/5 text-emerald-400 border-emerald-500/20',
  'Auth & Security': 'from-rose-500/20 to-rose-500/5 text-rose-400 border-rose-500/20',
  'Performance': 'from-amber-500/20 to-amber-500/5 text-amber-400 border-amber-500/20',
  'Distributed Systems': 'from-indigo-500/20 to-indigo-500/5 text-indigo-400 border-indigo-500/20',
  'Messaging': 'from-orange-500/20 to-orange-500/5 text-orange-400 border-orange-500/20',
  'Observability': 'from-teal-500/20 to-teal-500/5 text-teal-400 border-teal-500/20',
  'API Design': 'from-pink-500/20 to-pink-500/5 text-pink-400 border-pink-500/20',
  'Storage Systems': 'from-sky-500/20 to-sky-500/5 text-sky-400 border-sky-500/20',
  'Networking': 'from-cyan-500/20 to-cyan-500/5 text-cyan-400 border-cyan-500/20',
  'Concurrency': 'from-violet-500/20 to-violet-500/5 text-violet-400 border-violet-500/20',
  'Compiler & Runtimes': 'from-yellow-500/20 to-yellow-500/5 text-yellow-400 border-yellow-500/20',
  'ML Systems': 'from-fuchsia-500/20 to-fuchsia-500/5 text-fuchsia-400 border-fuchsia-500/20',
  'Cloud Native': 'from-teal-400/20 to-teal-400/5 text-teal-300 border-teal-400/20',
  'Data Engineering': 'from-lime-500/20 to-lime-500/5 text-lime-400 border-lime-500/20',
  'Testing & Reliability': 'from-green-500/20 to-green-500/5 text-green-400 border-green-500/20',
  'All': 'from-accent/20 to-accent/5 text-accent-light border-accent/20'
};

export const PROJECTS: Project[] = [
  { id: 1, title: "Database Migration Framework", cat: "Database", stack: "Python + SQLAlchemy + Alembic", prompt: "Build a Python CLI tool that mimics how Alembic works internally. Implement: version tracking table, migration file generator (upgrade/downgrade), dependency graph of migrations, auto-detect schema diff between ORM models and live DB. Output applied/pending status. Explain each step with print statements showing what SQL is being executed and why." },
  { id: 2, title: "Multi-Factor Authentication", cat: "Auth & Security", stack: "Python + FastAPI + TOTP", prompt: "Build a FastAPI app with full MFA flow: user registration with password hashing (bcrypt), login endpoint, TOTP secret generation (pyotp), QR code for authenticator apps, TOTP verification endpoint, JWT issuance after MFA passes. Add a /me protected endpoint. Show the full token lifecycle with decode/verify logic exposed." },
  { id: 3, title: "Rate Limiter (Token Bucket)", cat: "Performance", stack: "Python + Redis + FastAPI", prompt: "Implement a token bucket rate limiter as a FastAPI middleware. Use Redis to store bucket state per user/IP. Support: configurable capacity and refill rate, per-route overrides, X-RateLimit-* headers in response. Add a stress test script that fires 100 requests and shows which get throttled. Explain how token bucket differs from fixed window." },
  { id: 4, title: "Distributed Cache (LRU)", cat: "Performance", stack: "Python + Redis", prompt: "Build an LRU cache layer in Python backed by Redis. Implement: get/set/delete with TTL, LRU eviction using Redis sorted sets (score = timestamp), cache-aside pattern with a mock DB, cache stampede protection using locks, hit/miss ratio metrics. Add a simulation that shows cache warming and eviction in action." },
  { id: 5, title: "Event-Driven Order System", cat: "Messaging", stack: "Python + Kafka (or RabbitMQ)", prompt: "Build a mini e-commerce order system using event-driven architecture. Services: OrderService (produces OrderPlaced), InventoryService (consumes, produces InventoryReserved or Failed), PaymentService (consumes, produces PaymentProcessed), NotificationService (consumes all). Use Kafka topics per event type. Show exactly how each consumer group works and what happens on failure." },
  { id: 6, title: "Circuit Breaker Pattern", cat: "Distributed Systems", stack: "Python + FastAPI", prompt: "Implement the circuit breaker pattern from scratch (no library). States: CLOSED, OPEN, HALF_OPEN. Configure: failure threshold, timeout, probe interval. Wrap an HTTP client that calls a flaky external API (mock it). Log every state transition with reason. Add a dashboard endpoint showing current state and failure stats. Explain why each transition happens." },
  { id: 7, title: "JWT Auth with Refresh Tokens", cat: "Auth & Security", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a complete JWT auth system: access token (15min), refresh token (7d) stored in DB with device info, /refresh endpoint that rotates refresh token (detect reuse), /logout that revokes token family, blacklist check on protected routes. Show the full token rotation flow with diagrams in comments. Include token theft simulation." },
  { id: 8, title: "Read Replicas & Write Routing", cat: "Database", stack: "Python + PostgreSQL + SQLAlchemy", prompt: "Set up a PostgreSQL primary + 2 read replicas using Docker. Build a Python connection router that: sends writes to primary, load-balances reads across replicas, detects replication lag and skips laggy replicas, falls back to primary on replica failure. Add a test that shows stale reads happening and how to handle them." },
  { id: 9, title: "Distributed Tracing", cat: "Observability", stack: "Python + OpenTelemetry + Jaeger", prompt: "Instrument 3 FastAPI microservices (API Gateway, User Service, Product Service) with OpenTelemetry. Propagate trace context via HTTP headers. Send spans to Jaeger. Show: parent/child span relationships, custom attributes, error recording, sampling config. Add a slow endpoint and show how the trace reveals the bottleneck." },
  { id: 10, title: "GraphQL API with DataLoader", cat: "API Design", stack: "Python + Strawberry + SQLAlchemy", prompt: "Build a GraphQL API for a blog (Posts, Authors, Comments). Show the N+1 problem first — then fix it with DataLoader batching. Implement: query depth limiting, cost analysis, field-level auth, subscriptions for new comments via WebSocket. Log actual SQL queries to prove batching works." },
  { id: 11, title: "CQRS + Event Sourcing", cat: "Distributed Systems", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a bank account system using CQRS and Event Sourcing. Events: AccountOpened, MoneyDeposited, MoneyWithdrawn. Store events in an append-only event_store table. Rebuild state by replaying events. Separate read model (materialized view of balance). Show: event replay, point-in-time state, projection rebuild. No ORM updates — only event appends." },
  { id: 12, title: "API Gateway with Plugins", cat: "API Design", stack: "Python + FastAPI", prompt: "Build a programmable API Gateway from scratch. Plugin pipeline: auth (JWT verify), rate limiter, request logger, response transformer, circuit breaker, retry. Each plugin is a class with before_request/after_response hooks. Show how to compose plugins in order. Add a config file that enables/disables plugins per route. Explain the middleware chain pattern." },
  { id: 13, title: "Consistent Hashing Ring", cat: "Distributed Systems", stack: "Python", prompt: "Implement a consistent hashing ring from scratch. Features: add/remove nodes with virtual nodes (vnodes), key-to-node mapping, rebalancing stats when node joins/leaves (% of keys moved). Simulate a 5-node cache cluster, add a 6th node, and prove minimal key movement. Compare with modulo hashing to show why consistent hashing wins." },
  { id: 14, title: "Saga Pattern (Choreography)", cat: "Messaging", stack: "Python + RabbitMQ", prompt: "Implement the Saga pattern (choreography style) for a travel booking system: FlightBooking, HotelBooking, CarRental — each a separate service. On partial failure, each service listens for compensating events and rolls back. Show: happy path, FlightBooking succeeds + HotelBooking fails → flight gets cancelled. Log every event and compensation step." },
  { id: 15, title: "Bloom Filter for Deduplication", cat: "Performance", stack: "Python", prompt: "Build a Bloom filter from scratch (no library). Implement: configurable m bits and k hash functions, add/query, false positive rate estimation. Use it for URL deduplication in a web crawler simulation. Compare memory usage: bloom filter vs a set. Show false positive rate increasing as filter fills up. Visualize bit array state." },
  { id: 16, title: "Leader Election (Bully Algorithm)", cat: "Distributed Systems", stack: "Python + sockets", prompt: "Simulate the Bully Algorithm for leader election across 5 processes using Python sockets. Implement: election message, OK message, coordinator message, timeout detection, re-election when leader crashes. Run all 5 processes in separate threads. Kill the leader mid-run and show re-election happening. Log every message sent between nodes." },
  { id: 17, title: "Time-Series Data Ingestion", cat: "Database", stack: "Python + TimescaleDB + FastAPI", prompt: "Build a time-series ingestion pipeline for IoT sensor data. Use TimescaleDB hypertables. Implement: bulk insert with COPY for throughput, continuous aggregates (per-minute rollup), retention policy, gap-filling query for missing intervals. Send 100k fake sensor readings and measure insert throughput. Show query speedup from continuous aggregates." },
  { id: 18, title: "Service Mesh (Sidecar Pattern)", cat: "Infrastructure", stack: "Python + Docker + Envoy", prompt: "Build a simplified sidecar proxy pattern. Each service has a sidecar container (simple Python proxy) that intercepts all in/out traffic. Sidecar handles: mTLS between services, retry with backoff, request header injection (trace ID), access logging. Show how the app service is completely unaware of the mesh. Use Docker Compose for the setup." },
  { id: 19, title: "Column-Level Encryption", cat: "Auth & Security", stack: "Python + PostgreSQL + SQLAlchemy", prompt: "Implement column-level encryption for PII fields in PostgreSQL. Use AES-256-GCM with a per-row nonce. Store encrypted_ssn, encrypted_email alongside a key_id. Implement key rotation without downtime (dual-write period). Show: encrypt on write, decrypt on read, key rotation migration, what happens if key is lost. Use SQLAlchemy TypeDecorator." },
  { id: 20, title: "WebSocket Pub/Sub Server", cat: "Messaging", stack: "Python + FastAPI + Redis", prompt: "Build a real-time pub/sub server using WebSockets. Clients subscribe to named channels. Backend uses Redis Pub/Sub to fan out messages. Support: multi-server scaling (messages from server A reach clients on server B via Redis), channel presence (who's online), last-n-messages replay on join. Show connection lifecycle and message flow." },
  { id: 21, title: "Idempotency Keys", cat: "API Design", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a payment API with idempotency key support. Client sends Idempotency-Key header. Server stores request hash + response. On duplicate: return cached response without re-processing. Handle: concurrent duplicate requests (lock), expired keys (cleanup job), key collision detection. Show: network retry scenario where payment fires twice but only charges once." },
  { id: 22, title: "Query Plan Analyzer", cat: "Database", stack: "Python + PostgreSQL", prompt: "Build a tool that runs EXPLAIN ANALYZE on slow queries and parses the output. For each node: extract cost, actual rows, planning time. Detect anti-patterns: Seq Scan on large table, nested loop with high row estimates, index not used. Suggest fixes: add index, rewrite subquery as CTE, add statistics. Test on a 1M row table with bad and good queries." },
  { id: 23, title: "Distributed Lock Manager", cat: "Distributed Systems", stack: "Python + Redis", prompt: "Implement Redlock algorithm for distributed locking across 3 Redis nodes. Show: acquire lock from quorum, TTL-based expiry, fence tokens to handle lock expiry during long operations, lock renewal for long jobs. Simulate: two processes competing for the same resource, one process holding lock when it crashes. Explain why single-Redis locks are unsafe." },
  { id: 24, title: "S3-Compatible Object Storage", cat: "Infrastructure", stack: "Python + FastAPI + MinIO", prompt: "Build an S3-compatible object storage API backed by MinIO. Implement: presigned URLs for direct upload/download, multipart upload for large files, bucket versioning, lifecycle rules (delete after N days), server-side encryption. Add a client library that handles: retry on 5xx, checksum verification, multipart threshold. Show how presigned URLs work without server proxying data." },
  { id: 25, title: "gRPC Microservices", cat: "API Design", stack: "Python + gRPC + Protobuf", prompt: "Build 2 microservices using gRPC: UserService and OrderService. Define .proto files with nested messages, enums, and streams. Implement: unary RPC, server-streaming (order history), client-streaming (bulk import), bidirectional streaming (live chat). Show: proto compilation step, generated stubs, interceptors for auth and logging. Compare payload size with REST/JSON." },
  { id: 26, title: "Health Check & Readiness Probes", cat: "Observability", stack: "Python + FastAPI + Docker", prompt: "Build a FastAPI service with Kubernetes-style /healthz (liveness) and /readyz (readiness) endpoints. Liveness: checks process is alive. Readiness: checks DB connection, cache connection, downstream service reachability. Implement: dependency degradation (mark unready but stay alive), graceful shutdown (drain requests before unready), health check aggregation. Simulate each failure mode." },
  { id: 27, title: "Feature Flags Service", cat: "Infrastructure", stack: "Python + FastAPI + Redis", prompt: "Build a feature flag service. Flags support: boolean on/off, percentage rollout (user ID hashing), user segment targeting (beta users), environment overrides. SDK: get_flag(name, user_id) returns bool with <1ms latency using local cache + Redis. Show: gradual rollout from 0% to 100%, kill switch scenario, A/B test setup. Include admin API for flag management." },
  { id: 28, title: "Log Aggregation Pipeline", cat: "Observability", stack: "Python + Filebeat + Elasticsearch + Kibana", prompt: "Build a structured logging pipeline. Services emit JSON logs with trace_id, level, service, timestamp. Filebeat ships logs to Elasticsearch. Build: log schema (avoid mapping explosions), index lifecycle management (hot→warm→cold), a Kibana dashboard showing error rate and p99 latency per service. Add a log sampler that reduces DEBUG log volume under high load." },
  { id: 29, title: "Cron Job Scheduler", cat: "Infrastructure", stack: "Python + PostgreSQL + FastAPI", prompt: "Build a distributed cron job scheduler. Jobs defined as cron expressions in DB. Scheduler: polls every second, uses advisory locks to prevent double-execution across instances, marks jobs as running/done/failed, retry with exponential backoff. Support: one-shot jobs, recurring jobs, job chaining. Show: two scheduler instances running simultaneously — only one executes each job." },
  { id: 30, title: "OAuth2 Authorization Server", cat: "Auth & Security", stack: "Python + FastAPI", prompt: "Build a full OAuth2 authorization server from scratch. Implement flows: authorization_code with PKCE, client_credentials. Endpoints: /authorize, /token, /introspect, /revoke, /.well-known/openid-configuration. Generate JWTs signed with RSA private key. Show: entire browser redirect flow, token introspection, refresh token rotation. Explain why PKCE replaces client_secret for public clients." },
  { id: 31, title: "Database Connection Pooling", cat: "Database", stack: "Python + PostgreSQL + PgBouncer", prompt: "Set up PgBouncer in front of PostgreSQL. Benchmark: direct connection vs pooled (transaction mode vs session mode) at 100 concurrent clients. Measure: connection overhead, TPS, max connections on PG side. Implement: health checks, pool exhaustion handling, statement timeout. Show the pg_stat_activity difference with and without pooling." },
  { id: 32, title: "Eventual Consistency Demo", cat: "Distributed Systems", stack: "Python + FastAPI", prompt: "Build a demo of eventual consistency using the 2-node replicated counter. Operations: increment, read. Node A and B sync asynchronously. Show: read-your-writes violation, monotonic reads violation, then fix each with session tokens and vector clocks. Make the inconsistency window visible with sleep() in sync. Show how Last-Write-Wins CRDTs solve merge conflicts." },
  { id: 33, title: "Prometheus + Grafana Metrics", cat: "Observability", stack: "Python + FastAPI + Prometheus + Grafana", prompt: "Instrument a FastAPI app with Prometheus metrics. Expose: request_duration_seconds (histogram), requests_total (counter by status/method/path), active_connections (gauge), cache_hit_ratio (gauge). Build a Grafana dashboard showing: p50/p95/p99 latency, error rate, RPS, saturation. Add alerting rules for p99 > 500ms and error rate > 1%." },
  { id: 34, title: "Two-Phase Commit Protocol", cat: "Distributed Systems", stack: "Python", prompt: "Implement 2PC (Two-Phase Commit) with a coordinator and 3 participant nodes using Python threads. Phase 1: coordinator sends PREPARE, participants vote yes/no. Phase 2: coordinator sends COMMIT or ABORT. Simulate: participant crash after vote, coordinator crash after phase 1. Show why 2PC blocks when coordinator crashes. Then demonstrate how 3PC or Saga avoids blocking." },
  { id: 35, title: "Reverse Proxy from Scratch", cat: "Infrastructure", stack: "Python", prompt: "Build a reverse proxy server in pure Python using sockets. Features: route requests to upstream servers by path prefix, round-robin load balancing, health checking upstreams (remove failed), sticky sessions via cookie, HTTP/1.1 keep-alive. Log each request with upstream selection reason. Show failover when one upstream goes down mid-traffic." },
  { id: 36, title: "Schema Registry for Events", cat: "Messaging", stack: "Python + FastAPI + Avro", prompt: "Build a schema registry service. Producers register Avro schemas. Consumer validates incoming messages against registered schema. Features: schema versioning, backward/forward/full compatibility checks, schema evolution demo (add nullable field = backward compatible, add required field = breaking). Show: producer and consumer with different schema versions coexisting." },
  { id: 37, title: "Bulkhead Pattern", cat: "Distributed Systems", stack: "Python + FastAPI", prompt: "Implement the Bulkhead pattern to isolate failures. Separate thread pools for: critical-path requests (checkout), standard requests (browse), background jobs (reports). Each pool has a queue with max size. When reports overload, it doesn't starve checkout threads. Show: without bulkhead (one slow endpoint kills all), with bulkhead (isolation). Measure response times under load." },
  { id: 38, title: "Full-Text Search Engine", cat: "Database", stack: "Python + Elasticsearch", prompt: "Build a full-text search API over a product catalog (10k products). Implement: multi-field search with boosting (title > description), faceted filtering (category, price range), typo tolerance (fuzziness), search-as-you-type (completion suggester), highlighting matched terms. Compare query performance with PostgreSQL ILIKE. Show the inverted index concept with a tiny manual example." },
  { id: 39, title: "Multi-Tenant Database Design", cat: "Database", stack: "Python + PostgreSQL + FastAPI", prompt: "Implement 3 multi-tenancy strategies side-by-side: (1) row-level (tenant_id column + RLS policy), (2) schema-per-tenant, (3) separate DB per tenant. Benchmark: query isolation, migration complexity, storage overhead. Build a tenant routing middleware. Show: data leak prevention, tenant onboarding automation, cross-tenant analytics query on strategy 1." },
  { id: 40, title: "Webhook Delivery System", cat: "Messaging", stack: "Python + FastAPI + Celery + PostgreSQL", prompt: "Build a reliable webhook delivery system. Features: HMAC-SHA256 signature on payload, delivery with exponential backoff retry (1s, 5s, 25s, 125s), dead-letter queue after 5 failures, delivery log with response status/body, fan-out to multiple subscribers per event. Simulate: slow consumer (timeout), error 500, success on retry. Show exactly how retry scheduling works." },
  { id: 41, title: "CDN Cache Invalidation", cat: "Infrastructure", stack: "Python + FastAPI + Nginx", prompt: "Simulate a CDN with edge caching. Build: origin server, Nginx as CDN edge with proxy_cache. Implement: cache-control headers (max-age, s-maxage, stale-while-revalidate), surrogate keys for tag-based invalidation, purge API that invalidates by tag (invalidate all pages containing product:123). Show: stale content served during high load (stale-while-revalidate), and purge propagation." },
  { id: 42, title: "Outbox Pattern", cat: "Messaging", stack: "Python + PostgreSQL + Kafka", prompt: "Implement the Transactional Outbox pattern. When saving an order to DB, also insert an event into an outbox table in the same transaction. A poller reads unprocessed outbox events and publishes to Kafka, then marks them done. Show: without outbox (DB commit succeeds but Kafka publish fails → lost event), with outbox (guaranteed delivery). Add CDC using Debezium as an alternative." },
  { id: 43, title: "Zero-Downtime Deployment", cat: "Infrastructure", stack: "Python + Docker + Nginx", prompt: "Implement blue-green deployment with zero downtime. Two environments: blue (current), green (new). Steps: deploy to green, run smoke tests, shift traffic from blue to green via Nginx upstream swap, keep blue warm for rollback. Show: in-flight request handling during switch, database migration compatibility (backward-compatible schema change required). Script the entire deploy + rollback flow." },
  { id: 44, title: "Quadtree Spatial Index", cat: "Database", stack: "Python", prompt: "Build a quadtree from scratch for 2D spatial queries. Insert 10,000 random points. Implement: range query (find all points in bounding box), nearest-neighbor search, node splitting when capacity exceeded. Compare query time: quadtree vs linear scan. Visualize tree structure showing how space is partitioned. Explain how PostGIS uses similar structures for geo queries." },
  { id: 45, title: "RBAC Authorization System", cat: "Auth & Security", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a Role-Based Access Control system. Entities: User, Role, Permission, Resource. Features: role hierarchy (admin inherits manager permissions), resource-instance permissions (user owns their own posts), permission check middleware, permission caching with Redis, audit log of all permission checks. Build a test suite showing: allow cases, deny cases, inherited permissions, ownership checks." },
  { id: 46, title: "Partition Tolerance Demo (CAP)", cat: "Distributed Systems", stack: "Python", prompt: "Build a 3-node key-value store that demonstrates CAP theorem tradeoffs. Implement: CP mode (refuse writes during partition to stay consistent), AP mode (accept writes during partition, merge conflicts after heal). Simulate network partition with socket blocking. Show: CP node rejects write with 503, AP node accepts write but diverges. Then demonstrate reconciliation. Make the tradeoff visceral with logs." },
  { id: 47, title: "Vector Database & Semantic Search", cat: "Database", stack: "Python + FastAPI + Qdrant", prompt: "Build a semantic search service for documents. Embed documents using sentence-transformers (all-MiniLM-L6-v2, runs locally). Store vectors in Qdrant. Implement: semantic search (cosine similarity), hybrid search (BM25 + vector), metadata filtering, pagination. Compare: keyword search vs semantic search on same queries. Show how embedding dimension affects accuracy vs storage." },
  { id: 48, title: "Message Deduplication", cat: "Messaging", stack: "Python + Redis + RabbitMQ", prompt: "Build a message deduplication layer for a consumer. Producer sends messages with a message_id. Consumer checks Redis bloom filter before processing. If duplicate: ack and skip. If not seen: process and add to bloom filter + exact-match set. Show: at-least-once delivery causing duplicate processing without deduplication, then show it being handled. Measure false positive rate under load." },
  { id: 49, title: "Observability: SLI/SLO/Error Budget", cat: "Observability", stack: "Python + Prometheus + Grafana", prompt: "Implement SLI/SLO tracking for a service. SLI: availability (5xx rate), latency (p99 < 300ms). SLO: 99.9% availability. Calculate error budget (0.1% = 43.8 min/month). Build Grafana dashboard showing: 30-day rolling SLI, error budget burn rate, burn rate alert (fast burn = 2% budget in 1hr triggers page). Show what happens when error budget runs out." },
  { id: 50, title: "Streaming Data Pipeline", cat: "Messaging", stack: "Python + Kafka + Faust", prompt: "Build a streaming pipeline for clickstream events. Events: page_view, add_to_cart, purchase. Use Faust (stream processing). Implement: session windowing (30min inactivity = new session), funnel analysis (view→cart→purchase rates), real-time aggregation (items per minute), exactly-once processing. Show: late-arriving events, out-of-order handling, watermark concept." },
  { id: 51, title: "Config Management Service", cat: "Infrastructure", stack: "Python + FastAPI + Vault", prompt: "Build a config management service backed by HashiCorp Vault. Features: secrets with versioning, dynamic secrets (generate DB credentials on demand, expire after 1hr), config namespacing (app/env/key), secret rotation without restart (app polls for changes), audit trail of all secret accesses. Show: hardcoded secrets (bad) vs dynamic Vault secrets (good). Include Vault policy examples." },
  { id: 52, title: "GraphQL Subscriptions", cat: "API Design", stack: "Python + Strawberry + Redis", prompt: "Build real-time GraphQL subscriptions for a stock ticker. Subscription: onPriceUpdate(symbol: String!) → PriceEvent. Backend: price simulation publishes to Redis Pub/Sub. Subscription resolver listens and filters by symbol. Support: multiple subscribers to same symbol (fan-out), subscriber disconnect cleanup, subscription depth limiting. Show WebSocket frames going over the wire." },
  { id: 53, title: "Sticky Sessions & Session Affinity", cat: "Infrastructure", stack: "Python + FastAPI + Nginx + Redis", prompt: "Demo sticky sessions vs. shared session store. Setup: 3 app instances behind Nginx. Strategy 1: IP hash for sticky sessions (show it breaks when instance restarts). Strategy 2: Redis-backed sessions (instance-independent). Implement: session creation, validation, expiry, concurrent session limit (max 3 devices). Show session data after node failure in both strategies." },
  { id: 54, title: "Dead Letter Queue Handler", cat: "Messaging", stack: "Python + RabbitMQ", prompt: "Build a DLQ (Dead Letter Queue) processing system. Messages that fail 3 times go to DLQ. DLQ processor: inspect dead letters, classify by error type (transient vs permanent), retry transient after delay, alert on permanent failures, provide a UI to manually re-queue or discard. Show: poison message detection (always fails), successful retry after transient failure, DLQ growth dashboard." },
  { id: 55, title: "Write-Ahead Log (WAL) Demo", cat: "Database", stack: "Python", prompt: "Implement a mini database with Write-Ahead Logging. Before modifying data files, write to WAL. Implement: WAL append (operation, before-image, after-image, transaction_id), fsync semantics, crash recovery (replay WAL on startup), checkpoint (flush dirty pages, truncate WAL). Simulate a crash mid-transaction and show recovery restoring consistent state. Explain how PostgreSQL uses WAL." },
  { id: 56, title: "Reactive Backpressure", cat: "Messaging", stack: "Python + asyncio", prompt: "Demonstrate backpressure in async pipelines. Build producer → transform → consumer pipeline. Producer is faster than consumer. Without backpressure: queue grows unbounded → OOM. With backpressure: use asyncio.Queue with maxsize, producer awaits when queue full. Implement: bounded queue, slow consumer detection, adaptive producer rate. Show memory usage difference with charts printed to terminal." },
  { id: 57, title: "Kubernetes Operator Pattern", cat: "Infrastructure", stack: "Python + kopf + Kubernetes", prompt: "Build a simple Kubernetes operator using kopf (Python). Custom Resource Definition: ManagedDatabase. Operator watches for create/update/delete events and: creates a PostgreSQL pod + service, creates a K8s Secret with credentials, handles updates (scale replicas), handles deletion (cleanup). Explain the reconciliation loop concept. Run locally with kind or minikube." },
  { id: 58, title: "Immutable Infrastructure Demo", cat: "Infrastructure", stack: "Python + Docker + Terraform", prompt: "Show immutable infrastructure: never modify a running container, always replace. Build: a Dockerized FastAPI app, Terraform config to run it on a VM, a deploy script that provisions a NEW instance with the new image, switches a load balancer target group, then destroys the old instance. Contrast with: SSH-and-update (mutable). Show configuration drift on the mutable approach." },
  { id: 59, title: "CRDTs (Conflict-Free Replicated Data Types)", cat: "Distributed Systems", stack: "Python", prompt: "Implement 3 CRDT types: G-Counter (grow-only, distributed increment), PN-Counter (increment + decrement), OR-Set (add/remove set with unique tags). Run 2 replica nodes, let them diverge, then merge. Show: increments on both replicas merge to correct sum, concurrent add + remove on OR-Set resolves correctly. Explain why CRDTs enable AP systems with eventual consistency." },
  { id: 60, title: "API Versioning Strategies", cat: "API Design", stack: "Python + FastAPI", prompt: "Implement 3 API versioning strategies on the same FastAPI app: (1) URL path (/v1/users, /v2/users), (2) header versioning (Accept: application/vnd.myapi.v2+json), (3) query param (?version=2). For each: show how to route to different handler versions, how to handle deprecated fields, sunset headers, how to run v1 and v2 simultaneously without code duplication. Include a migration guide generator." },
  { id: 61, title: "Optimistic vs Pessimistic Locking", cat: "Database", stack: "Python + PostgreSQL + FastAPI", prompt: "Build a ticket booking system demonstrating both locking strategies. Pessimistic: SELECT FOR UPDATE locks the row until transaction commits (show blocking). Optimistic: version column — UPDATE ... WHERE version=N fails if another transaction updated (show retry loop). Simulate 100 concurrent bookings for 10 seats. Show: deadlock with pessimistic, retry storms with optimistic, and how to choose." },
  { id: 62, title: "Geo-Distributed Routing", cat: "Infrastructure", stack: "Python + FastAPI + Docker", prompt: "Simulate latency-based routing across 3 regions (US, EU, APAC). Each region is a Docker container with an artificial latency modifier. A global load balancer measures latency to each region and routes new requests to the closest. Implement: anycast simulation, health check per region, failover to next-closest if primary fails. Show request routing decisions in logs with latency measurements." },
  { id: 63, title: "Message Ordering Guarantees", cat: "Messaging", stack: "Python + Kafka", prompt: "Demonstrate Kafka ordering guarantees. Produce events for 10 users (user_id as partition key). Show: within-partition ordering guaranteed, across-partition ordering not guaranteed. Implement: a consumer that handles out-of-order messages using sequence numbers, a reordering buffer with timeout, exactly-once semantics using Kafka transactions. Show what happens with consumer group rebalancing." },
  { id: 64, title: "Search Autocomplete (Trie)", cat: "Performance", stack: "Python + Redis + FastAPI", prompt: "Build a search autocomplete service. Data structure: Trie stored in Redis (using sorted sets per prefix with frequency scores). Implement: insert term with frequency, top-K suggestions for prefix (in <5ms), incremental updates as users search. Compare: Trie vs prefix scan on sorted set vs PostgreSQL ILIKE. Add: personalized suggestions (user search history blended with global). Benchmark at 1k QPS." },
  { id: 65, title: "Chaos Engineering Toolkit", cat: "Observability", stack: "Python + FastAPI", prompt: "Build a chaos engineering middleware for FastAPI. Faults injectable via API: latency injection (add N ms delay), error injection (return 500 with probability P), packet loss simulation (drop request with probability P), memory pressure (allocate and hold memory), CPU spike. Add a chaos schedule (run fault for 10min, observe, stop). Show: without resilience patterns (cascade failure), with circuit breaker (graceful degradation)." },
  { id: 66, title: "Protocol Buffers vs JSON Benchmark", cat: "API Design", stack: "Python + Protobuf + FastAPI", prompt: "Build the same API in both JSON and Protobuf. Serialize/deserialize 1M messages of varying sizes (small: 5 fields, large: 50 fields with nested objects). Measure: serialization time, deserialization time, payload size, memory usage. Show Protobuf's field numbering and why field names aren't sent on the wire. Benchmark HTTP/2 with Protobuf vs HTTP/1.1 with JSON." },
  { id: 67, title: "Raft Consensus Algorithm", cat: "Distributed Systems", stack: "Python", prompt: "Implement a simplified Raft consensus algorithm with 5 nodes. Implement: leader election (term, requestVote, timeout randomization), log replication (appendEntries, commit index), handling node failures (candidate fails to get quorum). Show: initial election, log replication of 10 commands, leader crash and re-election. Log every message with term numbers. This is the core of etcd/CockroachDB." },
  { id: 68, title: "Database Sharding", cat: "Database", stack: "Python + PostgreSQL", prompt: "Build a horizontal sharding layer for a users table across 4 PostgreSQL instances. Implement: hash sharding on user_id, shard router middleware, cross-shard query fan-out (query all shards and merge), resharding simulation (add 5th shard, migrate data). Show: hot shard problem with range sharding, why consistent hashing helps. Include: connection pool per shard, shard metadata registry." },
  { id: 69, title: "API Contract Testing", cat: "API Design", stack: "Python + Pact", prompt: "Implement consumer-driven contract testing using Pact. Consumer (frontend) defines expected API shape. Provider (backend) verifies it meets the contract. Show: consumer generates pact file, provider runs pact verification, contract broker for sharing pacts, what happens when provider breaks the contract. Contrast with E2E testing: faster, no real server needed. Add to CI pipeline." },
  { id: 70, title: "Service Discovery", cat: "Infrastructure", stack: "Python + Consul + FastAPI", prompt: "Build service discovery using Consul. Services register on startup with health check endpoint. Client uses Consul DNS or HTTP API to discover service addresses. Implement: service registration, deregistration on shutdown, health check failure removes service, load-balanced client (round-robin over healthy instances). Show: service comes up → registered → traffic flows → goes down → auto-deregistered. No hardcoded IPs." },
  { id: 71, title: "Recursive DNS Resolver", cat: "Infrastructure", stack: "Python", prompt: "Build a recursive DNS resolver from scratch using UDP sockets. Start with a root nameserver, follow referrals to TLD server, then authoritative server. Implement: caching with TTL, negative caching, A/AAAA/CNAME/MX record parsing. Show the exact resolution path for google.com with timing at each step. Explain the difference between recursive and iterative resolution." },
  { id: 72, title: "API Pagination Patterns", cat: "API Design", stack: "Python + FastAPI + PostgreSQL", prompt: "Implement 3 pagination strategies: (1) offset/limit (show phantom records bug on concurrent inserts), (2) cursor-based (keyset pagination — show stable results + how to encode cursor), (3) time-based (before/after timestamps). Benchmark each on 1M rows at page 1 vs page 10,000. Show: offset scan degrades with depth, cursor doesn't. Add: total count estimation without COUNT(*)." },
  { id: 73, title: "Serverless Function Cold Start", cat: "Infrastructure", stack: "Python + AWS Lambda (or LocalStack)", prompt: "Demo serverless cold start problem and mitigations. Deploy a FastAPI app as a Lambda. Measure: cold start (first invoke after idle) vs warm start. Mitigations: provisioned concurrency, Lambda snapstart, keeping handler outside function scope, connection pooling with RDS Proxy. Show: cold start with and without VPC (VPC adds ~1s). Build a keep-warm pinger and measure its effect." },
  { id: 74, title: "Distributed Rate Limiting", cat: "Performance", stack: "Python + Redis + FastAPI", prompt: "Implement distributed rate limiting across 3 API server instances. Strategies: (1) Fixed window in Redis (show boundary spike exploit), (2) Sliding window log (show memory cost), (3) Sliding window counter (best tradeoff). Use Redis Lua scripts for atomic operations. Show: rate limit is correctly enforced when requests hit different servers. Add: per-user, per-IP, and global rate limits simultaneously." },
  { id: 75, title: "Materialized View Refresh", cat: "Database", stack: "Python + PostgreSQL", prompt: "Build a system that maintains materialized views for a dashboard. Views: daily_sales_summary, product_rankings, user_cohorts. Implement: full refresh (simple but slow), incremental refresh using event log (fast but complex), scheduled refresh via cron, on-demand refresh triggered by data changes. Show: stale data window for each strategy, refresh time comparison, lock behavior during refresh." },
  { id: 76, title: "Load Testing Framework", cat: "Observability", stack: "Python + locust", prompt: "Build a custom load testing framework and run it against a FastAPI service. Test scenarios: ramp-up (0 → 100 users over 60s), spike (sudden 10x load), soak test (constant load for 30min). Measure: RPS, p50/p95/p99 latency, error rate, saturation point. Find the breaking point. Then add connection pooling, caching, async handlers — and show the improvement in the same charts." },
  { id: 77, title: "Zero Trust Network Access", cat: "Auth & Security", stack: "Python + FastAPI + mTLS", prompt: "Implement zero trust principles for service-to-service communication. All services must: authenticate with client certificate (mTLS), present a short-lived JWT (SPIFFE/SVID style), be verified against a service registry. Show: request without cert (rejected), expired JWT (rejected), valid cert but wrong service trying to access admin endpoint (rejected by policy). Generate certificates with Python cryptography library." },
  { id: 78, title: "Event Store with Snapshots", cat: "Database", stack: "Python + PostgreSQL", prompt: "Build an event store for a shopping cart. Events: CartCreated, ItemAdded, ItemRemoved, CouponApplied. Store events append-only. Rebuild cart state by replaying. Add snapshots: every 50 events, save current state snapshot. On load: find latest snapshot, replay only events after it. Show: replay time with and without snapshots, how snapshot doesn't break event history, snapshot versioning." },
  { id: 79, title: "Async Task Queue with Workers", cat: "Infrastructure", stack: "Python + Celery + Redis + Flower", prompt: "Build a task queue system with Celery. Tasks: image resizing (slow), email sending (fast), report generation (very slow). Workers: dedicate worker pools per task type using queues. Show: task routing, priority queues, task chaining (chord/group), progress tracking, retry with dead-letter, task revocation. Monitor with Flower. Simulate: worker crash during long task — show retry behavior." },
  { id: 80, title: "Gossip Protocol", cat: "Distributed Systems", stack: "Python", prompt: "Implement a gossip protocol for cluster membership across 10 nodes. Each node periodically picks 3 random peers and exchanges its membership view (node → heartbeat counter). Nodes detect failures when heartbeat stops incrementing. Show: node joins cluster and knowledge propagates in O(log N) rounds, node fails and all nodes detect it within 3 gossip rounds. This is how Cassandra and Redis Cluster work." },
  { id: 81, title: "Content Negotiation", cat: "API Design", stack: "Python + FastAPI", prompt: "Build an API that supports content negotiation. Same endpoint returns different formats based on Accept header: application/json, application/xml, text/csv, application/msgpack. Also: language negotiation (Accept-Language: fr, en-US;q=0.9), API version via Accept (application/vnd.myapi.v2+json), compression negotiation (Accept-Encoding: br, gzip). Show: client preference ranking, fallback to default format, 406 Not Acceptable handling." },
  { id: 82, title: "Differential Sync (OT)", cat: "Distributed Systems", stack: "Python", prompt: "Implement Operational Transformation for collaborative text editing (simplified Google Docs). Operations: insert(pos, text), delete(pos, len). Transform function: adjust operation positions when two concurrent ops conflict. Show: two users editing same document simultaneously, ops sent to server, transformed and applied, both clients converge to same state. Add: cursor position transformation. This is the core of collaborative editors." },
  { id: 83, title: "Secrets Rotation", cat: "Auth & Security", stack: "Python + FastAPI + Vault", prompt: "Build automated secret rotation without downtime. Rotate: DB passwords (create new user, update app config, verify, drop old user), API keys (dual-validity window — both old and new keys work during rotation), JWT signing keys (support multiple active keys by kid header, retire old key after 24hr). Show: zero dropped requests during rotation. Schedule rotation with a cron job. Alert if rotation fails." },
  { id: 84, title: "Backoff and Retry Strategies", cat: "Performance", stack: "Python", prompt: "Implement and compare retry strategies: fixed delay, linear backoff, exponential backoff, exponential with jitter (full jitter, equal jitter, decorrelated jitter). Simulate: 10 clients all hitting a recovering server simultaneously. Show: without jitter → thundering herd re-crashes server, with jitter → smooth recovery. Implement retry budget (max X% of requests can be retries) to prevent retry amplification." },
  { id: 85, title: "Database Trigger Audit Log", cat: "Database", stack: "Python + PostgreSQL", prompt: "Build an audit log system using PostgreSQL triggers. For any INSERT/UPDATE/DELETE on sensitive tables (users, orders, payments): trigger fires, writes to audit_log (table, operation, old_row, new_row, user_id, timestamp). Implement: row-level security so users can't modify audit_log, audit log querying API, report of all changes to a specific record, monthly compliance export. Show: trigger fires even on direct DB access." },
  { id: 86, title: "Long Polling vs Server-Sent Events vs WebSocket", cat: "API Design", stack: "Python + FastAPI", prompt: "Build the same notification system 3 ways: (1) long polling (client holds connection, server responds when event ready), (2) SSE (server pushes events, client reconnects), (3) WebSocket (bidirectional). For each: show connection overhead, latency, server resource usage, proxy/firewall compatibility. Load test: 1000 concurrent subscribers, compare memory and CPU per strategy. Show when to use each." },
  { id: 87, title: "GDPR Data Subject Rights API", cat: "Auth & Security", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a GDPR compliance API. Endpoints: /export (collect all user data from all tables and services, return as JSON), /delete (anonymize or delete data, cascade to related tables, send confirmation), /portability (export in machine-readable format), /restrict-processing (flag in all services). Show: data spread across 5 tables, export collects everything, delete leaves audit trail, verify deletion with re-export." },
  { id: 88, title: "Adaptive Concurrency Limiter", cat: "Performance", stack: "Python + FastAPI", prompt: "Build an adaptive concurrency limiter (like Netflix Concurrency Limits library). Algorithm: track inflight requests and latency. If latency increases, reduce concurrency limit (Vegas algorithm). If latency decreases, increase limit. Show: fixed concurrency vs adaptive under variable load. Implement: gradient-based limit calculation, probe cycles to increase limit, automatic reduction under queueing. Compare with static rate limits." },
  { id: 89, title: "Polyglot Persistence Demo", cat: "Database", stack: "Python + PostgreSQL + Redis + Elasticsearch + MongoDB", prompt: "Build a product catalog that uses the right database for each use case: PostgreSQL (transactional: orders, inventory), Redis (cache, session, rate limits), Elasticsearch (full-text search, facets), MongoDB (flexible product attributes: different fields per category). Show: write path (PostgreSQL → trigger → sync to Elasticsearch), read path (search → ES, fetch → PG), caching layer. Include sync lag handling." },
  { id: 90, title: "Infrastructure as Code (Pulumi)", cat: "Infrastructure", stack: "Python + Pulumi + AWS/LocalStack", prompt: "Provision a 3-tier web app using Pulumi (Python). Resources: VPC with public/private subnets, EC2 or ECS for app, RDS in private subnet, ALB, S3 for static assets, CloudFront. Show: Pulumi state management, stack outputs, resource dependencies (RDS created before app), preview before apply, rollback on failure. Compare with Terraform HCL: same infra, different DX. Use LocalStack for local dev." },
  { id: 91, title: "Horizontal Pod Autoscaler Simulation", cat: "Infrastructure", stack: "Python + Kubernetes", prompt: "Build a simulation of HPA (Horizontal Pod Autoscaler) behavior. Deploy a CPU-intensive FastAPI endpoint. Configure HPA: min 2 replicas, max 10, target CPU 50%. Generate load with locust, watch pods scale up. Remove load, watch scale down (with cooldown period). Show: metrics pipeline (metrics-server → HPA controller), stabilization window preventing flapping, scale-to-zero with KEDA." },
  { id: 92, title: "Request Coalescing", cat: "Performance", stack: "Python + asyncio + FastAPI", prompt: "Implement request coalescing (also called request deduplication or single-flight). When 100 concurrent requests arrive for the same cache miss (same key), only ONE request goes to the origin DB — the others wait and share the result. Implement using asyncio.Event or asyncio.Future. Show: without coalescing (100 DB queries), with coalescing (1 DB query, 100 clients get the result). Measure DB load difference." },
  { id: 93, title: "Merkle Tree for Data Integrity", cat: "Distributed Systems", stack: "Python", prompt: "Implement a Merkle tree for data integrity verification. Build: leaf hash = SHA256(data), parent hash = SHA256(left+right), root hash = fingerprint of entire dataset. Use cases: (1) verify a single record changed without re-hashing all data, (2) find which records differ between two DB replicas efficiently, (3) tamper detection. Show how Bitcoin/Git/DynamoDB use Merkle trees for sync and verification." },
  { id: 94, title: "Cold Storage Tiering", cat: "Database", stack: "Python + PostgreSQL + S3", prompt: "Build data tiering for a logs table. Hot tier: PostgreSQL (last 7 days, fully indexed). Warm tier: PostgreSQL partitioned (last 90 days, partial index). Cold tier: S3 as Parquet files (older data, query via Athena). Implement: automatic tiering cron job, transparent query routing (check date range → decide tier), cold data recall to hot tier on demand. Show cost/performance tradeoffs for each tier." },
  { id: 95, title: "Dynamic Plugin System", cat: "Infrastructure", stack: "Python", prompt: "Build a plugin architecture for a data processing pipeline. Core: pipeline engine with stages. Plugins: loaded from .py files at runtime (importlib), register themselves via @plugin decorator, implement a defined interface (process(event) → event). Features: hot reload (detect file change → reload plugin without restart), plugin dependency ordering, sandboxed execution (resource limits), plugin marketplace-style metadata (name, version, author)." },
  { id: 96, title: "Distributed Job Scheduling (DAG)", cat: "Infrastructure", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a DAG-based job scheduler (simplified Airflow). DAG: define tasks and dependencies (A → B, C; B → D; C → D). Scheduler: topological sort, run independent tasks in parallel, wait for dependencies, retry failed tasks. Features: task status tracking, log streaming per task, DAG visualization API, backfill (run DAG for past dates). Show: parallel execution, one task fails → downstream skipped, retry recovers." },
  { id: 97, title: "Canary Deployment", cat: "Infrastructure", stack: "Python + FastAPI + Nginx", prompt: "Implement canary deployment. V1 is current (100% traffic). V2 is new. Gradually shift: 5% → 25% → 50% → 100%. Nginx split_clients or weighted upstream. Monitor: error rate and latency per version. Auto-rollback: if V2 error rate > 2× V1, revert to 0% canary. Show: feature flag to force V2 for specific users (testers), metric comparison dashboard, step-by-step traffic shift script." },
  { id: 98, title: "Session Replay & Debugging", cat: "Observability", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a request session recorder for debugging production issues. Every request: record full request (headers, body, params), response, DB queries executed (with timing), errors raised, user context — stored in a ring buffer (last 10k requests per user). On incident: replay a specific user's session, reproduce exact DB state. Add: replay in staging mode, redact PII before storage, retention policy." },
  { id: 99, title: "Homomorphic-Lite: Encrypted Computation", cat: "Auth & Security", stack: "Python", prompt: "Implement a simplified encrypted computation demo. Use Paillier partially homomorphic encryption: encrypt salary data, compute SUM and AVERAGE without decrypting individual values (only the aggregate is decrypted). Show: analyst queries sum of salaries without seeing any individual salary. Explain: additive homomorphism, where fully homomorphic encryption (FHE) goes further, real-world use cases (private aggregation, federated analytics)." },
  { id: 100, title: "Self-Healing System", cat: "Observability", stack: "Python + FastAPI + Docker", prompt: "Build a self-healing system. Health monitor watches 3 services. Actions: if memory > 80% → trigger GC and log warning, if error rate > 5% → restart the service container (Docker API), if DB connections exhausted → kill idle connections (pg_terminate_backend), if disk > 90% → compress and archive old logs. Show: fault injection → detection → automated remediation → recovery. Log the full healing event timeline." },
  { id: 101, title: "Time-Series Anomaly Detection", cat: "Observability", stack: "Python + Statsmodels + Plotly", prompt: "Build an anomaly detector for metrics using statistical methods. Use ARIMA/Prophet for baseline prediction. Detect anomalies when actual > baseline + 3σ. Implement: sliding window detection, seasonality handling, multiple baseline models, anomaly confirmation (must persist 3+ periods). Use real metric data (CPU, latency). Show: false positive rates per method, how Prophet handles trend+seasonality better than simple stddev." },
  { id: 102, title: "Distributed Transaction Coordinator", cat: "Distributed Systems", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a coordinator for distributed transactions across 3 databases. Implement: 2PC (Two-Phase Commit) with timeout handling, saga pattern as alternative (choreography-based), compensating transaction definitions. Features: transaction log, coordinator state machine, participant state tracking, timeout/restart recovery. Show success, coordinator crash, participant crash scenarios with transaction consistency maintained." },
  { id: 103, title: "Mock Telemetry Backend (OTLP)", cat: "Observability", stack: "Python + FastAPI", prompt: "Build a minimal backend that receives OpenTelemetry Protocol (OTLP) data. Accept gRPC telemetry payloads (traces, metrics, logs). Implement: span storage (in-memory or simple SQLite), metric aggregation (sum, histogram buckets), log ingestion. Build a query API: traces by trace_id, metrics by name and aggregation window, logs with filtering. Compare: raw OTLP payload size vs compressed. This is what Jaeger/Prometheus backends do." },
  { id: 104, title: "Request Tracing with Baggage", cat: "Observability", stack: "Python + FastAPI + W3C Trace Context", prompt: "Implement distributed tracing using W3C Trace Context standard. Each request carries: traceparent (trace-id, parent-id, trace-flags), tracestate. Propagate through 4 services. Add baggage context: user_id, tenant_id, feature_flag_state — accessible to all downstream services without polluting trace. Show: trace visualization, baggage extraction, cross-service context propagation. Explain why baggage exists separate from trace context." },
  { id: 105, title: "Database Query Optimizer Hints", cat: "Database", stack: "Python + PostgreSQL + SQLAlchemy", prompt: "Build a query optimizer tuning system. Developers add query hints: FORCE INDEX, HASH JOIN, NESTED LOOP. Implement: hint parser, hint application via query rewrite, hint storage in code comments. For each query: run with and without hints, measure execution time. Collect statistics: which hints help/hurt. Auto-suggest hints based on query plan analysis. Show: bad query with wrong execution plan, hint forces correct join type, query becomes 10x faster." },
  { id: 106, title: "Cache Coherency Protocol", cat: "Distributed Systems", stack: "Python", prompt: "Implement a simplified cache coherency protocol (like CPU caches or distributed systems use). Write-Through, Write-Back, and MSI (Modified, Shared, Invalid) states. Simulate 3 nodes with replicated caches. Show: write on node A invalidates copies on nodes B+C, reads on B+C cause cache miss → fetch from A, then write on B. Compare protocol overhead vs data consistency guarantees. Explain why it's hard to scale beyond a few nodes." },
  { id: 107, title: "Audit Trail Immutability", cat: "Auth & Security", stack: "Python + PostgreSQL + SQLAlchemy", prompt: "Build an immutable audit trail using append-only logs with cryptographic verification. Each audit entry includes: action, actor, resource, timestamp, previous_hash (links to prior entry). Implementation: BLAKE2 hashing, store hash chain in DB, verify chain integrity. Read-only audit schema enforced by DB role. Show: tampering detection (change one entry → hash chain breaks), compliance-ready reporting, retention policies." },
  { id: 108, title: "Real-Time Analytics Dashboard Backend", cat: "Observability", stack: "Python + FastAPI + WebSocket + Redis", prompt: "Build a real-time analytics dashboard API. Events arrive via HTTP (impressions, clicks, conversions). Aggregate in Redis: count by page/device/user_segment (real-time counters with rolling windows). Clients connect via WebSocket, receive updates every second. Implement: backpressure (limit WebSocket update frequency), data retention (keep last 24hr), bulk backfill on client connect. Show: chart updates in real-time as events arrive, 100 concurrent clients connected." },
  { id: 109, title: "Stateless vs Stateful Architecture", cat: "Infrastructure", stack: "Python + FastAPI + Kubernetes + Redis", prompt: "Refactor a FastAPI app in stages: (1) Stateful: global in-memory dict for session data (fails on pod restart), (2) Redis-backed: session → Redis, survives pod restart but requires Redis, (3) Stateless: session → signed cookie, no server storage. For each: test 2-pod deployment, kill a pod, show session recovery. Measure latency and storage of each. Explain scaling implications (stateful doesn't scale horizontally)." },
  { id: 110, title: "Linearizability vs Serializability", cat: "Distributed Systems", stack: "Python + FastAPI + PostgreSQL", prompt: "Implement a counter service demonstrating linearizability vs serializability differences. Show: serializability (transactions don't interfere as if sequential) but not linearizable (updates not visible in real-time order). Implement: (1) Single-shard writes (linearizable), (2) Multi-replica with async replication (serializable but not linearizable). Trace a scenario where operation order differs from wall-clock order. This is why consensus is hard in distributed systems." },
  { id: 111, title: "Database Sharding with Migration", cat: "Database", stack: "Python + PostgreSQL + FastAPI", prompt: "Build a live sharding migration: move from 1 shard to 2 shards without downtime. Phase 1: Dual-write (new shard gets copy of all data), Phase 2: Verify shards match, Phase 3: Redirect reads to correct shard, Phase 4: Stop writes to old shard. Handle: in-flight requests during migration, query routing middleware, data verification at each phase. Show: no dropped data, no downtime." },
  { id: 112, title: "Feature Store for ML", cat: "Observability", stack: "Python + FastAPI + PostgreSQL + Redis", prompt: "Build a feature store for ML pipelines. Features: user age, items viewed in last 7 days, purchase history aggregates. ML model needs features at prediction time. Implement: online store (Redis for <10ms latency) and offline store (PostgreSQL for historical features). Sync: offline → online daily. Track: feature versioning, feature lineage, freshness. Show: prediction latency improvement with online store vs computing on-the-fly." },
  { id: 113, title: "Batch Processing with Checkpointing", cat: "Infrastructure", stack: "Python + PostgreSQL", prompt: "Build a batch job that processes 1M records (e.g., data transformation). Implement: checkpoint every 10k records (state saved to DB), resumable from checkpoint on failure, exactly-once processing semantics. Add: progress tracking, ETA calculation, graceful shutdown (finish current batch → checkpoint → exit). Simulate job crash at record 35k — show resumption from checkpoint 30k without reprocessing. Compare with at-least-once (duplicates)." },
  { id: 114, title: "GraphQL N+1 Query Prevention", cat: "API Design", stack: "Python + Strawberry GraphQL + SQLAlchemy", prompt: "Build a GraphQL API for Blog (Authors, Posts, Comments). Implement: DataLoader for batching, query complexity scoring (prevent expensive queries), depth limiting, field-level caching. Show: naive resolver causing 100 DB queries, optimized with DataLoader (5 queries), enforce limits preventing malicious depth-5 queries. Include: cost estimation algorithm, caching strategy for nested objects." },
  { id: 115, title: "Request Deduplication at API Gateway", cat: "API Design", stack: "Python + FastAPI + Redis", prompt: "Build an API Gateway that deduplicates requests based on: Content-MD5 header + request path + method. Window: 5 minutes. Duplicate request returns cached response without processing. Use cases: idempotent writes that may be retried. Show: client retries request 3 times due to network timeout, second/third retries get cached response immediately. Add: dedupe key customization per route." },
  { id: 116, title: "Encrypted Search", cat: "Auth & Security", stack: "Python + FastAPI + Searchable Encryption", prompt: "Build a search system over encrypted data. Implement: deterministic encryption for searchable fields (email, username). Encrypted data is stored, but you can still search by encrypted value. Show: client encrypts query → server finds matching encrypted records → returns encrypted results → client decrypts. Explain: trade-off vs full homomorphic encryption (practical vs theoretical). Implement basic pattern matching without decryption." },
  { id: 117, title: "Consensus in Distributed Systems", cat: "Distributed Systems", stack: "Python + paxos-lite", prompt: "Implement simplified Paxos (or Raft alternative). Setup: 5 nodes voting on a list of values. Roles: Proposer, Acceptor, Learner. Show: leader proposes value, quorum accepts, all learners get consensus value. Simulate: node crash after propose, network partition (only 3 nodes can communicate). Explain: why quorum (N/2+1) prevents split-brain. This underlies Google Chubby, Apache ZooKeeper." },
  { id: 118, title: "Password-Based Key Derivation", cat: "Auth & Security", stack: "Python + Argon2 + FastAPI", prompt: "Implement password hashing/verification with key derivation. Use Argon2id (memory-hard, resistant to GPU brute-force). Features: salt generation (CSPRNG), work factor tuning (memory, iterations, parallelism), upgrade mechanism (re-hash login with new params). Compare: bcrypt vs scrypt vs Argon2 in terms of speed, memory, crack resistance. Show: brute force attack taking 10 years vs 1 year with weaker settings." },
  { id: 119, title: "Streaming Decompression Pipeline", cat: "Performance", stack: "Python + AsyncIO", prompt: "Build a streaming decompression pipeline for large files. Process gzip-compressed data in 64KB chunks without loading full file into memory. Implement: async file I/O, streaming decompression, downstream processing in parallel. Support: gzip, brotli, zstd (pluggable compressors). Measure: throughput (MB/s), memory usage (peak vs avg). Compare: load-all-then-decompress vs streaming." },
  { id: 120, title: "Rate Limiting with Token Refresh", cat: "Performance", stack: "Python + FastAPI + Redis", prompt: "Build an adaptive rate limiter that refreshes tokens in background. Client gets quota (e.g., 100 req/min). Backend refreshes unused quota every 10s → quota doesn't accumulate to max (prevent burst). Leaky bucket with smart refill. Show: fair usage under sustained vs bursty load. Implement: priority queues (premium users get more quota), quota sharing between related users." },
  { id: 121, title: "Dependency Injection Framework", cat: "Infrastructure", stack: "Python", prompt: "Build a simple DI container. Decorators: @injectable (mark class for DI), @inject (constructor injection). Features: singleton/transient lifecycle, cyclic dependency detection, autowiring by type, manual binding overrides. Show: loose coupling (swap implementations), testing (inject mocks). Compare with FastAPI's Depends. Implement: resolution cache, graph-based dependency ordering." },
  { id: 122, title: "Load Shedding with Graceful Degradation", cat: "Performance", stack: "Python + FastAPI", prompt: "Implement load shedding under overload. Tiers: (1) Normal (process all), (2) Load Shed v1 (reject lowest-priority requests), (3) Load Shed v2 (enable fast-path mode — skip optional computation), (4) Circuit Break (reject all). Monitor: CPU, queue depth, latency p99. Auto-transition between tiers. Show: under high load, priority=high requests still get processed while priority=low are rejected. Measure: system latency stabilizes." },
  { id: 123, title: "Recency-Biased Cache", cat: "Performance", stack: "Python + Redis", prompt: "Build a cache backend that favors recent items (LIRS: Low Inter-reference Recency Set). Tracks: recency (how recently accessed), inter-reference recency (how far apart recent accesses). Hot items stay longer despite infrequent access if recent. Implement: heated set (frequent + recent) vs cold set (infrequent). Compare LRU vs LIRS on a workload with temporal locality. Real-world: improves hit rate on database query caching." },
  { id: 124, title: "Multi-Tenant Isolation", cat: "Auth & Security", stack: "Python + FastAPI + PostgreSQL + Row-Level Security", prompt: "Secure multi-tenant database isolation. Implement: row-level security policies (WHERE tenant_id = current_setting('app.tenant_id')), audit logging per tenant, cross-tenant data leak prevention (verify in application layer too). Attack: try to access another tenant's data via SQL injection / parameter tampering. Show: RLS blocks access, audit log records attempt. Include: tenant data segregation verification tests." },
  { id: 125, title: "Circuit Breaker Advanced Patterns", cat: "Performance", stack: "Python + FastAPI", prompt: "Build advanced circuit breaker: (1) Basic CLOSED→ OPEN→HALF_OPEN, (2) With slow-down (failures reduce concurrency quota before opening), (3) With context awareness (different CB per dependency), (4) Adaptive thresholds (failure threshold adjusts based on latency). Show: one failing dependency doesn't crash others, client itsself degrades gracefully before full circuit open." },
  { id: 126, title: "Structured Concurrency", cat: "Infrastructure", stack: "Python + anyio + async", prompt: "Implement structured concurrency principles. Use nurseries to manage task lifecycles. Build: 3 concurrent tasks in a nursery, one fails → all get cancelled cleanly. Implement: cancellation propagation, timeout handling, exception aggregation. Show: difference vs bare threading (unstructured — hard to cleanup). Use this pattern in an HTTP crawler: parent context → child tasks per URL → clean cancellation." },
  { id: 127, title: "Database View Materialization", cat: "Database", stack: "Python + PostgreSQL", prompt: "Build a system that materializes views on-demand. View definition: complex JOIN + aggregation (slow). Strategy: user queries view → cache miss → compute result in background → return cached result on next query. Implement: cache invalidation triggers (watch source tables), refresh scheduler, stale-while-recompute. Show: first query slow (1s), subsequent queries fast (1ms). Measure freshness vs speed tradeoff." },
  { id: 128, title: "Request Context Propagation", cat: "Infrastructure", stack: "Python + contextvars", prompt: "Implement context propagation through async call stacks using contextvars. Store: request_id, user_id, tenant_id, feature_flags → context → accessible in all async tasks without passing parameters. Build: async function calls 3 levels deep, all can access context. Show: context survives thread pool execution, context cleanup after request, context isolation between concurrent requests. This is how frameworks like FastAPI handle request scope." },
  { id: 129, title: "Chaos Mesh Simulation", cat: "Observability", stack: "Python + Kubernetes simulation", prompt: "Simulate chaos engineering experiments. Fault types: kill pod, network latency (add 500ms), network loss (drop 10% packets), CPU throttling (limit CPU). Run experiment on 3-pod service: introduce latency, watch requests degrade gracefully (circuit breaker kicks in), recover when chaos stops. Log: when fault started, how long to detect, recovery time. This is production readiness testing." },
  { id: 130, title: "API Monetization & Rate Plan", cat: "API Design", stack: "Python + FastAPI + Stripe API", prompt: "Build an API monetization layer. Plans: free (100 req/day), pro ($10/mo = 10k req/day), enterprise (custom). Implement: hourly quota tracking, overage fees, plan upgrade/downgrade, trial period (free → paid with 14-day grace). integrate with Stripe for payment. Show: user hits quota → 429 error, can upgrade mid-request, billing reflects usage accurately." },
  { id: 131, title: "Distributed Tracing with Sampling", cat: "Observability", stack: "Python + Jaeger + OpenTelemetry", prompt: "Implement trace sampling to reduce overhead in high-volume systems. Sampling strategies: (1) No sampling (100%), (2) Fixed rate (1%), (3) Adaptive (increase sampl rate if error detected), (4) Tail sampling (sample based on entire trace). Show: 1M req/s, 100% sampling = terabytes collected, 1% sampling = manageable but misses low-volume errors, adaptive = low overhead + high-value traces captured." },
  { id: 132, title: "Change Data Capture (CDC)", cat: "Database", stack: "Python + PostgreSQL + Debezium concept", prompt: "Build a CDC layer to capture DB changes for downstream services. Strategy: transaction log tailing (pg_logical_replication), store changes in outbox table. Services subscribe to change stream. Show: user updates email in DB → CDC captures change → email service receives event → updates email service DB. Implement: exactly-once delivery, backpressure handling, schema versioning. Compare polling (SELECT every N sec) vs CDC (real-time, low overhead)." },
  { id: 133, title: "Billing & Usage Metering Engine", cat: "Infrastructure", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a usage metering engine for subscription billing. Track: API calls, data transfer (GB), compute minutes. Generate monthly invoice based on consumption + plan. Handle: partial-month prorating, plan changes mid-month, credit/refunds. Implement: real-time usage queries (for dashboard), batch invoice generation, webhook notifications for usage alerts. Show: customer used $500 in compute on $200/mo plan — overage charged." },
  { id: 134, title: "Virtual Threads & Green Threads", cat: "Infrastructure", stack: "Python + asyncio simulation", prompt: "Understand threading models: (1) OS threads (expensive, context switching overhead), (2) Green threads/coroutines (cheap, user-mode scheduling), (3) Hybrid. Build a playground: spin up 10k async tasks vs 10k threads. Compare latency and memory. Show: async tasks consume 1MB total, threads consume 100MB+. Explain: why async scales better, when you'd still use threads (blocking I/O libraries), context switching costs." },
  { id: 135, title: "Observability: Custom Metrics Aggregation", cat: "Observability", stack: "Python + FastAPI + Prometheus", prompt: "Design custom metrics for a business problem (e.g., checkout flow). Metrics: conversion_rate (completions/starts), cart_abandonment (added but not checked out), revenue_per_session. Aggregate: per country, per device, per user segment. Implement: real-time update (Prometheus scrape) + batch aggregation (daily). Show: dashboard with funnel analysis, segment comparison, anomaly highlighting (today's checkout rate 50% below 30-day average)." },
  { id: 136, title: "Hot Path Optimization", cat: "Performance", stack: "Python + cProfile + memory_profiler", prompt: "Profile code to find hot paths. Use cProfile to identify functions consuming CPU time. Use memory_profiler on memory allocations. Implement: algorithmic optimization (e.g., O(n²) → O(n log n)), caching repeated computations, lazy evaluation, vectorization with NumPy. Show before/after: same code, 5x speedup. Teach: profiling discipline, Amdahl's law (optimizing 10% of code isn't worth it)." },
  { id: 137, title: "Testing Strategy for Concurrency", cat: "Observability", stack: "Python + pytest + concurrent.futures", prompt: "Write tests for concurrent code without flakiness. Challenges: race conditions (hard to reproduce), resource cleanup, timeout selection. Techniques: deterministic test seeding, ThreadSanitizer-like detection (track shared state access), stress testing (run 1000x), property testing with Hypothesis. Show: broken code that passes 99% of runs, then fix to pass 99.99% consistently." },
  { id: 138, title: "Request Signing & Webhook Verification", cat: "Auth & Security", stack: "Python + FastAPI + HMAC", prompt: "Implement webhook delivery with cryptographic signing. Sender: HMAC-SHA256 signature over payload + timestamp, include sig in header. Receiver: verify signature and timestamp (< 5min old). Show: attacker modifies payload → signature mismatch → rejected, attacker replays old webhook → timestamp too old → rejected. Include: signature rotation (key versioning), algorithm agility." },
  { id: 139, title: "Metrics Cardinality Control", cat: "Observability", stack: "Python + Prometheus", prompt: "Learn about cardinality explosion in metrics. Bad: metric with unbounded label (url_path → billions of unique values if not careful). Impact: memory bloat, query slowdown, cost explosion. Mitigate: (1) Fixed labels only (method, status, path_regex), (2) Cardinality limits (reject new label combinations), (3) Aggregation (drop low-cardinality data). Show: prometheus memory usage before (10GB) vs after (1GB) cardinality control." },
  { id: 140, title: "Circuit Breaker State Machine", cat: "Distributed Systems", stack: "Python", prompt: "Build a formal state machine for circuit breaker with rigorous testing. States: CLOSED, OPEN, HALF_OPEN, + BROKEN (stuck open). Transitions: success_count, failure_count, timeout. Implement each transition explicitly. Property test: from any state, I can reach any state (reachability), cyclic transitions are finite (no infinite loops). This foundations understanding makes debugging production circuit breakers easier." },
  { id: 141, title: "Dynamic Threshold Alerting", cat: "Observability", stack: "Python + FastAPI + Prometheus", prompt: "Implement smart alerting that adapts thresholds over time. Baseline: MAD (Median Absolute Deviation) computed daily based on historical distribution. Alert fires if metric deviates 3× MAD. Show: weekend traffic pattern different from weekday — old static threshold (500 req/s) generates false positives on weekends, dynamic threshold adjusts (weekend baseline = 200 req/s)." },
  { id: 142, title: "Message Ordering with Event Versioning", cat: "Messaging", stack: "Python + Kafka", prompt: "Handle message ordering guarantees with versioned events. Events should be processed in order + tolerate out-of-order delivery. Implement: sequence number per stream, reordering buffer (hold out-of-order up to N messages, timeout if not received), idempotent handler (same message twice = same result). Show: messages 1,3,2,4 arriving out of order, correctly reordered and processed." },
  { id: 143, title: "Replica Synchronization Protocol", cat: "Distributed Systems", stack: "Python + sockets", prompt: "Build a primary-replica sync protocol. Write on primary → batched write to replicas → ACK when quorum confirms. Read on primary (strong consistency) vs read on replica (eventual consistency). Show: client reads from replica while primary is writing (stale read), or reads primary after write (fresh). Implement: configurable consistency level per query." },
  { id: 144, title: "Time Synchronization (NTP)", cat: "Infrastructure", stack: "Python", prompt: "Understand NTP and clock skew issues in distributed systems. If clocks differ by 1 second across 3 nodes, version vectors give wrong ordering. Implement: clock offset detection (exchange timestamps with peers), gradual clock adjustment (don't jump), outlier rejection (don't trust obviously wrong clocks). Show: without NTP (events recorded out of order), with NTP (correct order). Explain: why GPS + atomic clocks needed for data centers." },
  { id: 145, title: "Dead Code Elimination in Monitoring", cat: "Observability", stack: "Python + Prometheus", prompt: "Clean up old metrics from codebase. Challenges: how to know if a metric is used (queried)? Solution: log all metric queries to Prometheus query log, correlate with available metrics, flag unused metrics. Build: tool that scans code, extracts metric names, checks query log (last 30 days) for usage, reports unused. Remove dead metrics to reduce cardinality." },
  { id: 146, title: "Compliance as Code", cat: "Auth & Security", stack: "Python + Checkov-like DSL", prompt: "Build a compliance checker that codifies regulations. Define rules: (1) All databases must be encrypted, (2) All secrets must be rotated monthly, (3) Audit logs must be immutable, (4) PII must be classified. Inspector: scan infrastructure code, runtime config, DB state. Report: violations + remediation steps. Show: infra without encryption detected, then fixed to comply. Real-world: this is how tools like Checkov/Terraform Cloud work." },
  { id: 147, title: "Cache Invalidation Strategy", cat: "Performance", stack: "Python + Redis + FastAPI", prompt: "Compare cache invalidation strategies: (1) TTL-based (simple, stale data risk), (2) Event-based (update cache on data change), (3) Hybrid (TTL + purge on change). Show: stale data window for each strategy, consistency guarantee. Implement: publish/subscribe for event-based, purge API for forced invalidation, refresh endpoints. Measure: hit rate, freshness, operational complexity." },
  { id: 148, title: "Chaos Testing Framework", cat: "Observability", stack: "Python + FastAPI", prompt: "Build a framework for injecting faults into a FastAPI system. Faults: latency injection, error injection, memory leak, connection pool exhaustion. Implement: annotation-based (@inject_latency(100ms)), config file-based activation, metrics for fault injection overhead. Show: system survives injected latency (bulkhead prevents cascade), shows graceful degradation. Use for chaos day exercises." },
  { id: 149, title: "Deterministic Simulation Testing", cat: "Observability", stack: "Python", prompt: "Write tests for distributed systems using simulated time (not real time). Test scenarios: node crash at T+5s, network partition at T+10s. Use deterministic random seed to reproduce failures. Implement: virtual clock that advances on events, deterministic message ordering. Show: a bug that only manifests under specific timing can be reproduced reliably. Compare with RNG testing (flaky because real-time dependent)." },
  { id: 150, title: "SLA vs SLO vs SLI in Practice", cat: "Observability", stack: "Python + Prometheus", prompt: "Implement tracking for SLA/SLO/SLI in a real system. SLI: error rate < 0.1 % (measured). SLO: 99.5% availability = 3.6 hours downtime/month (target). SLA: 99.0% = financial penalty (contract). Build dashboard: (1) 30-day SLI trend, (2) error budget consumption rate (fast burn alert at >100%/month), (3) remaining budget, (4) projection (will we hit SLO this month?). Tie to on-call rotation: when budget runs out, freeze deployments." },
  { id: 151, title: "Tail Latency Analysis", cat: "Observability", stack: "Python + OpenTelemetry + Grafana", prompt: "Analyze tail latency (p95, p99, p99.9) vs median. Tail caused by: garbage collection pauses, disk I/O, context switches. Collect: latency histogram, attribute-based breakdown (which requests are slow), SQL query plans for slow queries. Show: median 50ms but p99 500ms — 10x difference warrants investigation. Implement: slow query capture, trace correlation (slow requests often share a pattern)." },
  { id: 152, title: "Quota Management System", cat: "Infrastructure", stack: "Python + FastAPI + PostgreSQL + Redis", prompt: "Build a usage quota system across multiple dimensions. Quotas: API rate (per hour), concurrent connections, data storage (GB), compute budget ($). Implementation: Redis for real-time usage, PostgreSQL for historical, enforcement middleware. Features: quota sharing between users in same account, reservation of quota for upcoming batch job, quota transfer (give unused quota to other user). Show: forecast usage, get warnings before exceeding." },
  { id: 153, title: "Session Management with Distributed State", cat: "Infrastructure", stack: "Python + FastAPI + Redis + Kafka", prompt: "Build distributed session management across 3 geographies. Session created in US → user travels to EU → session must be valid there. Strategy: Redis Cluster with geo replication, or Redis Pub/Sub to replicate session updates. Show: session write in US replicates to EU cache (100ms), ensure EU read gets fresh data. Handle: network partition (EU disconnected), read repair on stale session." },
  { id: 154, title: "Dependency Injection for Configuration", cat: "Infrastructure", stack: "Python + FastAPI + Pydantic", prompt: "Build a config DI system where different environments get different implementations. Configs: DatabaseConfig (dev: SQLite, prod: PostgreSQL), CacheConfig (dev: memory dict, prod: Redis), LogConfig (dev: stdout, prod: Elasticsearch). Use DI to inject correct impl. Add: config validation (pydantic), environment-based selection, secret management. Show: same codebase, different config per environment, no hardcoded values." },
  { id: 155, title: "Replay Buffer for Testing", cat: "Observability", stack: "Python + pytest", prompt: "Build a replay buffer to record and replay production requests for testing. Record: all API requests (anonymized) → replay in staging environment. Compare responses: same output? Implement: request filtering (exclude user-sensitive data), response comparison (ignore timestamps), diff reporting highlighting differences. Use case: regression testing, production debugging. Show: production bug that only manifests on specific data sequence, replay in staging to reproduce." },
  { id: 156, title: "Observability: Spans vs Logs vs Metrics", cat: "Observability", stack: "Python + OpenTelemetry", prompt: "Understand three pillars of observability and when to use each. Events (logs): human-readable details + debugging context. Metrics: numeric aggregates (sum, histogram, gauge). Traces: causal chain of events. Show: same system monitored with only logs (hard to correlate), only metrics (missing details), + traces (see full picture). Implement collectors for all three, unified query interface (find request by user ID across all three)." },
  { id: 157, title: "Bulkhead Isolation Advanced", cat: "Performance", stack: "Python + FastAPI", prompt: "Implement advanced bulkhead patterns. Isolation dimensions: (1) By user (one user's requests don't starve others), (2) By priority, (3) By dependency (each downstream service has dedicated pool), (4) By resource type (CPU-bound vs I/O-bound get different pools). Show: system with shared pool (one slow query starves fast paths), with bulkheads (slow query isolated, fast paths unaffected)." },
  { id: 158, title: "Blue-Green Database Migration", cat: "Database", stack: "Python + PostgreSQL + Alembic", prompt: "Migrate production database schema with zero downtime (blue-green). Current schema (blue), new schema (green) in parallel. Strategy: dual-write (all writes go to both), sync data from blue→green, validate consistency, switch reads to green, keep blue for rollback. Show: application works entire time, if green migration fails rollback to blue (seconds, not hours)." },
  { id: 159, title: "Request Coalescing at Cache Layer", cat: "Performance", stack: "Python + asyncio + FastAPI", prompt: "Implement cache-coherent request coalescing. Multiple requests for same cache key → only one DB query, others wait. Implement using asyncio.Event with pending request tracking. Show: 100 concurrent requests for cache miss (could be 100 DB queries), with coalescing (1 DB query, 100 get result). Measure: latency, DB impact, resource utilization." },
  { id: 160, title: "Incident Response Runbook System", cat: "Observability", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a runbook system for incident response. Runbooks: text procedures (if X → do Y), automated actions (send alert, create incident ticket, execute remediation script). Triggered by: alert threshold, manual invocation. Track: runbook execution log, action results, manual overrides. Show: alert fires → runbook auto-remedies → if not fixed, escalate to on-call. Include: runbook versioning, testing capability (dry-run mode)." },
  { id: 161, title: "Graceful Service Shutdown", cat: "Infrastructure", stack: "Python + FastAPI + asyncio", prompt: "Implement graceful shutdown: stop accepting new requests, wait for in-flight requests to complete, close DB connections, save state. Phases: (1) SIGTERM received → stop listeners, (2) Drain phase (30s) → wait for requests, (3) Force kill (if drain timeout). Implement: request tracking middleware, health check endpoint that returns UNAVAILABLE after SIGTERM, connection pooling cleanup. Show: 10 in-flight requests on shutdown, all complete gracefully." },
  { id: 162, title: "OpenAPI/Swagger Auto-Generation Best Practices", cat: "API Design", stack: "Python + FastAPI + Pydantic", prompt: "Build comprehensive API documentation from code (zero extra effort). Use Pydantic models for schema inference, but go beyond: (1) Custom descriptions (docstrings), (2) Example values (field(example=...)), (3) Deprecated fields marking (deprecated=True), (4) Authorization schemes (OpenAPI security schemes), (5) Response codes. Generate client SDK from OpenAPI. Show: generated docs match actual API exactly, client code generated and matches." },
  { id: 163, title: "Adaptive Connection Pooling", cat: "Database", stack: "Python + PostgreSQL", prompt: "Implement adaptive connection pooling that adjusts pool size based on demand. Monitor: queue depth, wait time, connection age. If queue > threshold → grow pool. If idle > X min → shrink. Bounds: min 5, max 50. Show: under low load (pool = 5), spike (pool grows to 20), sustained (stays), decreases back. Measure: latency improvement vs memory cost." },
  { id: 164, title: "Observability Trade-offs", cat: "Observability", stack: "Python", prompt: "Understand observability trade-offs: cost (storage) vs detail level vs lag. High detail (all logs) = high cost but great debugging. Low detail (samples) = cheap but misses issues. Implement: sampling levels (DEBUG < INFO < WARN), adaptive sampling (increase detail on errors), retention policies (keep high-detail 1 day, low-detail 30 days). Show: 1% sampling = 10x cost reduction, still catches 99% of errors." },
  { id: 165, title: "Identity & Access Management (IAM)", cat: "Auth & Security", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a role-based identity and access management system. Resources: Projects, Databases. Roles: Owner, Admin, Viewer. Implement: role assignment, inherited permissions (Owner > Admin), resource-scoped roles (Admin of Project A ≠ Admin of Project B), audit trail of all IAM changes, SSO framework (SAML/OIDC ready). Show: user tries to access resource without permission → denied + logged." },
  { id: 166, title: "Observability for Caching", cat: "Observability", stack: "Python + Prometheus", prompt: "Build observability for cache behavior. Metrics: hit rate, eviction rate, memory usage, item age distribution. Implement: cache hit/miss tracking, eviction reason breakdown (TTL, LRU, size limit), hotkey detection (which keys used most?). Show: cache hit rate declining → indicates size too small, eviction rate spiking → indicates memory pressure, detect hotkeys consuming 90% of cache." },
  { id: 167, title: "Request Deduplication Across Retries", cat: "API Design", stack: "Python + FastAPI + PostgreSQL", prompt: "Build an idempotency layer for APIs. Client sends Idempotency-Key (UUID). Server: check if already processed (look up DB), if yes return cached response, if no execute and cache result. Challenges: concurrent requests with same key (locking), key expiration (cleanup old records). Show: network timeout → retry → same result without double-charging. Call the same endpoint 3 times with same key — get same response all 3 times." },
  { id: 168, title: "Progressive Deployment Strategies", cat: "Infrastructure", stack: "Python + FastAPI + Kubernetes", prompt: "Implement multiple deployment strategies. Canary: 5% → 25% → 50% → 100%. Blue-green: 0% → 100% (instant). Rolling: 20% at a time over 5 phases. Shadow: run v2 parallel, don't use responses (traffic shadowing). For each: show traffic shift, rollback behavior, risk/safety. Implement: traffic weighting config, automatic rollback on high error rate, metric comparison dashboard." },
  { id: 169, title: "Observability: Request Sampling", cat: "Observability", stack: "Python + OpenTelemetry + Jaeger", prompt: "Implement sampling strategies for distributed traces to manage cost. Strategies: (1) Head-based (sample at gateway), (2) Tail sampling (sample based on trace outcome — include all errors). Head-based: 10% sample = 10% cost. Tail-based: sample all initially, keep errors (5%), drop successes — better for error detection. Show: with head sampling, might miss rare error cases, with tail sampling catches all errors." },
  { id: 170, title: "Query Optimization for Pagination", cat: "Database", stack: "Python + PostgreSQL", prompt: "Optimize paginated queries which can be slow at high offsets. Compare: OFFSET (slow at high offset), cursor-based (stable, efficient). Implement keyset pagination: WHERE id > last_id LIMIT 10. Show: OFFSET 1M rows + LIMIT 10 scans 1M rows. Keyset scans only 10. Trade-off: cursor-based doesn't support arbitrary page jumps but is fast. Add: total count estimation (don't COUNT(*) on 1B rows)." },
  { id: 171, title: "Circuit Pattern Flexibility", cat: "Distributed Systems", stack: "Python + FastAPI", prompt: "Build flexible circuit breaker that supports multiple strategies. Strategies: (1) Failure threshold (N failures → open), (2) Latency threshold (p99 > 1s → open), (3) Error rate threshold (>5% errors → open). Implement: per-strategy thresholds, combination logic (open if ANY condition met). Show: different downstream services have different thresholds (flaky service: loose threshold; critical service: tight threshold)." },
  { id: 172, title: "Observability: Beyond Logs, Metrics, Traces", cat: "Observability", stack: "Python + OpenTelemetry + Prometheus", prompt: "Explore observability beyond standard pillars: (1) Profiles (CPU, memory allocation stacks), (2) Continuous profiling (always-on, low overhead), (3) Correlation IDs (link logs/metrics/traces/profiles). Show: high CPU issue — run continuous profile → see top 10 functions, correlate with slow trace IDs, find specific customer affected by profiling samples." },
  { id: 173, title: "API Rate Limiting with Sliding Window", cat: "Performance", stack: "Python + Redis + FastAPI", prompt: "Implement sliding window rate limiting (more accurate than fixed window). Algorithm: log request timestamps in Redis sorted set, count entries in last 60s window, allow if < limit. Advantages: no boundary spike exploit (fixed window exploit). Disadvantages: higher memory. Implement: efficient cleanup (remove old entries), per-user quota, burst allowance (higher limit in short window). Benchmark at 10k QPS." },
  { id: 174, title: "Distributed Testing", cat: "Observability", stack: "Python + pytest + Docker", prompt: "Test distributed system behavior across multiple nodes. Challenges: determinism (hard to reproduce timing bugs), isolation (tests shouldn't interfere), reproducibility. Solutions: use deterministic ordering, mock clock, chaos injection on demand. Show: test that generates 3 network failures in deterministic order, verifies recovery, passes every run. Compare with flaky real-time tests (pass 90% of runs)." },
  { id: 175, title: "Observability for Cost Optimization", cat: "Observability", stack: "Python + FastAPI + Prometheus + BigQuery", prompt: "Monitor costs as observability metric. Track per-request: CPU consumption, memory, storage I/O, network egress. Calculate cost (CPU = $0.00001/sec, GB = $0.023/mo). Show: dashboard with cost per service, cost per feature, cost per user. Find waste: feature X uses 30% of resources but generates 1% revenue. Implement: cost alerts, budget tracking per project." },
  { id: 176, title: "Security: OWASP Top 10 Mitigations", cat: "Auth & Security", stack: "Python + FastAPI", prompt: "Implement mitigations for OWASP Top 10: (1) SQL injection → Parameterized queries, (2) Broken auth → MFA, strong password policy, (3) Sensitive data exposure → Encryption, PII masking, (4) XML/XXE → Disable entity expansion, (5) Broken access control → RBAC enforcement, (6) Security misconfiguration → Config audit, (7) XSS → Input sanitization, CSP headers, (8) Insecure deserialization → Type validation + sandboxing, (9) Known vulnerabilities → Dependency scanning, (10) Insufficient logging → Audit trail. Build tests that verify each mitigation." },
  { id: 177, title: "Observability: Custom Attributes", cat: "Observability", stack: "Python + OpenTelemetry", prompt: "Track custom business attributes in observability. Attributes: user_tier (free/pro/enterprise), feature_flag_state, geographic_region, experiment_id (A/B testing). Attach to: traces, metrics, logs. Query: 'traces from enterprise users', 'error rate by region', 'latency for treatment vs control groups'. Show: how attributes enable business-focused analysis, move beyond just technical metrics." },
  { id: 178, title: "Request Deduplication Window", cat: "API Design", stack: "Python + FastAPI + Redis", prompt: "Implement efficient idempotency using time windows. Requests: POST /payment with idempotency key. Server keeps: key → result for 24 hours (configurable). Edge cases: (1) concurrent requests (lock), (2) key collision, (3) result expiry (delete old results). Show: duplicate request after 23h → still in window → cached response. After 24h → not found → could re-process (risky if original wasn't idempotent)." },
  { id: 179, title: "Load Balancing Algorithms", cat: "Infrastructure", stack: "Python", prompt: "Implement load balancing algorithms: (1) Round-robin (stateless, fair), (2) Least connections (consider active requests), (3) Weighted (capacity-aware), (4) IP hash (sticky sessions). For each: simulate 3 servers with varying capacities. Show: round-robin treats them equally (wasteful), weighted distributes by capacity (efficient). Implement: health checking, sticky session cookies, connection draining." },
  { id: 180, title: "Observability: Correlation IDs", cat: "Observability", stack: "Python + FastAPI + Logging", prompt: "Implement correlation IDs that follow a request through the entire system. Middleware: extract from request header or generate, attach to all logging, propagate to downstream calls. Show: single request touches Service A → Service B → Service C over 500ms. Single log query with correlation_id returns all relevant logs from all services, full trace of request journey reconstructed." },
  { id: 181, title: "Key Rotation Strategy", cat: "Auth & Security", stack: "Python + FastAPI", prompt: "Build a key rotation system (encryption keys, signing keys, credentials). Strategy: (1) dual-write window (new key active alongside old), (2) clients send kid (key ID) identifying which key to use, (3) retire old key after N days. Implement: key versioning, automatic rotation trigger (on schedule or manual), no downtime rotation. Show: key rotation happening while requests flow, no dropped requests." },
  { id: 182, title: "Observability: Percentile Latencies", cat: "Observability", stack: "Python + Prometheus + Grafana", prompt: "Understand percentile distributions for latency. Collect: p50, p95, p99, p99.9, max. Show: median 50ms (looked good), but p99 = 500ms (bad experience for some users). Histogram quantiles: bucketing strategy affects accuracy. Implement: HdrHistogram for better percentile accuracy. Show: difference between naive percentile calculation (bad) vs HdrHistogram (accurate), especially important for p99.9." },
  { id: 183, title: "Idempotent Event Processing", cat: "Messaging", stack: "Python + Apache Kafka", prompt: "Build idempotent event processing. Events have dedup_id. Processor: check if dedup_id seen before (Redis/DB), if yes skip, if no process. Challenges: dedup store size (TTL cleanup), concurrent processing, exactly-once vs at-least-once. Show: same event delivered twice (network retry), second time skipped. Implement: bloom filter for fast negative check, exact-match lookup for positive confirmation." },
  { id: 184, title: "Observability: High-Cardinality Data", cat: "Observability", stack: "Python + Prometheus", prompt: "Handle high-cardinality labels that blow up storage. Problem: metric.requests_total{path: ...} where path can be thousands of unique values (OK for logs, bad for metrics). Solution: (1) regex label matching (group paths), (2) drop high-cardinality labels, (3) use exact-match for important ones. Show: cardinality explosion (millions of series), post-cleanup (thousands of series)." },
  { id: 185, title: "Authentication with Device Trust", cat: "Auth & Security", stack: "Python + FastAPI", prompt: "Implement device-based authentication. User logs in → device registered + fingerprint stored. Future logins: require device trust or additional verification. Use: device ID (browser cookie + server state), device certificate, or WebAuthn. Show: normal login on trusted device (seamless), login from new device (2FA required), device compromise detection (login from geographically impossible location)." },
  { id: 186, title: "Background Job Priorities", cat: "Infrastructure", stack: "Python + Celery + Redis", prompt: "Implement priority queues for background jobs. Queues: critical (send password reset → minutes), normal (generate report → hours), low (cleanup → days). Worker pool size: more workers on critical queue. Show: 1000 low-priority jobs don't starve critical jobs. Implement: job preemption (cancel low-priority if critical arrives), starvation prevention (low-priority eventually promoted if waiting too long)." },
  { id: 187, title: "GraphQL Complexity Analysis", cat: "API Design", stack: "Python + Strawberry GraphQL", prompt: "Implement query complexity scoring to prevent expensive GraphQL queries from crashing server. Cost: simple field = 1, connection (list) = multiplier, nested = compound. Score query before execution: reject if > threshold. Show: naive query complexity (query getting all users → all posts → all comments = 1B items), with complexity limit prevents it. Allow high-value queries while blocking abuse." },
  { id: 188, title: "Observability: Request-Local Context", cat: "Observability", stack: "Python + contextvars + FastAPI", prompt: "Implement request-local context that spans async boundaries. Store: request_id, user_id, feature_flags, tenant_id. Access from any async task without parameter passing. Challenge: context isolation (concurrent requests don't see each other's context). Implement: copy context on task spawn, clear context on request end. Show: 3 concurrent requests have isolated context." },
  { id: 189, title: "Testing Async Code Deterministically", cat: "Observability", stack: "Python + pytest + async mock", prompt: "Test async code without flakiness. Challenges: timing-dependent (race conditions), hard to reproduce (time-dependent). Solutions: use pytest-asyncio with deterministic ordering, mock time using freezegun, control event ordering. Show: async code that fails 1% of time due to race condition, test captures it by controlling event order deterministically." },
  { id: 190, title: "Observability: Reducing Alert Fatigue", cat: "Observability", stack: "Python + Prometheus + AlertManager", prompt: "Reduce alert noise while catching real issues. Techniques: (1) dynamic thresholds (adjusted per day of week / time of day), (2) alert grouping (notify once per incident, not once per alert), (3) severity levels (page for p1, ticket for p2), (4) runbook-enabled alerts (auto-remediate before alerting). Show: before → 100 alerts/day (most false positives), after → 5 true alerts/day + auto-fixes for 20 issues." },
  { id: 191, title: "Database Statement Timeout Strategy", cat: "Database", stack: "Python + PostgreSQL + SQLAlchemy", prompt: "Implement statement timeouts to prevent runaway queries. Configure: global 60s timeout, override per query if needed. Behavior: query hitting timeout → error propagated, connection killed, retry possible. Show: one slow JOIN query times out → prevents pile-up of waiting connections → system remains responsive. Implement: query log of all timeouts for analysis, auto-create indexes for repeated timeout victims." },
  { id: 192, title: "Observability: Histogram Bucketing", cat: "Observability", stack: "Python + Prometheus", prompt: "Choose histogram buckets wisely to capture latency accurately. Problem: buckets [0.001, 0.01, 0.1, 1, 10] but all requests land in 0.1 or 1 → loses detailed distribution. Solution: exponential buckets (base 2 or custom) matching your SLOs. Show: bad bucketing (p99 estimate off by 10x), good bucketing (accurate). Understand: tradeoff between bucket count (storage) and accuracy. Use automated bucket suggestion tools." },
  { id: 193, title: "API Backwards Compatibility", cat: "API Design", stack: "Python + FastAPI + pytest", prompt: "Maintain backwards compatibility while evolving API. Strategies: (1) additive (new fields optional, old clients ignore), (2) renamed fields (support both names temporarily), (3) versioning (v1 vs v2 endpoints). Show: client on v1 continues working during v2 rollout, no forced upgrades. Implement: deprecation timeline (v1 sunset = 6 months after v2 launch), client SDK version metadata in requests (detect and warn)." },
  { id: 194, title: "Structured In-Process Logging", cat: "Observability", stack: "Python + structlog + FastAPI", prompt: "Use structured logging (JSON) for easier querying and aggregation. Log: {timestamp, level, message, context: {request_id, user_id}, error_details}. Show: queryable logs in ELK/Loki (filter by user_id, correlate across services). Compare: unstructured logs (grep-and-pray) vs structured (queries). Implement: log sampler (reduce DEBUG volume under load), PII redaction (don't log passwords), async log shipping to avoid blocking." },
  { id: 195, title: "Distributed Consensus Visualization", cat: "Distributed Systems", stack: "Python + web UI", prompt: "Build visual simulation of distributed consensus algorithms. Algorithms: Raft, Paxos, PBFT. Visualization: nodes, leader election, log replication, recovery. Interactive: add/remove nodes, partition network, show state transitions in real-time. Educational tool to understand: quorum, split-brain prevention, consensus guarantees. Show: leader election taking 3 rounds, one node crash and recovery." },
  { id: 196, title: "Observability: Error Tracking", cat: "Observability", stack: "Python + Sentry + FastAPI", prompt: "Implement comprehensive error tracking. Capture: exception type, stack trace, breadcrumbs (recent events before error), user context, release version, environment. Grouping: similar errors grouped by fingerprint (don't accidentally deduplicate different bugs). Show: error dashboard (frequency, affected users, first/last seen), release notes flagging new errors, fix verification (error rate drops after deploy)." },
  { id: 197, title: "Request Flow Visualization", cat: "Observability", stack: "Python + FastAPI + Jaeger UI concept", prompt: "Build a visualization of request flow through services. Show: request enters API Gateway → Service A calls Service B and C in parallel → B calls D → all responses merge. Timing per service (Bar chart showing which service delays). Use OpenTelemetry spans. Interactive: click on slow span to see logs and DB queries. Real-world: debugging distributed system bottlenecks." },
  { id: 198, title: "Observability: Cost Per Request", cat: "Observability", stack: "Python + FastAPI + Prometheus", prompt: "Calculate cost attribution per request. Track: compute cost (CPU-mins * cost), storage cost (GB-days), network egress. Show: request to API costs $0.0001, background job costs $1.50. Implement: cost isolation by customer/feature (charge customers by usage). Alert: request got unexpectedly expensive (detect cost regressions). Real-world: FinOps / cloud cost optimization." },
  { id: 199, title: "Self-Documenting Code with Observability", cat: "Observability", stack: "Python + OpenTelemetry + Docstrings", prompt: "Make code self-documenting via observability. Each function emits spans with: function name, parameters (non-sensitive), return value, execution time. Docstrings include: (1) intent, (2) expected span structure, (3) observability requirements. Show: reading traces gives code execution story without reading source. Implement: trace analysis to auto-generate docs. Real-world: teams understand code flow from traces (especially distributed code)." },
  { id: 200, title: "Observability Maturity Model", cat: "Observability", stack: "Python + Framework", prompt: "Define observability maturity: Level 0 (nothing), Level 1 (logs only), Level 2 (logs + metrics), Level 3 (logs + metrics + traces), Level 4 (unified system with context propagation + cost tracking). Build assessment tool: scan codebase (check for structured logging, metric emission, span instrumentation), generate maturity score + improvement roadmap. Show: typical system at Level 2, path to Level 3 (add tracing), Level 4 (add cost attribution)." },
  { id: 201, title: "Serverless Autoscaling Simulator", cat: "Infrastructure", stack: "TypeScript + AWS Lambda (local)", prompt: "Simulate serverless autoscaling: cold starts, concurrent invocations, provisioned concurrency. Build a local harness to replay traffic patterns and measure latency/cold-start ratio. Provide visualization of concurrency over time and cost estimation per traffic curve." },
  
 
  { id: 202, title: "MVCC (Multi-Version Concurrency Control)", cat: "Database", stack: "Python + PostgreSQL", prompt: "Implement a toy MVCC engine to show how databases handle concurrent reads/writes without locks. Each row has (xmin, xmax) transaction IDs. Read: return row version visible to current transaction's snapshot. Write: insert new version, set xmax on old. Show: reader never blocks writer, writer never blocks reader, snapshot isolation in action. Compare with PostgreSQL's actual pg_class visibility rules. Implement vacuum to clean dead versions. Explain why Postgres has bloat problem without autovacuum." },
 
  { id: 203, title: "B-Tree Index from Scratch", cat: "Database", stack: "Python", prompt: "Implement a B-Tree index from scratch to understand how databases store and query indexed data. Operations: insert (with node splits), search (exact + range), delete (with merge/redistribution). Visualize the tree after each operation. Show: unindexed scan O(n) vs B-Tree O(log n) for 1M rows. Implement: composite key ordering, index-only scan simulation. Explain: why B-Tree and not binary tree (disk page size alignment), fill factor, and page splits causing fragmentation." },
 
  { id: 204, title: "Database Partitioning Strategies", cat: "Database", stack: "Python + PostgreSQL", prompt: "Implement and compare all PostgreSQL partitioning strategies on a 10M row table. Range partitioning: orders by date (monthly partitions). List partitioning: users by region. Hash partitioning: events by user_id. Show: partition pruning in EXPLAIN (only scans relevant partition), cross-partition query overhead, partition attachment/detachment (zero-downtime archival). Benchmark: query time with/without partition pruning, maintenance cost of adding new partition, partition key hotspot problem." },
 
  { id: 205, title: "Full-Text Search Engine from Scratch", cat: "Database", stack: "Python", prompt: "Build a mini full-text search engine. Steps: (1) Tokenizer: split text, normalize, stem (Porter stemmer), (2) Inverted index: term → [doc_id, position] list, (3) TF-IDF scoring: rank documents by relevance, (4) Boolean queries: AND/OR/NOT, (5) Phrase queries: 'exact phrase' using position lists. Index 10,000 Wikipedia articles. Show: query latency <10ms, relevance ranking comparison vs simple keyword match. Explain: why PostgreSQL tsvector/tsquery works this way, GIN index role." },
 
  { id: 206, title: "Replication Slot & CDC Pipeline", cat: "Database", stack: "Python + PostgreSQL + Debezium concept", prompt: "Build a Change Data Capture (CDC) pipeline using PostgreSQL logical replication slots. Steps: (1) Create replication slot with pgoutput plugin, (2) Stream WAL changes via replication protocol, (3) Decode INSERT/UPDATE/DELETE events, (4) Publish to downstream (Kafka or file). Show: insert a row → appears in CDC stream within 100ms. Handle: schema changes (ALTER TABLE), transaction boundaries, slot lag monitoring. Compare: trigger-based CDC (write amplification) vs WAL-based (transparent)." },
 
  { id: 207, title: "Distributed SQL: Sharding Proxy", cat: "Database", stack: "Python + PostgreSQL", prompt: "Build a SQL sharding proxy that transparently distributes queries across 3 PostgreSQL shards. Parse SQL queries (use sqlparse), extract shard key (user_id), route to correct shard. Handle: cross-shard queries (scatter-gather + merge sort), distributed aggregates (SUM, COUNT), cross-shard JOINs (broadcast small table). Show: INSERT routes to shard 2, SELECT WHERE user_id=X goes to one shard, SELECT COUNT(*) hits all 3 and merges. Explain why cross-shard JOINs are expensive." },
 
  { id: 208, title: "Query Result Caching Layer", cat: "Database", stack: "Python + PostgreSQL + Redis", prompt: "Build an intelligent query result cache that sits between application and database. Cache key: normalized SQL + parameters. Invalidation: table-level dependency tracking (cache entries tagged with tables they touch), on write to table X → invalidate all entries tagged X. Implement: cache warm-up on deploy, probabilistic early expiry (prevent stampede), per-query TTL overrides, cache hit metrics. Show: 100x speedup for read-heavy workload, stale read scenario and how to avoid it." },
 
  { id: 209, title: "Index Advisor & Slow Query Detector", cat: "Database", stack: "Python + PostgreSQL", prompt: "Build an automated index advisor. Monitor pg_stat_statements for slow queries. For each slow query: run EXPLAIN (no execute), detect: Seq Scan on large table, missing index on JOIN column, unused index (bloat). Generate: CREATE INDEX suggestions with estimated speedup. Simulate: add the suggested index, re-run EXPLAIN, compare cost. Also detect: duplicate indexes, indexes never used (pg_stat_user_indexes). Output report: top 10 missing indexes + expected improvement." },
 
  // ─────────────────────────────────────────────
  // STORAGE SYSTEMS
  // ─────────────────────────────────────────────
  { id: 210, title: "Log-Structured Merge Tree (LSM)", cat: "Storage Systems", stack: "Python", prompt: "Implement an LSM-Tree storage engine from scratch (the core of LevelDB/RocksDB/Cassandra). Components: (1) MemTable (in-memory sorted skip-list), (2) WAL for durability, (3) SSTable flush (MemTable → sorted file on disk), (4) Compaction (merge SSTables, eliminate tombstones), (5) Bloom filter per SSTable (skip-scan optimization). Operations: put, get, delete, range scan. Show: write amplification during compaction, read amplification without bloom filters, level size ratios. Compare read/write tradeoffs vs B-Tree." },
 
  { id: 211, title: "Append-Only File Store", cat: "Storage Systems", stack: "Python", prompt: "Build an append-only file store (like Redis AOF or Kafka log segments). Operations: append record, read by offset, compact (remove deleted/overwritten entries). Implement: CRC checksum per record, segment rolling (new file every 100MB), index file (key → segment:offset for O(1) reads), crash recovery by scanning from last valid CRC. Show: sequential writes are 10x faster than random writes. This is how Bitcask (Riak's storage) works." },
 
  { id: 212, title: "Content-Addressable Storage", cat: "Storage Systems", stack: "Python", prompt: "Build a content-addressable storage system (like Git's object store or IPFS). Key = SHA-256 of content. Operations: put(content) → hash, get(hash) → content. Properties: deduplication (same content = same key), immutability, verifiability. Implement: chunking large files (split into 4MB chunks, store chunks separately, store manifest), delta compression between similar blobs, garbage collection (find unreferenced blobs). Show: store same file twice → stored once. This is how Docker layers and Git blobs work." },
 
  { id: 213, title: "Column Store vs Row Store", cat: "Storage Systems", stack: "Python + DuckDB + PostgreSQL", prompt: "Implement a mini column store and compare with row store for analytical queries. Row store: store each row contiguously (good for OLTP: fetch all columns of one row). Column store: store each column contiguously (good for OLAP: fetch one column across all rows). Implement: run-length encoding, dictionary encoding per column, vectorized execution (process 1024 values at once). Benchmark: SELECT AVG(salary) on 10M rows. Column store: 10x faster (only reads salary column). Explain: why Parquet/Redshift/BigQuery use columnar format." },
 
  { id: 214, title: "Distributed File System (GFS-lite)", cat: "Storage Systems", stack: "Python + FastAPI", prompt: "Build a simplified distributed file system inspired by Google File System. Components: (1) Master: namespace tree, chunk metadata (location + version), (2) ChunkServers: store 64MB chunks, (3) Client library: chunk location lookup, read/write to chunkservers. Features: 3-way replication, re-replication on chunkserver failure (master detects via heartbeat), chunk lease for write consistency. Show: write file → split into chunks → store on 3 servers, read file → parallel chunk fetch → reassemble. Master failure? Explain shadow master." },
 
  { id: 215, title: "Persistent Data Structure (Immutable)", cat: "Storage Systems", stack: "Python", prompt: "Implement persistent (immutable) data structures for time-travel queries. Persistent Red-Black Tree: every update creates a new root; old roots still valid. Applications: (1) Database snapshot isolation (read old version), (2) Undo/redo without copying entire state, (3) Functional programming. Show: insert 1000 elements, keep 10 snapshots, query snapshot at time T without affecting current. Compare memory usage: naive copy-on-write (O(n) per update) vs structural sharing (O(log n) per update). This is how Clojure's persistent collections work." },
 
  // ─────────────────────────────────────────────
  // NETWORKING
  // ─────────────────────────────────────────────
  { id: 216, title: "HTTP/2 Multiplexing from Scratch", cat: "Networking", stack: "Python + h2 library", prompt: "Build an HTTP/2 server and client to understand multiplexing. Show HTTP/1.1 head-of-line blocking: 6 requests queue up, slow first response blocks all. HTTP/2: all 6 requests on same TCP connection, streams interleaved. Implement: HEADERS frame, DATA frame, SETTINGS frame, flow control (WINDOW_UPDATE), server push. Measure: page load time with 20 resources, HTTP/1.1 vs HTTP/2 (expect 2-3x improvement). Explain: why HTTP/2 over TLS only in browsers, HPACK header compression." },
 
  { id: 217, title: "TCP Congestion Control Simulation", cat: "Networking", stack: "Python", prompt: "Simulate TCP congestion control algorithms (Reno, Cubic, BBR) to understand how TCP adapts to network conditions. Implement: slow start, congestion avoidance, fast retransmit/recovery. Simulate: packet loss (random drop 1%), bandwidth limit (1 Gbps), latency (50ms RTT). Plot: congestion window size over time for each algorithm. Show: Reno saw-tooth pattern, Cubic faster recovery, BBR bandwidth-delay product filling. Explain: why BBR is better on high-BDP links (inter-datacenter)." },
 
  { id: 218, title: "DNS Resolver from Scratch", cat: "Networking", stack: "Python", prompt: "Build a recursive DNS resolver from scratch using raw UDP sockets. Parse DNS wire format: query/response packets, question section, answer/authority/additional sections, record types (A, AAAA, MX, CNAME, TXT). Implement: iterative resolution (root → TLD → authoritative), caching with TTL, negative caching (NXDOMAIN). Resolve any domain name in <100ms. Explain: why DNS is UDP not TCP (size limit), DNSSEC basics, DNS-over-HTTPS. Show a full resolution trace for google.com." },
 
  { id: 219, title: "TLS Handshake Visualizer", cat: "Networking", stack: "Python + scapy or tlslite-ng", prompt: "Implement and visualize TLS 1.3 handshake steps. Show: ClientHello (supported cipher suites, key share), ServerHello (chosen cipher, server key share), Certificate, CertificateVerify, Finished. Implement ECDH key exchange (X25519): client+server generate ephemeral keys, compute shared secret. Show: what data is encrypted at each step, forward secrecy (new keys per session). Compare: TLS 1.2 (2 RTT) vs TLS 1.3 (1 RTT, 0-RTT resumption). This demystifies HTTPS." },
 
  { id: 220, title: "WebRTC Signaling Server", cat: "Networking", stack: "Python + FastAPI + WebSocket + aiortc", prompt: "Build a WebRTC signaling server and peer connection demo. Steps: (1) Signaling: exchange SDP offers/answers via WebSocket, (2) ICE: discover peer connectivity paths (host, srflx, relay), (3) DTLS handshake: establish encrypted connection, (4) SRTP: send audio/video. Implement: STUN server (reflexive address discovery), TURN server fallback (relay for symmetric NAT). Show: two browser peers connecting peer-to-peer, latency vs WebSocket, NAT traversal success/failure." },
 
  { id: 221, title: "Load Balancer with Health Checks & BGP Concepts", cat: "Networking", stack: "Python + FastAPI", prompt: "Build a layer-4/layer-7 load balancer from scratch with active health checking. L4 mode: TCP proxy (connection forwarding). L7 mode: HTTP proxy (full request parsing). Health check: HTTP GET /health every 5s, mark backend unhealthy after 3 failures, re-enable after 2 successes. Algorithms: Least response time (track moving average), power-of-two-choices. Add: connection draining, slow start (gradually ramp new backend), anycast concept explanation. Benchmark: 10k RPS through the LB." },
 
  { id: 222, title: "Service Discovery with Gossip Protocol", cat: "Networking", stack: "Python + sockets", prompt: "Implement service discovery using a gossip protocol (like Consul or Serf). Each node maintains a member list with health state. Gossip: every 1s, pick 3 random peers, exchange member lists, merge. Failure detection: SWIM protocol (ping + indirect ping). Show: start 10 nodes, kill 2, within 3 gossip rounds all nodes know about failures. Implement: node metadata (service name, port, tags), anti-entropy sync (periodic full state exchange). Compare: gossip vs centralized registry (etcd) — tradeoffs." },
 
  { id: 223, title: "Packet Capture & Protocol Analyzer", cat: "Networking", stack: "Python + scapy", prompt: "Build a packet capture and protocol analyzer. Capture live traffic using scapy/pcapy. Decode: Ethernet → IP → TCP/UDP → HTTP/DNS/TLS. Reassemble TCP streams (handle out-of-order packets, retransmissions). Detect: port scans (many SYN, few SYN-ACK), HTTP clear-text passwords, DNS queries for blocked domains. Build: per-connection stats (bytes, packets, duration), protocol breakdown pie chart, top talkers. Show: capture your own HTTP request to see exactly what's on the wire." },
 
  { id: 224, title: "NAT Gateway Implementation", cat: "Networking", stack: "Python + iptables concepts / raw sockets", prompt: "Implement a software NAT gateway to understand how home routers and cloud NAT work. Maintain NAT table: (private_ip, private_port) → (public_ip, public_port). Outbound: rewrite source IP/port, add to NAT table. Inbound: look up NAT table, rewrite destination. Handle: TCP state machine (track SYN/FIN for connection lifecycle), UDP timeout, ICMP. Show: 5 private hosts sharing 1 public IP, all making HTTP requests simultaneously. Explain: why NAT breaks peer-to-peer (needs STUN/TURN) and why IPv6 eliminates NAT." },
 
  // ─────────────────────────────────────────────
  // CONCURRENCY
  // ─────────────────────────────────────────────
  { id: 225, title: "Lock-Free Data Structures", cat: "Concurrency", stack: "Python / C via ctypes or Java", prompt: "Implement lock-free data structures using compare-and-swap (CAS) primitives. Structures: (1) Lock-free stack (Treiber stack), (2) Lock-free queue (Michael-Scott queue), (3) Lock-free counter. Show: ABA problem (classic pitfall) and fix with tagged pointers or hazard pointers. Benchmark: mutex-based vs lock-free at 8 threads: expect 2-5x throughput improvement. Explain: memory ordering (acquire/release semantics), why lock-free ≠ wait-free, Java's java.util.concurrent.atomic. This is how high-perf systems avoid locks." },
 
  { id: 226, title: "Actor Model Implementation", cat: "Concurrency", stack: "Python + asyncio", prompt: "Implement the Actor Model from scratch to understand Erlang/Akka concurrency. Each actor: mailbox (asyncio queue), message handler, can send to other actors, spawn child actors. Features: supervision (parent restarts failed child), location transparency (actors addressable by ID not reference), backpressure (mailbox full → sender waits). Build: bank account system using actors (no shared mutable state, no locks). Show: 1000 concurrent transfers, no deadlock, no race condition. Compare: actors vs threads vs async coroutines." },
 
  { id: 227, title: "Coroutine Scheduler from Scratch", cat: "Concurrency", stack: "Python", prompt: "Build a cooperative coroutine scheduler from scratch using Python generators (no asyncio). Implement: yield to pause a coroutine, scheduler runs each coroutine round-robin, I/O multiplexing with select() to wake coroutines on socket events. Build: HTTP server running 100 concurrent connections on a single thread using your scheduler. Show: context switch cost (< 1μs vs 10μs for OS threads), cooperative vs preemptive scheduling tradeoff. This shows exactly how asyncio/gevent/Node.js event loop works internally." },
 
  { id: 228, title: "Thread Pool with Work Stealing", cat: "Concurrency", stack: "Python / Java", prompt: "Implement a thread pool with work-stealing scheduler (like Java's ForkJoinPool). Each worker thread has its own deque. Producers push to the front of their deque. Idle workers steal from the back of a random peer's deque. Implement: fork-join tasks (recursive divide-and-conquer), parallel merge sort using fork-join. Show: work stealing balances load automatically when tasks are uneven. Compare: fixed thread pool (starvation if tasks uneven) vs work stealing (self-balancing). This is how Go's goroutine scheduler works." },
 
  { id: 229, title: "Async vs Sync vs Threaded Benchmarks", cat: "Concurrency", stack: "Python", prompt: "Build a benchmark harness comparing 4 concurrency models: (1) Sequential (baseline), (2) Multi-threaded (GIL-limited in Python for CPU), (3) Multi-process (true parallelism for CPU), (4) Async/await (I/O bound). Workloads: CPU-bound (hash 10k passwords), I/O-bound (100 HTTP requests), Mixed. For each combination: measure throughput, latency p50/p99, CPU usage, memory. Show: async wins for I/O, multiprocess wins for CPU, threading limited by GIL. Explain: when each model is appropriate in real systems." },
 
  { id: 230, title: "Deadlock Detection & Prevention", cat: "Concurrency", stack: "Python + threading", prompt: "Build a deadlock detection and prevention system. Implement: resource allocation graph, cycle detection algorithm (Kahn's algorithm on wait-for graph), periodic deadlock detector thread. Prevention strategies: (1) Lock ordering (always acquire A before B), (2) Try-lock with timeout and retry, (3) Banker's algorithm (only grant if safe state remains). Simulate: classic dining philosophers deadlock, then fix each way. Show deadlock detection finding the cycle in <10ms. This is how database lock managers work." },
 
  { id: 231, title: "Software Transactional Memory (STM)", cat: "Concurrency", stack: "Python", prompt: "Implement Software Transactional Memory (STM) — the concurrency model used in Haskell (STM monad). Transaction: read a set of variables, compute new values, atomically commit if no conflicts (optimistic concurrency). On conflict: retry transaction. Implement: transaction log, conflict detection (version numbers), retry semantics. Build: bank transfer system using STM (no explicit locks). Show: 1000 concurrent transfers, all consistent. Compare: STM composability vs mutex (nested locks cause deadlock, STM composes freely). Explain: why STM hasn't replaced mutexes in practice (write amplification)." },
 
  // ─────────────────────────────────────────────
  // COMPILER & RUNTIMES
  // ─────────────────────────────────────────────
  { id: 232, title: "Build a Bytecode Interpreter", cat: "Compiler & Runtimes", stack: "Python", prompt: "Build a stack-based bytecode interpreter from scratch (like CPython's eval loop). Define: instruction set (PUSH, POP, ADD, CALL, RETURN, JUMP_IF_FALSE, LOAD_VAR, STORE_VAR). Write a compiler for a tiny language → bytecode. Execute bytecode in your interpreter. Implement: call stack with frames, local variable slots, exception handling (try/except as jump tables). Show: factorial(10) compiled to bytecode, trace each instruction. Disassemble CPython bytecode for comparison. This demystifies how Python/JVM execute code." },
 
  { id: 233, title: "Garbage Collector from Scratch", cat: "Compiler & Runtimes", stack: "Python / C", prompt: "Implement 3 garbage collection algorithms to understand how runtimes manage memory. (1) Mark-and-Sweep: mark reachable objects, sweep unmarked. (2) Reference Counting: increment/decrement on assign/delete, collect when count=0 (show cyclic reference problem + fix with cycle detector). (3) Copying GC: divide heap in two, copy live objects, swap halves (eliminates fragmentation). Implement all 3 on the same toy runtime. Compare: pause time, throughput, fragmentation, implementation complexity. Explain: Java G1GC, Python CPython GC, Go GC — which strategy each uses." },
 
  { id: 234, title: "JIT Compilation Tracing Demo", cat: "Compiler & Runtimes", stack: "Python + LLVM via llvmlite", prompt: "Build a tracing JIT compiler for a tiny interpreter. Steps: (1) Interpret normally, (2) Detect hot loops (counter per loop header), (3) Record trace (sequence of operations in hot loop), (4) Compile trace to native code via LLVM IR, (5) Execute native code. Show: loop runs 1000x interpreted (slow), then JIT kicks in, 100x speedup. Implement: guard checks (deoptimize if type changes), inline caches. This demystifies PyPy, LuaJIT, V8's Turbofan." },
 
  { id: 235, title: "Memory Allocator (malloc/free)", cat: "Compiler & Runtimes", stack: "C or Python + ctypes", prompt: "Implement a heap memory allocator from scratch. Allocator: slab allocator (size classes: 8, 16, 32, 64... bytes), free list per size class, coalescing adjacent free blocks. Compare: first-fit vs best-fit vs buddy system. Measure: fragmentation ratio, allocation throughput (ops/sec). Show: heap layout after 1000 alloc/free ops, fragmentation with/without coalescing. Implement: tcmalloc-style per-thread cache (avoid lock contention). This explains why malloc is not simply brk() syscall." },
 
  { id: 236, title: "Parser Combinator for Config Language", cat: "Compiler & Runtimes", stack: "Python", prompt: "Build a parser combinator library from scratch and use it to parse a YAML/TOML-like config language. Primitive parsers: char, string, regex. Combinators: sequence (A then B), choice (A or B), many (zero-or-more), optional. Build grammar: key-value pairs, nested sections, arrays, comments. Parse a real nginx.conf or docker-compose.yaml equivalent. Show: parser error with position + expected/got message. Explain: parser combinators vs lex/yacc (PEG vs CFG), backtracking, memoization (packrat parser)." },
 
  { id: 237, title: "Module System & Dynamic Linker", cat: "Compiler & Runtimes", stack: "Python", prompt: "Build a dynamic module loading system (like Python's import system or dlopen). Implement: module registry (name → module object), import resolution (sys.path equivalent), circular import detection, lazy loading (import on first use), module reloading. Simulate: hot-code reload for a web server (reload handler without restarting process). Show: import order matters (circular import breaks), namespace isolation, import hooks (intercept import to add instrumentation). This explains Python's import system, Java ClassLoader, Node.js require." },
 
  // ─────────────────────────────────────────────
  // ML SYSTEMS
  // ─────────────────────────────────────────────
  { id: 238, title: "Feature Store Implementation", cat: "ML Systems", stack: "Python + Redis + PostgreSQL + FastAPI", prompt: "Build a feature store for ML serving. Offline store (PostgreSQL): batch-computed historical features for training. Online store (Redis): latest feature values for low-latency inference (<10ms). Components: (1) Feature ingestion pipeline (batch job writes to offline, streaming writes to online), (2) Point-in-time correct joins (no data leakage: only use features that existed at prediction time), (3) Feature versioning and lineage. Show: train with offline features, serve with online features, detect training-serving skew (features differ between offline and online)." },
 
  { id: 239, title: "Model Serving with Batching", cat: "ML Systems", stack: "Python + FastAPI + PyTorch", prompt: "Build a high-throughput model inference server with dynamic batching. Problem: single inference is slow but GPU is underutilized. Solution: batch multiple requests arriving within 10ms window, run single forward pass on batch, return results to each caller. Implement: request queue, batcher thread (waits for N requests or timeout), async result dispatch. Benchmark: single-request (10ms each × 100 = 1s total) vs batched (10ms + 10ms overhead × 100 = 110ms). Add: max batch size, latency SLO enforcement, model warm-up." },
 
  { id: 240, title: "A/B Testing Framework", cat: "ML Systems", stack: "Python + FastAPI + PostgreSQL + Redis", prompt: "Build a statistically sound A/B testing framework. Components: (1) Experiment definition (variants, traffic split, success metric), (2) Assignment: deterministic user→variant (hash user_id + experiment_id), (3) Event tracking (impression, conversion), (4) Statistical analysis: z-test for conversion rates, t-test for continuous metrics, p-value, confidence intervals, (5) Sequential testing (stop early if significant). Show: run fake experiment, accumulate data, declare winner at 95% confidence. Explain: multiple testing problem (Bonferroni correction), CUPED variance reduction." },
 
  { id: 241, title: "Vector Database from Scratch", cat: "ML Systems", stack: "Python + NumPy", prompt: "Build a vector similarity search engine from scratch. Implement: (1) Flat index (brute-force cosine similarity), (2) LSH (Locality Sensitive Hashing) for approximate nearest neighbor, (3) HNSW (Hierarchical Navigable Small World) graph index — the algorithm behind Pinecone/Weaviate/pgvector. Index 100k 128-dim vectors. Benchmark: flat (perfect recall, slow), LSH (fast, ~90% recall), HNSW (fast, ~99% recall). Show: add vectors, query top-K neighbors, update/delete. Explain: why cosine similarity over Euclidean for embeddings." },
 
  { id: 242, title: "ML Pipeline Orchestration", cat: "ML Systems", stack: "Python + Prefect / custom DAG", prompt: "Build an ML pipeline orchestrator covering the full training lifecycle. Steps in DAG: (1) Data extraction, (2) Feature engineering, (3) Train/test split (time-based, not random), (4) Model training with hyperparameter grid search, (5) Evaluation (AUC, F1, RMSE), (6) Model registration if better than current prod, (7) Shadow deployment. Implement: step caching (skip unchanged steps on re-run), artifact lineage (which data + code produced which model), experiment tracking (MLflow-like). Show: full pipeline run, one step fails → partial re-run from cache." },
 
  { id: 243, title: "Data Drift Detection", cat: "ML Systems", stack: "Python + SciPy + FastAPI", prompt: "Build a production data drift monitor that alerts when incoming data diverges from training distribution. Methods: (1) Population Stability Index (PSI) for categorical features, (2) Kolmogorov-Smirnov test for continuous features, (3) Chi-squared test for label distribution. Monitor: per-feature drift scores, composite drift score, model performance drift (if labels available). Alert: PSI > 0.2 = significant drift → trigger retraining. Show: inject artificial drift (shift mean by 2σ), detector triggers alert within 1000 requests. Explain: concept drift vs data drift." },
 
  { id: 244, title: "Embedding Pipeline & Semantic Search", cat: "ML Systems", stack: "Python + sentence-transformers + FAISS + FastAPI", prompt: "Build an end-to-end semantic search system. Offline: encode 50k documents using sentence-transformers (384-dim), store in FAISS HNSW index. Online: encode query, search HNSW index (top-10 candidates), re-rank with cross-encoder. Show: keyword search (fails on synonyms) vs semantic search (finds conceptually similar). Implement: incremental index updates (add new docs without full rebuild), caching hot queries, approximate vs exact mode. Benchmark: search latency p99 < 50ms at 100 QPS." },
 
  // ─────────────────────────────────────────────
  // CLOUD NATIVE
  // ─────────────────────────────────────────────
  { id: 245, title: "Kubernetes Operator from Scratch", cat: "Cloud Native", stack: "Python + kopf (Kubernetes Operator Framework)", prompt: "Build a Kubernetes operator that manages a custom resource DatabaseCluster. CRD: defines spec (replicas, version, storageGB). Operator reconciler: on create → provision StatefulSet + PVC + Service, on update → rolling upgrade, on delete → cleanup. Implement: status conditions (Provisioning, Ready, Degraded), event recording, owner references for garbage collection. Show: create DatabaseCluster YAML, operator provisions the cluster, scale replicas, operator handles reconciliation. Explain: controller pattern, informers, work queue." },
 
  { id: 246, title: "Container Runtime (OCI) from Scratch", cat: "Cloud Native", stack: "Python + Linux namespaces", prompt: "Build a minimal container runtime using Linux primitives to understand how Docker works. Steps: (1) Unpack OCI image layer (extract tar), (2) Create namespaces: PID (isolated process tree), NET (isolated network), MNT (pivot_root for filesystem isolation), (3) cgroups: limit CPU (cpu.max) and memory (memory.max), (4) Run process in isolated environment. Show: process inside container sees PID 1, can't see host processes, memory limit kills OOM. Explain: how Docker is just process isolation with a nice API, not a VM." },
 
  { id: 247, title: "GitOps CD Pipeline", cat: "Cloud Native", stack: "Python + ArgoCD concepts + Kubernetes", prompt: "Build a GitOps-based continuous deployment system (ArgoCD-lite). Git repo is source of truth. Sync controller: polls repo every 30s, compares desired state (YAML in git) vs actual state (Kubernetes API), applies diff. Features: sync status (Synced/OutOfSync/Degraded), auto-sync vs manual approval, rollback (git revert = instant rollback), multi-environment (branches = environments). Show: commit new image tag → auto-deployed in 30s, rollback by reverting commit." },
 
  { id: 248, title: "Service Mesh Traffic Policies", cat: "Cloud Native", stack: "Python + Istio concepts + Envoy config", prompt: "Implement service mesh traffic policies using Envoy proxy configuration. Policies: (1) Retry with exponential backoff (retry on 503, max 3 times), (2) Circuit breaker (outlier detection: eject host after 5 consecutive 5xx), (3) Traffic mirroring (shadow 10% of prod traffic to canary), (4) Fault injection (inject 5% 500 errors for resilience testing), (5) mTLS (enforce mutual TLS between services). Write Envoy dynamic configuration (xDS API). Show each policy in action. Explain: why service mesh moves resilience from code to infrastructure." },
 
  { id: 249, title: "Serverless Function Runtime", cat: "Cloud Native", stack: "Python + Docker + FastAPI", prompt: "Build a serverless function execution platform (Lambda-lite). Components: (1) Function registry: store user code + dependencies, (2) Execution engine: spin up container per invocation or reuse warm container, (3) Cold start optimization: pre-warm pool of containers, (4) Billing: track execution time + memory, (5) Concurrency: max 10 concurrent executions per function. Show: cold start (500ms) vs warm start (5ms), concurrency throttling, function timeout (kill after 30s). Explain: how AWS Lambda handles this at scale, execution environment reuse." },
 
  { id: 250, title: "Multi-Tenant SaaS Architecture", cat: "Cloud Native", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a multi-tenant SaaS backend with 3 isolation models. (1) Shared everything: all tenants in one DB, tenant_id on every row (cheapest, most risk). (2) Shared DB, separate schemas: each tenant has own schema (balance). (3) Separate DB per tenant: strongest isolation, highest cost. Implement: tenant context middleware (extract from JWT), row-level security (PostgreSQL RLS), tenant provisioning/deprovisioning, cross-tenant data leakage prevention tests. Benchmark: query latency for each model at 100 tenants, 1000 tenants, 10000 tenants." },
 
  { id: 251, title: "Platform Engineering: Internal Developer Portal", cat: "Cloud Native", stack: "Python + FastAPI + React (Backstage concept)", prompt: "Build an internal developer portal (Backstage-lite). Service catalog: register service (name, team, repo, runbook, dependencies). Scaffolding: create new service from template (FastAPI, gRPC, or worker). Observability integration: link to Grafana dashboard per service. Incident management: current incidents affecting this service. Dependency graph: visualize which services call which. Implement: YAML-based service descriptor, auto-discovery via Kubernetes annotations, search by capability. Show: on-call engineer opens incident → immediately sees owner, runbook, dependencies." },
 
  // ─────────────────────────────────────────────
  // DATA ENGINEERING
  // ─────────────────────────────────────────────
  { id: 252, title: "Stream Processing Engine", cat: "Data Engineering", stack: "Python + Kafka + Flink concepts", prompt: "Build a mini stream processing engine supporting stateful operations. Operators: filter, map, groupBy + aggregate (count/sum per window), join (stream + stream on key). Windows: tumbling (fixed 1min), sliding (1min every 30s), session (gap-based). State backend: in-memory hash map + periodic checkpoint to disk. Implement: watermarks for out-of-order events (tolerate 30s late events), exactly-once semantics (idempotent sinks + checkpointing). Show: word count over streaming sentences, latency vs batch processing. This shows how Flink/Spark Streaming work." },
 
  { id: 253, title: "Data Lake with Delta Table Concepts", cat: "Data Engineering", stack: "Python + DuckDB + Parquet", prompt: "Build a Delta Lake-lite: ACID transactions on Parquet files in object storage. Transaction log: JSON file per commit (add/remove file list + schema + stats). Operations: ACID insert (new Parquet file + log entry), update (write new file + tombstone old), delete, merge (upsert). Time travel: read snapshot at version N by replaying log. Schema evolution: add/rename columns without rewrite. Show: concurrent writers (conflict detection via log), time travel query at T-1 day. Explain: why Parquet alone isn't enough (no transactions, no deletes), how Delta/Iceberg/Hudi solve it." },
 
  { id: 254, title: "ETL Pipeline with Data Quality", cat: "Data Engineering", stack: "Python + Great Expectations + dbt concepts", prompt: "Build an ETL pipeline with built-in data quality checks. Pipeline: Extract (PostgreSQL → Python), Transform (clean, normalize, join), Load (DuckDB/BigQuery). Data quality: (1) Schema validation (expected columns, types), (2) Statistical validation (mean ± 3σ, no nulls in key columns), (3) Referential integrity (foreign key exists in dim table), (4) Freshness (data not older than 2 hours). On failure: quarantine bad rows, alert, block downstream loads. Show: inject a bad batch (null user_ids) → quarantined, pipeline continues with good data." },
 
  { id: 255, title: "Columnar Compression Algorithms", cat: "Data Engineering", stack: "Python", prompt: "Implement columnar compression algorithms used in Parquet/ORC. Algorithms: (1) Dictionary encoding (replace repeated strings with integer codes), (2) Run-length encoding (compress runs of same value), (3) Delta encoding (store differences for sequential numbers), (4) Bit-packing (pack small integers tightly), (5) Snappy/LZ4 block compression (general-purpose). Implement all 5 on a sample dataset (10M rows, realistic data). Measure: compression ratio, encode/decode throughput. Show: string column 100MB → 8MB with dictionary. Explain: why columnar compression beats row-based." },
 
  { id: 256, title: "Distributed Shuffle (MapReduce Core)", cat: "Data Engineering", stack: "Python + multiprocessing", prompt: "Implement MapReduce from scratch, focusing on the shuffle phase (the hard part). Map phase: each mapper emits (key, value) pairs. Shuffle: group all values for same key across all mappers (network transfer). Reduce phase: aggregate per key. Implement: external merge sort for shuffle (too large for memory), combiner optimization (partial reduce before shuffle), speculative execution (launch duplicate slow tasks). Show: word count on 1GB text file, shuffle bottleneck (network bound), combiner reducing shuffle data by 10x. This shows how Spark/Hadoop work internally." },
 
  { id: 257, title: "Schema Evolution & Backward Compatibility", cat: "Data Engineering", stack: "Python + Avro + Protobuf + JSON Schema", prompt: "Implement schema evolution strategies across 3 serialization formats. Avro: schema stored with data, reader uses writer schema + reader schema for projection. Protobuf: field numbers enable additive evolution, no schema in payload. JSON Schema: permissive, no enforcement. Test all evolution scenarios: (1) Add optional field (backward compatible), (2) Remove field (forward compatible), (3) Rename field (breaking), (4) Change type (breaking). Show: consumer with old schema reads new data (Avro handles transparently), breaking change crashes old consumer. Best practices for each format." },
 
  { id: 258, title: "Real-Time Aggregation with HyperLogLog", cat: "Data Engineering", stack: "Python + Redis", prompt: "Implement HyperLogLog for approximate distinct count and use it in real-time analytics. Problem: COUNT DISTINCT on 1B events requires storing all 1B values (GB of RAM). HyperLogLog: <1KB per counter, 2% error. Implement HyperLogLog from scratch: hash function, bucket selection, register update, cardinality estimation formula. Use Redis HLL for production-grade implementation. Show: count unique visitors per page per hour, count unique IPs in a DDoS detection window. Compare: exact set (1GB RAM) vs HLL (1KB RAM, 98% accurate)." },
 
  { id: 259, title: "Change Tracking & Incremental Processing", cat: "Data Engineering", stack: "Python + PostgreSQL + Redis", prompt: "Build incremental processing that only re-processes changed data (not full re-scans). Strategies: (1) Updated_at timestamp watermark (scan rows updated > last run), (2) Row hash change detection (compare current vs stored hash), (3) CDC stream (Postgres logical replication). Implement all 3, compare: timestamp (misses deletes), hash (expensive but comprehensive), CDC (real-time but complex). Build: incremental materialized view refresh (update only changed rows, not rebuild from scratch). Show: 10M row table, full refresh 60s vs incremental 0.5s." },
 
  // ─────────────────────────────────────────────
  // TESTING & RELIABILITY
  // ─────────────────────────────────────────────
  { id: 260, title: "Chaos Engineering Framework", cat: "Testing & Reliability", stack: "Python + FastAPI + Docker", prompt: "Build a chaos engineering platform (Chaos Monkey-lite). Fault types: (1) Network latency injection (add 200ms to specific service), (2) Packet loss (drop 5% of requests), (3) Service crash (kill container), (4) CPU spike (stress-ng), (5) Disk fill (consume disk space). Experiments: define fault + scope + duration + success criteria. Safety: automatic rollback if error rate exceeds threshold. Show: inject DB latency → observe cascade → circuit breaker opens → system degrades gracefully (not crashes). Generate: experiment report with hypothesis, blast radius, outcome." },
 
  { id: 261, title: "Property-Based Testing", cat: "Testing & Reliability", stack: "Python + Hypothesis", prompt: "Use property-based testing to find bugs that unit tests miss. Properties to test: (1) Serialization round-trip: serialize(deserialize(x)) == x, (2) Commutativity: sort(sort(x)) == sort(x), (3) Monotonicity: if a <= b then f(a) <= f(b), (4) Idempotency: apply_migration(apply_migration(db)) == apply_migration(db). Use Hypothesis to generate random inputs. Find a real bug: implement a buggy JSON parser, Hypothesis finds the edge case (empty string, nested arrays) that causes failure. Show: Hypothesis shrinks failing case to minimal reproducer. Explain: why random testing beats manual test cases." },
 
  { id: 262, title: "Contract Testing (Pact)", cat: "Testing & Reliability", stack: "Python + Pact + FastAPI", prompt: "Implement consumer-driven contract testing between a frontend consumer and backend API provider. Consumer: writes tests defining expected API shape (request/response). Pact: generates contract file (JSON). Provider: verifies contract (runs recorded requests, checks responses match). Show: consumer expects {id, email}, provider adds {id, email, phone} → still compatible (additive). Provider removes {email} → contract broken → CI fails. Implement: Pact broker (share contracts between teams), bi-directional contract testing, canary release validation. This prevents integration test environment hell." },
 
  { id: 263, title: "Load Test Framework from Scratch", cat: "Testing & Reliability", stack: "Python + asyncio", prompt: "Build a load testing framework (locust-lite). DSL: define user behavior as Python coroutines (login → browse → add to cart → checkout). Executor: spawn N virtual users, each runs behavior loop. Metrics collector: latency histogram, throughput, error rate per endpoint. Ramp: gradual user ramp (0 → 1000 users over 5 minutes). Output: real-time terminal dashboard + final HTML report. Show: find the breaking point of your FastAPI app (at what RPS does p99 exceed 500ms?). Add: think time simulation, session state per user, correlation ID tracking." },
 
  { id: 264, title: "Snapshot Testing for APIs", cat: "Testing & Reliability", stack: "Python + pytest + FastAPI", prompt: "Build a snapshot testing system for API responses. First run: record actual response as snapshot. Subsequent runs: compare actual vs snapshot, fail if different. Smart diffing: ignore dynamic fields (timestamps, IDs) via path exclusion config. Update workflow: explicit snapshot update command (not automatic). Implement: snapshot serialization (JSON with sorted keys for stability), snapshot review (show diff before approving). Show: API refactor changes response shape → snapshot fails → developer reviews diff → approves intentional change. This prevents accidental API breakage." },
 
  { id: 265, title: "Test Data Management", cat: "Testing & Reliability", stack: "Python + PostgreSQL + FastAPI", prompt: "Build a test data management system for integration tests. Fixtures: define reusable test data as Python factories (UserFactory, OrderFactory with sensible defaults + overrides). Database strategies: (1) Truncate + seed per test (slow but isolated), (2) Rollback transaction per test (fast, works for most cases), (3) Snapshot + restore (Docker volume snapshot for complex setups). Implement: builder pattern for complex object graphs (User with 3 Orders each with 2 Items), data masking for production data clones, cleanup verification (no orphan data after test). Show: 100 integration tests running in <10s." },
 
  { id: 266, title: "Fuzz Testing a Protocol Parser", cat: "Testing & Reliability", stack: "Python + atheris or AFL++", prompt: "Implement fuzzing for a custom protocol parser (HTTP-lite, DNS, or custom binary format). Fuzzer: mutate valid inputs (bit flips, byte substitutions, length field corruption, nested structure fuzzing). Track: coverage (which parser code paths are hit), crashes, hangs. Implement: corpus management (keep interesting inputs that hit new coverage), crash deduplication (unique stack traces). Find: buffer overflow, integer overflow, infinite loop in your parser. Show: fuzzer finds a crash in <60 seconds that a normal test suite would miss. This is how real CVEs are found." },
 
  { id: 267, title: "Resilience Testing with Toxiproxy", cat: "Testing & Reliability", stack: "Python + Toxiproxy + FastAPI", prompt: "Build automated resilience tests using Toxiproxy to inject network failures. Test scenarios: (1) Latency: 500ms DB latency → app uses cache fallback, (2) Bandwidth limit: 1Kbps connection → app returns cached stale data, (3) Slicer: TCP connection resets → retry succeeds, (4) Timeout: connection never responds → circuit breaker opens within 3s. Automate: run each scenario in CI, verify system behavior meets spec (not crashes, degrades gracefully). Show: without resilience code → test fails, with circuit breaker + fallback → all tests pass." },
 
  // ─────────────────────────────────────────────
  // INFRASTRUCTURE (deeper topics)
  // ─────────────────────────────────────────────
  { id: 268, title: "Zero-Downtime Database Migration", cat: "Infrastructure", stack: "Python + PostgreSQL + Alembic", prompt: "Implement zero-downtime database schema migrations for a live system. Technique: expand-contract pattern. (1) EXPAND: add new column nullable (old code works, new code writes both), (2) BACKFILL: populate new column in batches (small locks, avoid table scan), (3) SWITCH: deploy new code using new column only, (4) CONTRACT: drop old column (now unused). Show: each phase deployed separately, no downtime, old + new app versions coexist. Handle: adding NOT NULL constraint (populate first, then add constraint), renaming column, splitting table. This is how Stripe, GitHub do migrations." },
 
  { id: 269, title: "Infrastructure as Code: Terraform Module", cat: "Infrastructure", stack: "Terraform + AWS/GCP/localstack", prompt: "Build a production-grade Terraform module for deploying a 3-tier web application. Resources: VPC with public/private subnets, NAT gateway, ALB + target group, ECS Fargate service (or Kubernetes), RDS PostgreSQL with read replica, ElastiCache Redis, S3 bucket with CloudFront. Module inputs: environment (dev/staging/prod), instance sizes, replica counts. Show: plan shows exact changes, apply is idempotent, state locking prevents concurrent apply, workspace per environment. Implement: remote state (S3 + DynamoDB lock), module versioning, variable validation, output values for other modules." },
 
  { id: 270, title: "Build System from Scratch (Make-lite)", cat: "Infrastructure", stack: "Python", prompt: "Build a dependency-aware build system like Make. Features: rule definitions (target: dependencies → commands), dependency graph with cycle detection, incremental builds (only rebuild if source newer than target, using file modification times + content hashing), parallel execution (independent targets in parallel), remote caching (cache build outputs by input hash). Show: project with 20 files, change one file → only dependent targets rebuild (not all 20). Compare: Make vs Bazel vs Gradle — why content hashing > timestamps for reproducibility." },
 
  { id: 271, title: "Secret Management System", cat: "Infrastructure", stack: "Python + FastAPI + HashiCorp Vault concepts", prompt: "Build a secrets management system (Vault-lite). Features: (1) Secret storage with encryption at rest (AES-256-GCM), (2) Dynamic secrets: generate DB credentials on demand (short-lived, auto-revoke), (3) Secret leasing: secrets expire, clients must renew, (4) Access policies (which service can access which secrets), (5) Audit log (who accessed what, when). Show: app requests DB password → vault generates temporary user in PostgreSQL → credential expires after 1hr → auto-revoked. Compare: static secrets (env vars) vs dynamic secrets (generated on demand)." },
 
  { id: 272, title: "Container Image Build Optimizer", cat: "Infrastructure", stack: "Python + Docker", prompt: "Build a Dockerfile analyzer and optimizer. Analyze: layer order (frequently changing layers last), unnecessary files in layers (use .dockerignore), large base images, secrets accidentally baked in, multi-stage build opportunities. Implement: layer diff comparison (show size contribution of each layer), base image recommendations, multi-stage build generator. Show: naive Dockerfile (1GB image, secrets in layer), after optimization (100MB, multi-stage, secrets-free). Measure: build time, push time, pull time improvements. Generate: optimized Dockerfile with explanations." },
 
  { id: 273, title: "Autoscaling Controller", cat: "Infrastructure", stack: "Python + Kubernetes API + Prometheus", prompt: "Build a custom horizontal pod autoscaler. Metrics-driven scaling: query Prometheus for custom metrics (request queue depth, business metrics like orders/sec), not just CPU/memory. Scale-up: if metric > threshold for 60s → add pods (max 2x per scale event). Scale-down: if metric < threshold for 300s → remove pods (conservative, avoid flapping). Implement: stabilization window, cooldown period, predictive scaling (time-of-day patterns), scale-to-zero for background workers. Show: traffic spike → pods scale up in 90s, spike ends → scale down in 5 min." },
 
  { id: 274, title: "Distributed Config Management", cat: "Infrastructure", stack: "Python + etcd + FastAPI", prompt: "Build a distributed configuration management system backed by etcd. Features: (1) Hierarchical keys (/service/env/key), (2) Watch for changes (real-time config push to services), (3) Config versioning (rollback to previous version), (4) Environment promotion (copy config from staging → production), (5) Schema validation (reject invalid config), (6) Config diff (compare two versions). Show: service receives config update without restart, invalid config rejected at write time, rollback restores previous state. Compare: etcd vs ZooKeeper vs Consul for config management." },
 
  // ─────────────────────────────────────────────
  // AUTH & SECURITY (deeper topics)
  // ─────────────────────────────────────────────
  { id: 275, title: "Zero Trust Network Architecture", cat: "Auth & Security", stack: "Python + FastAPI + mTLS", prompt: "Implement Zero Trust Architecture: never trust, always verify. Every service-to-service call requires: (1) mTLS (mutual certificate authentication), (2) Short-lived certificates (rotate every 24h), (3) Authorization policy (service A can call service B endpoint X, not Y). Build: certificate authority (CA), cert issuance on service startup, SPIFFE/SPIRE concept (workload identity). Show: service C tries to call service B → blocked by policy (not authorized), service A → service B → allowed. No implicit trust based on network location." },
 
  { id: 276, title: "Cryptographic Primitives from Scratch", cat: "Auth & Security", stack: "Python", prompt: "Implement cryptographic primitives to understand how security libraries work. Build (don't use crypto library): (1) SHA-256 hash (bit manipulation, Merkle-Damgård construction), (2) HMAC (hash-based MAC using SHA-256), (3) PBKDF2 (password key derivation: HMAC repeated N times), (4) AES block cipher (SubBytes, ShiftRows, MixColumns, AddRoundKey — 10 rounds). Verify output matches Python's hashlib/cryptography library. Show: timing attack on naive password comparison vs constant-time comparison. Explain: why you should never implement crypto for production (side-channel attacks)." },
 
  { id: 277, title: "OAuth2 Threat Modeling", cat: "Auth & Security", stack: "Python + FastAPI", prompt: "Build OAuth2 flows and then attack/defend them. Attacks: (1) Authorization code interception (fix: PKCE), (2) State parameter CSRF (fix: verify state), (3) Open redirect (fix: whitelist redirect URIs), (4) Token leakage via Referer header (fix: referrer policy), (5) Refresh token theft (fix: rotation + family tracking), (6) Mix-up attack (fix: iss claim validation). For each: demonstrate the attack succeeding on a vulnerable implementation, then apply fix. Show attack/fix side-by-side. This is how security engineers approach OAuth2 deployments." },
 
  { id: 278, title: "API Security Hardening Toolkit", cat: "Auth & Security", stack: "Python + FastAPI", prompt: "Build a comprehensive API security hardening toolkit as FastAPI middleware. Implement: (1) Security headers (HSTS, CSP, X-Frame-Options, X-Content-Type-Options), (2) Request signing (HMAC-SHA256 of method+path+timestamp+body, reject if >5min old), (3) API key management (hashed storage, scoped permissions, rate limit per key), (4) Input sanitization (reject oversized payloads, Unicode normalization attacks, null byte injection), (5) Response filtering (strip internal fields, server headers). Run OWASP ZAP scan before and after — show security score improvement." },
 
  { id: 279, title: "Passkeys (WebAuthn) Implementation", cat: "Auth & Security", stack: "Python + FastAPI + py_webauthn", prompt: "Implement WebAuthn (Passkeys) authentication from scratch. Registration flow: server generates challenge → client creates credential (public/private key pair on device) → sends public key to server. Authentication flow: server generates challenge → device signs with private key → server verifies signature. Implement: attestation verification, resident keys (no username needed), user verification (biometric), backup eligibility (iCloud Keychain sync). Show: full flow in a web UI, credential stored in DB (public key only, no password!). Explain: why passkeys are phishing-resistant, how Apple/Google sync them." },
 
  { id: 280, title: "Runtime Application Self-Protection (RASP)", cat: "Auth & Security", stack: "Python + FastAPI", prompt: "Implement RASP — security that lives inside the application runtime, not a WAF. Hooks: (1) SQL query hook: detect SQL injection patterns before execution, block + log, (2) Subprocess hook: detect command injection (user input in shell command), (3) File system hook: detect path traversal (../../etc/passwd), (4) Deserialization hook: detect unsafe deserialization. Implement as Python import hooks/monkey-patching. Show: SQLi attack → RASP intercepts before DB call, no payload reaches DB. Compare: WAF (network level, bypassable with encoding) vs RASP (runtime, knows context)." },
 
  // ─────────────────────────────────────────────
  // PERFORMANCE (deeper topics)
  // ─────────────────────────────────────────────
  { id: 281, title: "CPU Cache Optimization", cat: "Performance", stack: "Python / C via ctypes", prompt: "Demonstrate CPU cache effects on performance. Matrix multiplication: naive (row × column, cache-unfriendly) vs tiled (process 64-byte cache-line-sized blocks). Memory access patterns: sequential (cache-friendly) vs random (cache-miss heavy). False sharing: two threads updating adjacent variables on same cache line → 10x slower than separate cache lines. Show: profile with perf stat (cache-miss count), tiled matrix multiplication 3x faster. Implement: structure-of-arrays vs array-of-structures for SIMD vectorization. This explains why data layout matters." },
 
  { id: 282, title: "Memory-Mapped File Processing", cat: "Performance", stack: "Python + mmap", prompt: "Use memory-mapped files to process large datasets efficiently. Compare: read() (copies to user-space buffer), mmap (OS handles paging). Use case: process a 10GB log file, search for error patterns. mmap: map entire file, search with regex (OS pages in only accessed parts), no OOM even on 4GB RAM machine. Implement: concurrent readers (multiple processes mmap same file, no data copy), file-backed shared memory between processes, memory-mapped DB page cache simulation. Show: mmap 5x faster than fread for random access, similar for sequential." },
 
  { id: 283, title: "Connection Pool Internals", cat: "Performance", stack: "Python + PostgreSQL + asyncpg", prompt: "Build a database connection pool from scratch with all production features. Features: (1) Min/max pool size with dynamic scaling, (2) Connection validation (ping before checkout, discard stale), (3) Checkout timeout (fail fast if pool exhausted), (4) Pool wait queue (FIFO + priority), (5) Connection age limit (recycle old connections to avoid memory leaks), (6) Per-host pool (multi-DB routing), (7) Metrics: checkout time, pool utilization, wait time. Show: connection storm at startup (all connections establish in parallel), pool exhaustion behavior, connection leak detection." },
 
  { id: 284, title: "Zero-Copy Data Transfer", cat: "Performance", stack: "Python + sendfile syscall", prompt: "Implement zero-copy data transfer techniques to maximize throughput. Methods: (1) sendfile(): kernel transfers file directly to socket (no user-space copy), (2) splice(): pipe two file descriptors without kernel↔user copies, (3) mmap() + write(): map file into user space, write to socket. Compare: read()+write() (2 copies), sendfile() (0 copies for file serving). Build: static file server using sendfile, measure throughput at 10Gbps. Show: strace comparison (syscall count), CPU usage (read+write = 100% CPU, sendfile = 30%). This is how Nginx serves static files." },
 
  { id: 285, title: "Async I/O with io_uring", cat: "Performance", stack: "Python + liburing via ctypes or C", prompt: "Implement high-performance async I/O using Linux io_uring (the modern alternative to epoll). io_uring: submission ring + completion ring, batch syscalls, no context switches per I/O. Compare: traditional epoll (one syscall per event), io_uring (batch 1000 events with one syscall). Build: HTTP server using io_uring for socket accept + read + write. Benchmark: epoll vs io_uring at 100k connections, expect 30-50% throughput improvement. Show: syscall count difference (strace). Explain: why Nginx, Rust tokio, and Node.js are adopting io_uring. Kernel version requirement: 5.1+." },
 
  { id: 286, title: "Profiling & Flame Graph Analysis", cat: "Performance", stack: "Python + py-spy + perf", prompt: "Build a profiling toolkit and use it to find real bottlenecks. Tools: (1) py-spy: sampling profiler for Python (no code changes), (2) cProfile: deterministic profiler (traces every call), (3) memory_profiler: line-by-line memory usage, (4) perf: CPU performance counters (cache misses, branch mispredictions). Workflow: run load test → observe slowdown → capture flame graph → identify hot function → fix → verify improvement. Apply to a real slow endpoint. Show: before/after flame graphs, identify that 80% of CPU is in JSON serialization → switch to orjson → 3x faster." },
 
  { id: 287, title: "Database Query Batching & N+1 Elimination", cat: "Performance", stack: "Python + SQLAlchemy + FastAPI", prompt: "Systematically find and eliminate N+1 query problems. Tools: (1) SQL query logger (count queries per request), (2) N+1 detector (alert if same query runs > 5 times per request), (3) DataLoader batching (batch queries by key). Fix strategies: (1) Eager loading (JOIN or IN clause), (2) DataLoader (batch + cache per request), (3) Denormalization (precompute counts), (4) Materialized views. Show: list endpoint runs 101 queries (1 for list + 100 for author per post) → after fix: 2 queries. Measure: 500ms → 10ms response time." },
 
  // ─────────────────────────────────────────────
  // DISTRIBUTED SYSTEMS (deeper topics)
  // ─────────────────────────────────────────────
  { id: 288, title: "Vector Clocks & Causal Consistency", cat: "Distributed Systems", stack: "Python", prompt: "Implement vector clocks for causal consistency (used in DynamoDB, Riak). Each node maintains a vector clock: {nodeA: 3, nodeB: 1, nodeC: 2}. On send: increment own entry. On receive: merge (take max per entry). Detect causality: if vc1 < vc2 → vc1 happened before vc2. Detect concurrency: neither dominates → conflict (needs merge). Show: Alice updates, Bob reads Alice's update and updates, Charlie reads both → vector clock proves Charlie saw both. Implement: conflict resolution (last-write-wins, semantic merge). Explain: why Lamport timestamps are insufficient (only total order, no causality)." },
 
  { id: 289, title: "Raft Consensus Algorithm", cat: "Distributed Systems", stack: "Python + sockets", prompt: "Implement the Raft consensus algorithm from scratch across 5 nodes. Phases: (1) Leader election (RequestVote RPC, majority vote, term-based), (2) Log replication (AppendEntries RPC, leader sends to all followers, commit on majority acknowledgment), (3) Safety (only elect leader with most up-to-date log). Simulate: node crashes, network partition (split-brain), leader crash mid-commit. Show: data written to leader appears on all followers within 2 heartbeat intervals. Test: kill 2 of 5 nodes, system still makes progress. Kill 3 → system stalls (no quorum). This is how etcd, CockroachDB, TiKV work." },
 
  { id: 290, title: "CRDTs: Conflict-Free Data Structures", cat: "Distributed Systems", stack: "Python", prompt: "Implement Conflict-Free Replicated Data Types (CRDTs) for eventually consistent collaboration. Implement: (1) G-Counter (grow-only, merge = per-node max), (2) PN-Counter (positive + negative counters), (3) LWW-Register (last-write-wins with timestamp), (4) OR-Set (observed-remove set, handles add-then-remove), (5) YATA/RGA (sequence CRDT for collaborative text editing). Show: two users edit concurrently offline → merge produces consistent result. No conflicts, no coordination needed. Implement: collaborative todo list that merges offline edits. Explain: why Google Docs uses OT, not CRDTs (historical), why Figma and Notion moved to CRDTs." },
 
  { id: 291, title: "Distributed Rate Limiting (Global)", cat: "Distributed Systems", stack: "Python + Redis + FastAPI", prompt: "Implement truly global rate limiting across multiple API server instances. Problem: token bucket in local memory allows N requests per server × M servers = M×N total (wrong). Solution: (1) Redis atomic operations (INCR + EXPIRE), (2) Redis Lua script for atomic token bucket, (3) Sliding window log in Redis sorted set. Handle: Redis failure fallback (local limit as degraded mode), Redis Cluster (hash slot for user key), rate limit headers (X-RateLimit-Remaining, X-RateLimit-Reset). Show: 3 API servers, 100 req/min global limit enforced correctly. Benchmark: overhead of Redis call per request (< 1ms with pipelining)." },
 
  { id: 292, title: "Geo-Distributed System Design", cat: "Distributed Systems", stack: "Python + FastAPI + PostgreSQL", prompt: "Design and implement a geo-distributed system serving users from 3 regions (US, EU, APAC). Components: (1) Global load balancer (GeoDNS / anycast routing), (2) Regional data stores (each region has its own DB), (3) Cross-region replication (async, with conflict detection), (4) Data residency enforcement (EU user data stays in EU — GDPR), (5) Read-local policy (serve from nearest region), (6) Write routing (some writes go to home region). Show: EU user → request routed to EU region, data never leaves EU, consistent read-your-writes within same region. Latency: US user → EU API: 150ms, US API: 10ms." },
 
  { id: 293, title: "Anti-Entropy Repair Protocol", cat: "Distributed Systems", stack: "Python", prompt: "Implement anti-entropy repair used in Cassandra/DynamoDB to fix inconsistencies between replicas. Merkle tree: hash each data partition recursively, compare trees between replicas to find diverged subtrees efficiently. Repair process: compare tree roots → if equal, done; if different, recurse to find diverged leaf nodes → sync only diverged data. Show: 3 replicas, inject inconsistency on replica 2 (missed a write), Merkle comparison finds diverged range in O(log n) instead of O(n) full scan. Measure: repair bandwidth vs full scan. This is the core of Cassandra `nodetool repair`." },
 
  { id: 294, title: "Backpressure Propagation", cat: "Distributed Systems", stack: "Python + asyncio + FastAPI", prompt: "Implement backpressure propagation through a pipeline of services. Problem: producer faster than consumer → memory grows unbounded → OOM. Solution: Reactive Streams backpressure protocol. Upstream slows down based on downstream capacity signal. Implement: (1) Bounded queues with producer blocking, (2) TCP-style flow control (receiver sends window size), (3) Load shedding (drop work when overloaded, return 503 + Retry-After). Show: producer at 1000 req/s, consumer at 100 req/s, without backpressure → OOM in 10s, with backpressure → stable at 100 req/s, no memory growth. This is how Kafka, gRPC, and Reactive systems handle overload." },
 
  // ─────────────────────────────────────────────
  // MESSAGING (deeper topics)
  // ─────────────────────────────────────────────
  { id: 295, title: "Kafka Internals: Log Segments & Compaction", cat: "Messaging", stack: "Python + Kafka", prompt: "Understand Kafka internals by building a simplified Kafka broker. Log storage: append-only segment files, index files (offset → file position for O(1) seek), rolling segments (new segment every 1GB). Consumer offset tracking: each consumer group stores offset per partition. Log compaction: for compacted topics, keep only the latest value per key (like a KV store with history). Show: write 1M messages, log segments roll, compaction reduces 100 versions of same key to 1. Measure: throughput (MB/s), read latency with index vs without." },
 
  { id: 296, title: "Dead Letter Queue Handling", cat: "Messaging", stack: "Python + RabbitMQ + FastAPI", prompt: "Build a comprehensive dead letter queue (DLQ) system. Message routing to DLQ: (1) Max retry exceeded (poison pill), (2) TTL expired, (3) Queue max length. DLQ inspector: view DLQ messages, inspect payload + headers + error metadata (which consumer failed, why). Replay workflow: operator reviews DLQ, fixes code bug, bulk-replays messages. Poison pill detection: message that always fails → auto-quarantine (move to separate quarantine queue). Show: message processing fails 3× → DLQ, inspector shows the error, replay after fix → processes successfully." },
 
  { id: 297, title: "Transactional Outbox Pattern", cat: "Messaging", stack: "Python + PostgreSQL + Kafka", prompt: "Implement the Transactional Outbox pattern to guarantee exactly-once event publishing. Problem: write to DB + publish to Kafka atomically (two-phase distributed transaction is expensive). Solution: write event to outbox table in same DB transaction, separate outbox poller publishes to Kafka + marks as sent. Implement: outbox table (id, aggregate_id, event_type, payload, status, created_at), CDC-based poller (reads new rows), Kafka publish with idempotency key. Show: process crashes between DB write and Kafka publish → on restart, outbox poller retries → event published exactly once." },
 
  { id: 298, title: "Message Priority & Scheduling", cat: "Messaging", stack: "Python + Redis + FastAPI", prompt: "Build a message queue with priority levels and scheduled delivery. Priority: 5 levels (CRITICAL, HIGH, NORMAL, LOW, BACKGROUND). Consumer: always drain CRITICAL before HIGH before NORMAL. Prevent starvation: if LOW message waits >5 minutes, promote to NORMAL. Scheduled delivery: enqueue message with deliver_at timestamp (Redis sorted set by timestamp), scheduler moves to priority queue at correct time. Recurring jobs: cron-like schedule. Show: CRITICAL message enqueued → processed before 100 NORMAL messages already in queue." },
 
  { id: 299, title: "Event Streaming with Watermarks", cat: "Messaging", stack: "Python + Kafka + custom stream processor", prompt: "Implement event time processing with watermarks for handling late events. Event time vs processing time: events arrive out of order due to network delays. Watermark: assertion that 'all events before time T have arrived.' Window close: trigger window computation when watermark passes window end. Implement: watermark generator (current max event time - 30s lag), tumbling window (1 min), trigger on watermark. Show: events arriving up to 25s late → included in window, events arriving 35s late → dropped (past watermark). Allow event-time joins across two Kafka topics with watermark alignment." },
 
  { id: 300, title: "Competing Consumers Pattern", cat: "Messaging", stack: "Python + RabbitMQ", prompt: "Implement the competing consumers pattern for parallel message processing. Single queue, N consumers, first consumer to pull message processes it. Challenges: (1) Message ordering (lost when consumers process in parallel), (2) Consumer fairness (prefetch count controls how many messages per consumer), (3) Consumer crash recovery (unacked messages requeued), (4) Scaling: add consumers dynamically. Benchmark: 1 consumer (100 msg/s), 5 consumers (450 msg/s — not linear due to coordination overhead). Implement: consumer group scaling automation (add consumer when queue depth > threshold). Show ordering guarantee tradeoff." },
 
  // ─────────────────────────────────────────────
  // OBSERVABILITY (deeper / new angles)
  // ─────────────────────────────────────────────
  { id: 301, title: "OpenTelemetry Collector Pipeline", cat: "Observability", stack: "Python + OpenTelemetry Collector + Prometheus + Jaeger", prompt: "Build a complete OpenTelemetry Collector pipeline. Receivers: OTLP (from services), Prometheus scrape. Processors: batch (reduce export calls), memory_limiter (prevent OOM), attributes (add/remove/rename fields), sampling (tail-based: keep all errors, 1% of success). Exporters: Jaeger (traces), Prometheus (metrics), Elasticsearch (logs). Show: collector as a central hub, change backend without touching service code (just reconfigure collector). Implement: multi-pipeline routing (errors → PagerDuty, metrics → Prometheus), data filtering (drop PII before export)." },
 
  { id: 302, title: "SLO Error Budget Tracker", cat: "Observability", stack: "Python + Prometheus + FastAPI", prompt: "Build an SLO (Service Level Objective) error budget tracking system. Define SLOs: availability (99.9% = 43.8 min downtime/month), latency (p99 < 500ms for 99% of requests). Error budget = 1 - SLO target = budget to spend on incidents + releases. Track: remaining error budget as fraction, burn rate (how fast budget depletes), multi-window burn rate alert (fast burn over 1hr + 6hr). Dashboard: budget remaining (green/yellow/red), burn rate sparkline, top error contributors. Policy: if budget < 10%, freeze non-critical releases. Show: incident consumes budget, burn rate alert fires." },
 
  { id: 303, title: "Continuous Profiling Integration", cat: "Observability", stack: "Python + py-spy + FastAPI + Pyroscope", prompt: "Integrate always-on continuous profiling into a production service. Profiling agent: sample CPU stacks every 10ms (py-spy), aggregate into flame graphs per time window (every 10s), ship to Pyroscope or custom backend. Correlate with traces: attach profile snapshot to slow traces (when p99 latency spike detected, capture profile of that time window). Show: identify a CPU regression introduced in a deploy (flame graph comparison before/after). Implement: low overhead sampling (< 1% CPU overhead), profiling pause during high load, per-endpoint CPU breakdown." },
 
  { id: 304, title: "Multi-Signal Incident Correlation", cat: "Observability", stack: "Python + Prometheus + Elasticsearch + Jaeger", prompt: "Build an incident correlation engine that connects logs + metrics + traces automatically. When alert fires: (1) Find relevant traces in the same time window (high error rate traces), (2) Find related logs (ERROR logs with matching service + time), (3) Find correlated metrics changes (DB latency spike, memory growth). Output: incident card showing all 3 signals aligned on same timeline. Implement: trace-to-log correlation (trace_id in logs), metric anomaly detection (Z-score on alert time), root cause ranking (which signal changed first). Show: DB connection pool exhaustion incident — correlates trace timeouts + pool metric + error logs." },
 
  { id: 305, title: "Synthetic Monitoring & Canary Checks", cat: "Observability", stack: "Python + FastAPI + Playwright", prompt: "Build a synthetic monitoring system that continuously tests critical user journeys. Checks: (1) API health (HTTP GET /health every 30s), (2) Critical flow (Playwright: browse → add to cart → checkout → confirm, every 5 min), (3) Multi-region probes (run checks from 3 geographic regions, detect regional outages). Alerting: if check fails from 2 of 3 regions → P1 alert. Show: deploy a breaking change, synthetic check detects failure in <5 minutes (before real users notice). Implement: check history, waterfall diagram (which step failed), screenshot on failure, SLA reporting." },
 
  { id: 306, title: "Observability for Batch Jobs", cat: "Observability", stack: "Python + Prometheus + PostgreSQL", prompt: "Build observability specifically for batch/cron jobs (different from request/response). Metrics: (1) Job duration histogram (how long does nightly ETL take?), (2) Records processed / records failed, (3) Job lag (how late is the job from schedule?), (4) Checkpoint progress (% complete for long jobs). Alerting: job hasn't started within 10 min of schedule, job running 2x longer than p95, error rate > 1%. Implement: job heartbeat (update checkpoint table, alert if no heartbeat for > 5 min), anomaly detection (is today's job significantly slower than last 30 days?). Show: stuck job detection mid-run." },
 
  // ─────────────────────────────────────────────
  // API DESIGN (deeper topics)
  // ─────────────────────────────────────────────
  { id: 307, title: "API Versioning Strategies Compared", cat: "API Design", stack: "Python + FastAPI", prompt: "Implement and compare 4 API versioning strategies side by side. (1) URL versioning (/v1/, /v2/): simple, explicit, creates duplication. (2) Header versioning (Accept: application/vnd.api+json;version=2): clean URLs, hidden complexity. (3) Query param versioning (?version=2): easy for testing, hard to cache. (4) Content negotiation (Accept: application/vnd.myapi.v2+json): RESTful, complex. Build same API in all 4 styles, same client uses each. Measure: developer experience, cache behavior (URL versions cache better), router complexity. Include: sunset policy (return Sunset header on deprecated versions), migration guide generation." },
 
  { id: 308, title: "Hypermedia API (HATEOAS)", cat: "API Design", stack: "Python + FastAPI", prompt: "Build a HATEOAS (Hypermedia as the Engine of Application State) API. Every response includes _links to related actions: GET /orders/123 returns {id: 123, status: 'pending', _links: {cancel: '/orders/123/cancel', pay: '/orders/123/pay'}}. Client discovers what actions are available from response, not hardcoded. Implement: HAL format, link relations (self, next, prev, related), conditional links (cancel only shown if status=pending). Show: client navigates entire workflow by following links, never constructing URLs. Explain: why HATEOAS is theoretically ideal but rarely used in practice (discoverability vs simplicity tradeoff)." },
 
  { id: 309, title: "GraphQL Federation", cat: "API Design", stack: "Python + Strawberry + FastAPI", prompt: "Build a federated GraphQL API composed from 3 independent subgraphs. Subgraphs: UserService (User type), OrderService (Order type with user reference), ProductService (Product type). Gateway: Apollo Federation gateway that stitches subgraphs. Implement: @key directive (User.id is the federation key), @external (reference type from another subgraph), @requires (OrderService needs User.email from UserService). Show: single query fetches user + orders + products with one round-trip. Each team deploys independently, schema composition at gateway. Explain: why federation > schema stitching." },
 
  { id: 310, title: "Event-Driven API (Webhooks)", cat: "API Design", stack: "Python + FastAPI + PostgreSQL + Redis", prompt: "Build a production-grade webhook delivery system. Features: (1) Webhook registration (URL, events, secret), (2) Payload signing (HMAC-SHA256 signature in header for consumer verification), (3) Delivery with retry (exponential backoff, max 25 attempts over 3 days), (4) Delivery log (each attempt: status code, response body, latency), (5) Failure notification (email after 3 failures), (6) Replay (re-deliver historical events). Show: consumer receives webhook, verifies signature, processes idempotently. Handle: consumer returns 200 but processing fails → needs idempotency key. Compare: polling vs webhooks vs server-sent events vs WebSocket." },
 
  { id: 311, title: "BFF (Backend For Frontend) Pattern", cat: "API Design", stack: "Python + FastAPI", prompt: "Build the Backend For Frontend pattern: dedicated API layer per client type. Clients: Mobile BFF (reduced payload, battery-optimized batching), Web BFF (richer data, session cookies), Third-party API (OAuth2, versioned, rate-limited). Each BFF aggregates from same internal services but shapes response differently. Mobile: 3 services → 1 call, gzip, reduced fields. Web: real-time updates via SSE, CSRF tokens. API: stable versioned contract. Show: mobile client payload 5KB vs web payload 50KB for same data. Implement: BFF-specific caching strategies, A/B test support per BFF." },
 
  { id: 312, title: "API Gateway Rate Limiting & Quota Management", cat: "API Design", stack: "Python + FastAPI + Redis", prompt: "Build an API gateway with sophisticated quota management. Quota types: (1) Rate limit (100 req/min per user), (2) Quota (10,000 req/day), (3) Burst allowance (allow 200 req/min briefly), (4) Concurrent request limit (max 10 in-flight per user). Quota persistence: daily reset, partial usage rollover (unused quota doesn't accumulate beyond 2x). Billing integration: track overage for metered billing. Headers: X-RateLimit-Limit, X-RateLimit-Remaining, X-RateLimit-Reset, X-Quota-Used, X-Quota-Limit. Show: quota exhaustion → 429 with Retry-After, burst through 200 for 30s then throttled back to 100. Admin API to adjust limits." },
 
  // ─────────────────────────────────────────────
  // BONUS: SYSTEM DESIGN CLASSICS (POC implementations)
  // ─────────────────────────────────────────────
  { id: 313, title: "URL Shortener (System Design Classic)", cat: "Distributed Systems", stack: "Python + FastAPI + PostgreSQL + Redis", prompt: "Build a production-scale URL shortener with every system design consideration. Short ID generation: base62 encoding of auto-increment ID (vs MD5 hash — explain collision risk). Redirect: HTTP 301 (cached by browser) vs 302 (trackable). Storage: URL table + click event table. Scale: 100M URLs, 10B clicks/day. Optimizations: Redis cache for hot URLs (top 10% URLs serve 99% of traffic), bloom filter to check URL existence before DB. Analytics: click count by day, geographic distribution. Show: full write path (create short URL), read path (redirect + async click tracking). Handle: custom aliases, expiration, URL validation." },
 
  { id: 314, title: "Notification Service (Email/SMS/Push)", cat: "Messaging", stack: "Python + Celery + Redis + FastAPI", prompt: "Build a multi-channel notification service. Channels: email (SendGrid), SMS (Twilio), push notification (FCM). Features: (1) Template engine (Jinja2 with variable substitution), (2) User channel preferences (user opts out of SMS), (3) Deduplication (don't send same notification twice in 1hr), (4) Priority routing (critical alerts → all channels, marketing → email only), (5) Rate limiting per user (max 10 emails/day), (6) Delivery tracking (sent, delivered, bounced, opened). Show: payment failure event → triggers critical notification → delivered via all 3 channels → dedup prevents duplicate on retry." },
 
  { id: 315, title: "Distributed ID Generator (Snowflake)", cat: "Distributed Systems", stack: "Python", prompt: "Implement Twitter Snowflake ID generation. 64-bit ID: 41 bits timestamp (ms since epoch) + 10 bits machine ID + 12 bits sequence number. Properties: monotonically increasing (sortable by time), unique across machines without coordination, 4096 IDs/ms per machine. Implement: machine ID assignment (from ZooKeeper or env var), sequence overflow handling (wait to next ms), clock skew detection (monotonic clock guarantee). Show: generate 1M IDs across 3 instances, verify globally unique + time-ordered. Compare: UUID (no ordering, 128-bit) vs Snowflake (ordered, 64-bit) vs ULID (URL-safe, ms precision). Show use in distributed DB shard key." },
 
  { id: 316, title: "Search Autocomplete / Typeahead", cat: "Performance", stack: "Python + Redis + FastAPI", prompt: "Build a real-time search autocomplete service. Trie-based approach: insert all queries, prefix search. At scale: Redis sorted sets (store all prefixes as keys, score = query frequency), top-k results per prefix. Ranking: frequency + recency + personalization boost. Implement: (1) Query log ingestion (record searches), (2) Frequency aggregation (batch job), (3) Trie + Redis hybrid (exact match → trie, fuzzy match → Redis scan), (4) Caching layer (precompute top-1000 prefixes). Show: 'py' → ['python', 'pytorch', 'pandas'] in < 5ms. Handle: multi-word queries, misspelling tolerance (BK-tree for edit distance). Benchmark: 10k QPS at p99 < 20ms." },
 
  { id: 317, title: "Ride Matching System (Geospatial)", cat: "Distributed Systems", stack: "Python + Redis + FastAPI + PostGIS", prompt: "Build a geospatial ride-matching system (Uber/Lyft-lite). Driver location tracking: drivers send GPS every 5s, stored in Redis GEO. Rider request: find all drivers within 2km using Redis GEORADIUS. Matching algorithm: assign nearest available driver, avoid concurrent assignment (Redis atomic operations). Real-time tracking: driver location pushed to rider via WebSocket. Challenges: (1) Driver hotspot (many requests in small area), (2) Rebalancing (incentivize drivers to move), (3) ETA calculation (PostGIS road network). Show: rider requests → find 5 nearest drivers → assign best → driver accepts → real-time position updates begin." },
 
  { id: 318, title: "Collaborative Document Editing (OT)", cat: "Distributed Systems", stack: "Python + FastAPI + WebSocket", prompt: "Build a collaborative document editor using Operational Transformation (Google Docs approach). Operations: insert(pos, char), delete(pos). Transform function: if two concurrent ops conflict, transform one to account for the other (op1 ∘ transform(op2, op1) = op2 ∘ transform(op1, op2) — convergence guarantee). Server: maintain operation history, broadcast transformed ops to all clients. Show: Alice types 'Hello' at pos 0, Bob simultaneously types 'World' at pos 0, after transform both documents show 'WorldHello' (or 'HelloWorld', deterministically). Implement: undo/redo stack, cursor position transformation." },
 
  { id: 319, title: "Distributed Counter (HLL + Sharding)", cat: "Distributed Systems", stack: "Python + Redis + FastAPI", prompt: "Build a globally accurate distributed counter system serving 1M events/sec. Approaches: (1) Single Redis INCR (bottleneck at ~100k/sec), (2) Sharded counters (N Redis shards, SUM for total — eventual consistency), (3) HyperLogLog for approximate distinct count (see-also project 258), (4) Count-Min Sketch for frequency estimation (who are the top-100 most active users right now?). Implement all approaches, benchmark. Show: exact vs approximate tradeoff. Real-time leaderboard: top-K users by event count using Redis sorted set. Benchmark: 1M inserts/sec on sharded approach." },
 
  { id: 320, title: "Recommendation Engine (Collaborative Filtering)", cat: "ML Systems", stack: "Python + NumPy + FastAPI", prompt: "Build a product recommendation engine using collaborative filtering. Matrix factorization (SVD-lite): decompose user-item rating matrix into user factors × item factors. Online inference: user embedding → cosine similarity to all item embeddings → top-K recommendations. Approximate nearest neighbor with FAISS for speed. Show: users with similar taste get similar recommendations. Handle: cold start problem (new user / new item — fall back to popularity), implicit feedback (clicks, not ratings), exploration vs exploitation (occasionally recommend outside comfort zone). Implement: A/B test comparing recommendations vs random baseline (click-through rate)." },
 
  { id: 321, title: "Task Queue with Priorities & Dependencies", cat: "Messaging", stack: "Python + PostgreSQL + FastAPI", prompt: "Build a full-featured task queue system. Features: (1) Task dependencies (task B can't start until A finishes), (2) Priority levels (critical → urgent → normal → deferred), (3) Task cancellation (cancel + all downstream dependents), (4) Progress tracking (0-100% with custom status messages), (5) Concurrent execution limiting (max 5 video transcoding jobs in parallel), (6) Sticky workers (GPU jobs must run on GPU workers), (7) Timeout per task. Show: DAG of 10 tasks, some parallel, execute optimally. Show: cancel a mid-pipeline task → downstream automatically skipped. Implement: task visualization UI (DAG with status colors)." },
 
  { id: 322, title: "API Mocking & Virtualization", cat: "Testing & Reliability", stack: "Python + FastAPI", prompt: "Build a dynamic API mock/virtualization server for development and testing. Features: (1) Record real API calls → replay as mock, (2) Request matching (method + path + query + body matching), (3) Response templates (inject dynamic values: timestamps, UUIDs), (4) Latency simulation (add realistic delay), (5) Fault injection (return 500 on 10% of requests), (6) Scenario management (switch mock behavior per test). Show: integration tests run 10x faster against mock vs real API. Implement: WireMock-compatible API so existing tools work. Record production traffic → instant mock without writing fixtures manually." },
 
  { id: 323, title: "Database Schema Linter", cat: "Database", stack: "Python + PostgreSQL", prompt: "Build a database schema linter that detects anti-patterns and suggests improvements. Rules: (1) Missing indexes on foreign keys, (2) VARCHAR(255) where TEXT is appropriate, (3) Nullable columns that should be NOT NULL (all values non-null), (4) Missing primary keys, (5) Large columns in frequently-joined tables (suggest extraction), (6) Over-indexed tables (index never used), (7) Missing updated_at timestamp (makes CDC harder), (8) Enum columns that should be FK to reference table. Scan live PostgreSQL DB, score each table, generate report with severity + fix scripts. Show: before score 45/100, after applying fixes: 90/100." },
 
  { id: 324, title: "Connection Broker / Proxy Protocol", cat: "Networking", stack: "Python + asyncio", prompt: "Build a connection broker that handles connection multiplexing and protocol translation. Features: (1) Connection pooling to upstream (maintain 10 persistent connections, fan out 1000 client connections), (2) Protocol translation (HTTP → gRPC, REST → Thrift), (3) PROXY protocol header injection (pass real client IP through load balancer to backend), (4) TLS termination + re-encryption (terminate TLS from client, re-encrypt to backend), (5) Connection mirroring (copy traffic to shadow service). Show: 1000 clients → 10 backend connections (multiplexed), protocol translation in action. This is how PgBouncer, Envoy, and HAProxy work." },
 
  { id: 325, title: "Trace Sampling Strategy Comparison", cat: "Observability", stack: "Python + OpenTelemetry", prompt: "Implement and compare trace sampling strategies for cost vs coverage. (1) Head sampling: decide at request start (simple, loses rare errors). (2) Tail sampling: buffer entire trace, decide after completion (keep all errors, expensive). (3) Adaptive sampling: increase rate for new code paths or high-error periods. (4) Priority sampling: always sample if user_tier=enterprise or feature_flag=experiment. Implement all 4 in a configurable sampler. Benchmark: traces/sec, storage cost per strategy. Show: head sampling misses a 0.1% error rate (below threshold), tail sampling catches all. Report: sampling rate per strategy vs error coverage." },
 
  { id: 326, title: "Configuration-Driven Architecture", cat: "Infrastructure", stack: "Python + FastAPI + Pydantic", prompt: "Build a configuration-driven system where behavior is defined by config, not code. Features: (1) Typed config with validation (Pydantic models with constraints), (2) Environment-specific overrides (base.yaml + production.yaml), (3) Runtime config reload (watch file for changes, hot-apply without restart), (4) Config diffing (show what changed between environments), (5) Config as code in git (history, review, rollback), (6) Secret injection (config references Vault path, resolved at load time). Show: add new endpoint by editing YAML, restart-free feature toggle, wrong type in config → rejected with clear error at startup, not runtime." },
 
  { id: 327, title: "Distributed Tracing: Custom Propagator", cat: "Observability", stack: "Python + OpenTelemetry + FastAPI", prompt: "Implement a custom trace context propagator to understand how distributed tracing works across service boundaries. Default: W3C traceparent header. Custom: implement B3 format (Zipkin), AWS X-Ray format, and a custom vendor format. Propagator interface: inject (add headers to outgoing request), extract (parse headers from incoming request). Show: trace starts in service A (W3C format) → service B uses B3 (translation at boundary) → service C uses X-Ray → all appear as one unified trace in Jaeger. Implement: multi-format propagator (try all formats, accept any). Explain: why format standardization matters." },
 
  { id: 328, title: "Batch vs Micro-Batch vs Streaming Comparison", cat: "Data Engineering", stack: "Python + Kafka", prompt: "Build the same data pipeline in 3 modes to show the tradeoffs. Use case: count errors per service per minute from a log stream. Batch: run job every 5 minutes, read all logs from last 5 min, aggregate, write result. Micro-batch: run every 10 seconds (Spark Streaming style). Streaming: process each event as it arrives, maintain rolling window (Flink style). Measure per mode: latency (event → result), throughput, resource usage, complexity, fault tolerance. Show: batch is simplest but 5-minute stale, streaming is freshest but most complex. Show: windowing semantics differ between modes (batch window = run interval, streaming = real event time)." },
 
  { id: 329, title: "Kubernetes Admission Controller", cat: "Cloud Native", stack: "Python + FastAPI + Kubernetes", prompt: "Build a Kubernetes admission webhook (validating + mutating). Mutating webhook: auto-inject sidecar container into all pods in namespace, add resource limits if missing, inject environment variables from central config. Validating webhook: reject pods without required labels (team, service), reject images from non-approved registries, enforce resource limits (no more than 4 CPU per container). Show: deploy pod without team label → rejected with clear message. Deploy pod missing resource limits → mutating webhook adds defaults. Implement: webhook server TLS (Kubernetes requires HTTPS), request timeout, failure policy (fail open vs fail closed)." },
 
  { id: 330, title: "Event Store & Projection Rebuild", cat: "Distributed Systems", stack: "Python + PostgreSQL + FastAPI", prompt: "Build a production-grade event store with projection system (deep dive beyond project #11). Features: (1) Event stream per aggregate with optimistic concurrency (expected version), (2) Snapshot store (avoid replaying 10,000 events for one aggregate), (3) Catchup subscription (read from position 0, catch up to live), (4) Live subscription (get events as they're written), (5) Projection registry (multiple read models from same events), (6) Projection rebuild (drop read model, replay all events → rebuilt). Show: rebuild user_summary projection from 1M events in 30 seconds. Handle: event schema migration (v1 event deserialized by v2 projector)." },
 
  { id: 331, title: "Monorepo Build Caching (Turborepo-lite)", cat: "Infrastructure", stack: "Python", prompt: "Build a monorepo build cache system. Hash each package's inputs (source files + dependencies + env vars) → cache key. On build: check cache store (local disk or S3), if hit → restore output artifacts, if miss → build + store. Dependency graph: only rebuild packages affected by a change (and their downstream dependents). Show: change 1 file in shared-utils → only shared-utils + dependents rebuild (not entire 50-package monorepo). Remote cache: share build outputs across CI machines (commit on machine A → machine B doesn't rebuild). Measure: CI time with/without cache (expect 10x improvement after cache warm-up)." },
 
  { id: 332, title: "API Client SDK Generator", cat: "API Design", stack: "Python + FastAPI + Jinja2", prompt: "Build a tool that generates type-safe client SDKs from an OpenAPI spec. Target languages: Python (with type hints + async), TypeScript, and Go. Generator: parse OpenAPI JSON → template engine → emit client code. Generated code: request methods (matching API endpoints), Pydantic/TypeScript interface models, error handling, authentication injection, retry logic. Show: FastAPI app → generate Python SDK → use SDK in client code → type-safe calls with autocompletion. Implement: SDK versioning (lock to API version), mock generation (test without real API). Compare: generated SDK vs hand-written (correctness, maintenance burden)." },
 
  { id: 333, title: "Distributed Workflow Engine", cat: "Distributed Systems", stack: "Python + PostgreSQL + FastAPI", prompt: "Build a durable workflow engine (Temporal/Conductor-lite). Workflow: long-running business process (order fulfillment: payment → inventory → shipping → delivery). Activities: individual steps (can fail, are retried). Durability: workflow state stored in DB after each step, survives server restarts. Features: (1) Activity retries with backoff, (2) Workflow versioning (deploy new version without breaking in-flight workflows), (3) Side effects (non-deterministic operations wrapped as activities), (4) Saga compensation (on failure, run compensating transactions in reverse). Show: server crashes mid-workflow → resumes from last completed step on restart." },
 
  { id: 334, title: "Edge Caching & CDN Simulation", cat: "Performance", stack: "Python + FastAPI + Redis", prompt: "Build a CDN edge caching simulation. Edge nodes: 3 PoPs (Points of Presence). Origin: single backend server. Cache key: method + URL + Vary headers (Accept-Encoding, Accept-Language). Cache control: respect Cache-Control headers (max-age, s-maxage, no-store, stale-while-revalidate). Purge API: purge by URL or tag (all pages tagged category:electronics). Features: (1) Stale-while-revalidate (serve stale, refresh in background), (2) Conditional GET (ETag + If-None-Match → 304 Not Modified), (3) Compression (Brotli/gzip at edge). Show: origin gets 1000 requests, edge serves 990 from cache (1% origin traffic). Measure: latency reduction (10ms edge vs 150ms origin)." },
 
  { id: 335, title: "Blue-Green Database Migration", cat: "Database", stack: "Python + PostgreSQL", prompt: "Implement blue-green database deployment (two identical DBs, switch traffic instantly). Setup: Blue DB (current production), Green DB (new schema). Migration: (1) Clone Blue → Green, (2) Apply schema changes to Green, (3) Set up continuous replication Blue → Green (logical replication), (4) Verify Green (run validation queries), (5) Atomic cutover (update connection string + stop Blue replication), (6) Keep Blue for 24hr rollback. Show: zero-downtime migration, connection cutover in <100ms, rollback by reverting connection string. Compare: vs traditional migration (expand-contract). Handle: data written during cutover window (no data loss guarantee)." },
 
  { id: 336, title: "Observability Data Pipeline", cat: "Observability", stack: "Python + Kafka + ClickHouse", prompt: "Build a scalable observability data pipeline. Ingest: services send events to Kafka (structured logs, metric data points, trace spans). Stream processor: enrich events (add k8s metadata from label cache), normalize (convert different metric formats to common schema), route (errors → priority queue). Storage: ClickHouse for logs/traces (columnar, fast aggregations), Prometheus for metrics. Query layer: SQL over ClickHouse for ad-hoc log queries, PromQL for metrics. Show: 1M events/sec ingest, query 1B log rows in < 5 seconds using ClickHouse materialized views. Compare storage cost: Elasticsearch vs ClickHouse for logs (5-10x compression advantage)." },
 
  { id: 337, title: "Consensus-Based Configuration", cat: "Distributed Systems", stack: "Python + etcd", prompt: "Use etcd to build a strongly consistent configuration and coordination service. Features: (1) Atomic configuration update (all services see same version simultaneously), (2) Leader election using etcd leases (only one instance becomes primary), (3) Distributed semaphore (limit concurrent access to slow resource), (4) Service registration with TTL (services register, auto-deregister if crashed), (5) Watch + react (service reconfigures on config change). Show: 5 service instances, one becomes leader, leader crashes → another elected in <5s, all others watch and see new leader. Explain: why etcd over ZooKeeper (gRPC API, raft-based, simpler operational model)." },
 
  { id: 338, title: "Polyglot Persistence Pattern", cat: "Database", stack: "Python + PostgreSQL + Redis + Elasticsearch + MongoDB", prompt: "Build a system that uses the right database for each use case. Use cases: (1) User accounts → PostgreSQL (ACID transactions, relational), (2) Session store → Redis (fast key-value, TTL), (3) Product search → Elasticsearch (full-text, facets), (4) Activity feed → MongoDB (flexible schema, append-heavy), (5) Analytics → ClickHouse (columnar, aggregations). Implement: data synchronization between stores (CDC or dual-write), consistency guarantees per store, failure handling when one store is unavailable. Show: create order → PostgreSQL, update search index (async), increment analytics counter (fire-and-forget). Discuss: where to put new features." },
 
  { id: 339, title: "Kubernetes Network Policy Enforcer", cat: "Cloud Native", stack: "Python + Kubernetes API", prompt: "Build a Kubernetes Network Policy manager and tester. Define policies: service A can talk to service B on port 8080 (deny all else). Policy generator: given a service dependency graph, generate minimal NetworkPolicy YAML. Policy tester: deploy test pods, verify allowed paths work and denied paths are blocked (curl tests). Drift detector: compare actual running policies vs desired state, alert on gaps. Show: payment service should only accept traffic from checkout service, not from analytics service. Generate policy, apply, verify. Implement: policy visualization (graph of allowed connections), periodic compliance check." },
 
  { id: 340, title: "Progressive Web App Offline Sync", cat: "Distributed Systems", stack: "Python + FastAPI + JavaScript (Service Worker)", prompt: "Build an offline-first app with conflict resolution sync. Client: stores data in IndexedDB, works offline. Sync protocol: (1) On reconnect, send all local changes with vector timestamps, (2) Server merges changes (last-write-wins or semantic merge), (3) Server sends back missed server-side changes, (4) Client applies remote changes. Conflict resolution: user edits todo offline, another device also edits → both are shown to user for manual resolution. Show: disconnect network, create/edit/delete items, reconnect → changes sync bidirectionally. Implement: sync queue, partial sync (only unsync'd items), sync conflict UI." },
 
  { id: 341, title: "Database Audit Logging (Row-Level)", cat: "Database", stack: "Python + PostgreSQL + SQLAlchemy", prompt: "Build comprehensive row-level audit logging using PostgreSQL triggers. Track: every INSERT/UPDATE/DELETE on audited tables → write to audit log (old_data JSONB, new_data JSONB, changed_by, changed_at, operation, table_name, row_id). Features: (1) Automatic via trigger (no application code needed), (2) Diff view (show only changed columns), (3) Point-in-time reconstruction (rebuild row state at any timestamp by replaying audit log), (4) Who changed what (set current user context in PostgreSQL), (5) Retention policy. Show: compliance report for a specific record (all changes in last 90 days), reconstruct record state 30 days ago." },
 
  { id: 342, title: "Async Task Scheduling with Cron Expressions", cat: "Infrastructure", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a cron expression parser and distributed task scheduler from scratch. Parser: implement full cron expression parsing (minute, hour, day, month, weekday, including */n, ranges, lists, @yearly shortcuts). Scheduler: calculate next N execution times for any expression, use a sorted priority queue. Distributed: multiple scheduler instances, elect leader with DB advisory lock, only leader dispatches tasks. Features: timezone support, task history with output capture, missed execution detection + backfill, visual next-run preview. Show: parse '*/5 9-17 * * MON-FRI' → show next 10 executions. Scheduler survives restart, no missed or duplicate executions." },
 
  { id: 343, title: "HTTP Cache Control Deep Dive", cat: "Performance", stack: "Python + FastAPI", prompt: "Build an HTTP caching layer implementing the complete Cache-Control specification. Headers: Cache-Control, ETag, Last-Modified, Vary, Age, Expires. Behaviors: max-age (fresh response), stale-while-revalidate (serve stale, revalidate async), no-cache (always revalidate), private (browser only, not CDN), s-maxage (CDN-specific max-age). Implement: ETags via content hash, conditional GET (304 Not Modified saves bandwidth), cache busting for deploys, cache key normalization. Show: API endpoint with proper caching → browser makes 1 origin request per session, subsequent serve from cache. Measure: origin load reduction, bandwidth savings." },
 
  { id: 344, title: "Content Moderation Pipeline", cat: "ML Systems", stack: "Python + FastAPI + Celery", prompt: "Build a multi-stage content moderation pipeline. Stages: (1) Fast rule-based filter (block known bad words, regex patterns — <1ms), (2) ML classifier (text toxicity scoring using pre-trained model — 50ms), (3) Human review queue (edge cases near decision boundary — async). Features: audit trail (every moderation decision logged), appeals workflow (user challenges decision), calibration (measure classifier accuracy vs human decisions), feedback loop (human decisions retrain model monthly). Show: post submitted → instant pre-filter → async ML scoring → result in <100ms for easy cases, queue for hard cases. Measure: false positive/negative rates per stage." },
 
  { id: 345, title: "Multi-Region Failover", cat: "Infrastructure", stack: "Python + FastAPI + PostgreSQL", prompt: "Build an automated multi-region failover system. Setup: primary region (US-East), standby region (US-West). Replication: async PostgreSQL streaming replication to standby. Health monitoring: synthetic checks from neutral 3rd region. Failover trigger: primary unhealthy for 60s → automated failover. Failover steps: (1) Promote standby to primary, (2) Update DNS (GeoDNS TTL 60s), (3) Redirect application connections, (4) Alert on-call, (5) Estimate data loss (replication lag at failover time). Show: simulate primary region failure, measure RTO (Recovery Time Objective) and RPO (Recovery Point Objective). Implement: manual failover for planned maintenance, failback procedure." },
 
  { id: 346, title: "Request Hedging for Latency", cat: "Performance", stack: "Python + FastAPI + asyncio", prompt: "Implement request hedging to reduce tail latency. Problem: p50=20ms but p99=200ms. Solution: send duplicate request to 2nd backend after 50ms (p95 threshold), use whichever responds first, cancel the other. Trade-off: increases backend load by ~5% (only late requests hedge), reduces p99 by 50%. Implement: async hedging (send first request, after timeout send duplicate, await first response), configurable hedge delay (percentile of recent latencies), selective hedging (only hedge for premium users). Show: p99 before hedging: 200ms, after: 80ms. Backend load increase: 4.7%. Explain: Google Bigtable uses this, called tail latency reduction." },
 
  { id: 347, title: "Distributed Tracing: Database Queries", cat: "Observability", stack: "Python + SQLAlchemy + OpenTelemetry", prompt: "Instrument all database queries as OpenTelemetry spans automatically. SQLAlchemy event hooks: before_cursor_execute + after_cursor_execute → create span with SQL statement, table name, rows affected, query duration. Slow query detection: if span duration > 100ms → add EXPLAIN ANALYZE output as span attribute. N+1 detection: count same-table queries per trace, alert if > 10. Query sampling: only record full SQL for < 1% of queries (PII risk, volume). Show: trace that includes 15 DB spans, identifies the slowest JOIN, shows N+1 problem. Add: Redis + HTTP client auto-instrumentation." },
 
  { id: 348, title: "Progressive Rollout Feature System", cat: "Infrastructure", stack: "Python + FastAPI + Redis + PostgreSQL", prompt: "Build a progressive rollout system more sophisticated than a simple feature flag. Rollout types: (1) Percentage rollout (deterministic: same user always in same bucket), (2) Ring deployment (internal → beta → 5% → 25% → 100%), (3) Geographic rollout (AU first, then EU, then US), (4) Account-tier rollout (enterprise → pro → free), (5) Dependency-aware (only enable if dependent feature is also enabled). Guardrails: auto-rollback if error rate increases > 2x, latency p99 > 200ms. Show: gradual rollout from 0% to 100% over 7 days, automatic pause when guardrail triggers. Implement: rollout history + audit log." },
 
  { id: 349, title: "Self-Documenting System Architecture", cat: "Observability", stack: "Python + FastAPI + Neo4j or NetworkX", prompt: "Build a system that auto-documents its own architecture. Agents: (1) Service discovery (Kubernetes API → list all services), (2) Dependency mapping (analyze network traffic or trace spans → infer service dependencies), (3) Database mapping (which service connects to which DB), (4) API contract discovery (scrape /openapi.json from each service). Assemble: live architecture diagram (graph DB), highlight critical paths, flag undocumented dependencies (traffic without contract), outdated docs (service exists but no descriptor). Show: generate architecture diagram from live system with zero manual input. Detect: service talking to DB it shouldn't." },
 
  { id: 350, title: "Chaos Mesh: Automated Game Days", cat: "Testing & Reliability", stack: "Python + Kubernetes + Chaos Mesh concepts", prompt: "Build an automated Game Day runner that validates system resilience. Experiments catalog: (1) Kill random pod (verify service continues), (2) Network partition between services (verify circuit breaker), (3) DB failover (verify application recovers), (4) CPU saturation (verify autoscaling), (5) Memory leak simulation (verify OOM recovery). Runner: execute experiments sequentially, verify hypothesis (system meets SLO during/after), record results. Safety: blast radius limits (max 20% of pods), automatic abort if error rate > 5x normal. Generate: resilience report per experiment. Show: system passes 8 of 10 experiments, 2 failures identify real gaps to fix." },
 
  // ─────────────────────────────────────────────
  // FINAL 50: Advanced & Emerging Topics
  // ─────────────────────────────────────────────
  { id: 351, title: "eBPF-Based Observability", cat: "Observability", stack: "Python + BCC / bpftrace", prompt: "Use eBPF for low-overhead production observability. Programs: (1) HTTP tracing: intercept kernel send/recv syscalls, parse HTTP headers, record latency per URL without app changes, (2) DB query tracer: hook into PostgreSQL process, capture query text + duration, (3) Network packet counter: per-pod network stats, (4) Lock contention tracer: detect which kernel locks are hot. Show: add observability to a black-box service (no source code access) using eBPF. Measure: overhead < 1% vs 10% for sidecar proxy. Explain: why eBPF is the future of observability tooling (Pixie, Cilium, Falco all use it)." },
 
  { id: 352, title: "WASM Runtime Integration", cat: "Compiler & Runtimes", stack: "Python + Wasmtime or wasmer-py", prompt: "Integrate WebAssembly (WASM) as a plugin/extension runtime. Use case: user-defined business rules (plugins) run in sandboxed WASM. Plugin lifecycle: compile Rust/C code to WASM, load at runtime, execute with memory isolation. Implement: host functions (WASM can call Python functions you expose), memory limits, execution timeout, hot-reload plugins without server restart. Show: plugin that transforms order data, runs 100x sandboxed without crashing host. Compare: WASM sandbox vs Python exec() (unsafe) vs subprocess (slow). This is how Envoy filters, Fastly edge compute, and Shopify Scripts work." },
 
  { id: 353, title: "OpenFeature Standard Flags", cat: "Infrastructure", stack: "Python + OpenFeature SDK + Flagd", prompt: "Implement feature flags using the OpenFeature standard (vendor-neutral spec). Provider: implement the OpenFeature provider interface backed by your own flag store (Redis/file). Hooks: before (log flag evaluation), after (track flag exposure for analytics), error (fallback to default). Context: enrich evaluation context with user tier, region, A/B bucket. Show: swap flag provider from custom → LaunchDarkly → Flagd without changing application code (just swap the provider). Implement: flag dependency tracking, stale flag detection (flag never evaluated → safe to remove). Explain: why OpenFeature matters for avoiding vendor lock-in." },
 
  { id: 354, title: "CQRS Read Model with Elasticsearch", cat: "Distributed Systems", stack: "Python + PostgreSQL + Elasticsearch + Kafka", prompt: "Build a CQRS system where the read model is Elasticsearch (optimized for search). Write side: PostgreSQL (source of truth, relational). Events: published to Kafka on every write. Projection: Kafka consumer reads events, updates Elasticsearch index (denormalized, search-optimized structure). Queries: all reads go to Elasticsearch (faceted search, full-text, geo-queries impossible in PostgreSQL efficiently). Show: create product → appears in Elasticsearch search within 500ms. Handle: projection lag (read model slightly behind write model), rebuild projection (reindex from PostgreSQL). Measure: query latency PostgreSQL vs Elasticsearch for complex search." },
 
  { id: 355, title: "Distributed Scheduler with Anti-Affinity", cat: "Infrastructure", stack: "Python + FastAPI + Redis", prompt: "Build a task scheduler with placement constraints (simplified Kubernetes scheduler). Nodes: 5 worker nodes with different resources (CPU, memory, GPU, region). Tasks: resource requests (needs 2 CPU, 4GB RAM) + constraints (anti-affinity: don't place on same node as task X, region preference: EU only). Scheduling algorithms: (1) Bin packing (maximize resource utilization), (2) Spreading (minimize blast radius). Implement: resource tracking, constraint satisfaction, preemption (evict low-priority task to fit high-priority). Show: 20 tasks scheduled optimally across 5 nodes, anti-affinity prevents co-location of replicas." },
 
  { id: 356, title: "Protocol Buffers + FlatBuffers Comparison", cat: "API Design", stack: "Python + protobuf + flatbuffers", prompt: "Compare serialization formats in depth: JSON, Protobuf, FlatBuffers, MessagePack, Apache Arrow. For each: serialization speed, deserialization speed, wire size, zero-copy read (FlatBuffers/Arrow support random field access without full parse), schema evolution support, language support. Benchmark with 1M records: a complex nested struct (order with line items). Show: FlatBuffers 10x faster deserialization than Protobuf (zero-copy), Protobuf 5x smaller than JSON, Arrow ideal for columnar analytics. Implement schema evolution: add field → old reader handles gracefully. Decision matrix: when to use which format." },
 
  { id: 357, title: "Rate Limiting with Leaky Bucket & GCRA", cat: "Performance", stack: "Python + Redis + FastAPI", prompt: "Implement the Generic Cell Rate Algorithm (GCRA) — the most mathematically precise rate limiter. GCRA: tracks theoretical arrival time (TAT) per user. On request: new_tat = max(now, tat) + cost. Allow if new_tat <= now + burst_tolerance. Redis atomic Lua script. Compare all 4 algorithms: fixed window (simple, boundary exploit), sliding window log (accurate, memory-heavy), token bucket (bursty), leaky bucket (smooth), GCRA (precise, memory-efficient). Show: boundary exploit on fixed window, no exploit on GCRA. Measure: Redis memory per algorithm at 1M users. This is what Cloudflare and Stripe use." },
 
  { id: 358, title: "Immutable Infrastructure Pattern", cat: "Infrastructure", stack: "Python + Docker + Packer concepts", prompt: "Implement immutable infrastructure: never modify a running server, always replace. Build pipeline: (1) Bake: build Docker image with all dependencies (Packer-lite), run tests, tag with git SHA, (2) Push: upload to registry, (3) Deploy: replace all instances with new image (blue-green or rolling), (4) Never SSH into production (enforce with config). Implement: image scanning (check for CVEs before deploy), image provenance tracking (which git commit → which image), rollback (deploy previous tagged image in <60s). Show: 5-server deployment, new image rolled out without downtime, rollback triggered by failed health check." },
 
  { id: 359, title: "Database Proxy with Query Rewriting", cat: "Database", stack: "Python + PostgreSQL", prompt: "Build a database proxy that intercepts and rewrites queries for performance and security. Features: (1) Query normalization (strip comments, normalize whitespace for cache key), (2) Query rewriting (rewrite slow queries before they hit DB), (3) Column masking (SELECT * → rewrite to SELECT id, name — hide sensitive columns for certain roles), (4) Read/write splitting (route SELECT to replica, DML to primary), (5) Query blocking (block known-bad patterns like SELECT * without WHERE on large tables). Show: proxy intercepts query, applies column mask, routes to correct backend, returns masked result. Implement: query log with explain plan cache." },
 
  { id: 360, title: "Cloud Cost Attribution System", cat: "Cloud Native", stack: "Python + AWS Cost Explorer API + FastAPI", prompt: "Build a cloud cost attribution and chargeback system. Data: AWS Cost and Usage Reports (or mock equivalents). Attribution: tag-based cost allocation (service, team, environment tags on resources). Algorithms: (1) Direct allocation (tagged resources), (2) Shared resource allocation (split untagged costs by % of tagged costs), (3) Amortization (upfront RI/Savings Plans spread over usage period). Dashboard: cost by team, by service, by environment, day-over-day delta, anomaly detection. Chargeback report: monthly PDF per team with their costs. Show: detect $5k/month untagged resource waste, cost spike alert (team's spend 50% above last week)." },
 
  { id: 361, title: "Service Topology & Dependency Graph", cat: "Observability", stack: "Python + Jaeger API + NetworkX + FastAPI", prompt: "Build a live service dependency graph from distributed traces. Mine: query Jaeger (or trace store) for all unique service-to-service calls in last 24h. Build graph: nodes = services, edges = calls with weight (call count, error rate, p99 latency). Algorithms: (1) Critical path (longest latency chain), (2) Fan-out detection (services called by many others = risky to change), (3) Fan-in detection (services calling many others = potential bottleneck), (4) Orphan detection (service with no calls). Visualize: interactive graph. Alert: new edge appears (service started calling unexpected dependency). Compare: derived vs declared architecture." },
 
  { id: 362, title: "Multi-Model Database Usage", cat: "Database", stack: "Python + ArangoDB or PostgreSQL extensions", prompt: "Explore multi-model databases that support documents, graphs, and key-value in one system. Use case: social network. Documents: user profiles, posts (flexible schema). Graph: friend relationships, follow edges. Key-value: session tokens, rate limit counters. Implement: friend-of-friend query (graph traversal), user feed (document + graph join), rate limiting (key-value). Compare: separate specialized databases (Redis + MongoDB + Neo4j) vs multi-model (ArangoDB / PostgreSQL with JSONB + ltree). Show: friend-of-friend at depth 3 in graph DB vs simulated in relational (recursive CTE). Performance and operational complexity tradeoffs." },
 
  { id: 363, title: "Backoff & Jitter Strategies", cat: "Performance", stack: "Python + asyncio + FastAPI", prompt: "Implement all retry backoff strategies and show why jitter matters. Strategies: (1) No retry (fail fast), (2) Fixed delay (retry every 1s), (3) Exponential backoff (1s, 2s, 4s, 8s...), (4) Exponential with full jitter (random in [0, 2^n]), (5) Exponential with equal jitter (half fixed + half random), (6) Decorrelated jitter (AWS recommended). Simulate: 100 clients all retrying simultaneously (thundering herd). Without jitter: all retry at t=1s, 2s, 4s → correlated spikes overwhelm server. With jitter: distributed retries, server load stable. Show: concurrency graph over time per strategy. Prove: decorrelated jitter has lowest mean retry time and lowest peak load." },
 
  { id: 364, title: "Permissions & Authorization DSL", cat: "Auth & Security", stack: "Python + FastAPI + Casbin or custom", prompt: "Build a flexible authorization system with a policy DSL (Domain-Specific Language). DSL: policy file with rules: 'allow user:alice action:read resource:document:123', 'allow role:admin action:* resource:*'. Implement: (1) RBAC (role-based), (2) ABAC (attribute-based: user.department == resource.department), (3) ReBAC (relationship-based: Google Zanzibar style — is user a member of group that has access?). Build: policy evaluator, policy editor with validation, audit log (show which rule allowed/denied a request). Show: complex policy (Alice can read documents in her department, or documents she created). Benchmark: policy evaluation < 1ms." },
 
  { id: 365, title: "Stream-Table Join (Kafka + DB)", cat: "Data Engineering", stack: "Python + Kafka + PostgreSQL", prompt: "Implement a stream-table join: enrich streaming events with static reference data. Stream: order events (user_id, product_id, amount). Table: user dimension (user_id, country, tier). Enrichment: for each order event, look up user country and tier, emit enriched event. Strategies: (1) DB lookup per event (simple, slow), (2) Local cache of table (fast, may be stale), (3) KV store with CDC-based updates (fast + fresh). Implement: cache warm-up, cache invalidation when user table changes, cache TTL fallback. Benchmark: 10k events/sec enrichment latency: DB lookup (50ms avg) vs cache (0.1ms avg). Show cache hit rate over time." },
 
  { id: 366, title: "Distributed Secrets Rotation", cat: "Auth & Security", stack: "Python + FastAPI + HashiCorp Vault concepts", prompt: "Build zero-downtime secret rotation for all secret types. Rotation strategies: (1) DB passwords: create new user, update app, delete old user (dual-user strategy), (2) API keys: generate new key, update dependent services via Vault notify, revoke old key after grace period, (3) TLS certificates: ACME protocol (Let's Encrypt-lite), auto-renew 30 days before expiry, (4) Signing keys: key versioning + overlap period. Implement: rotation scheduler, rotation status API, manual rotation trigger, rollback (if new secret fails validation, restore old). Show: DB password rotated while app is serving traffic, no connection errors." },
 
  { id: 367, title: "Service Level Indicators (SLI) Framework", cat: "Observability", stack: "Python + Prometheus + FastAPI", prompt: "Build a comprehensive SLI measurement framework. SLI types: (1) Availability SLI: good_requests / total_requests (exclude health checks, synthetic tests), (2) Latency SLI: fraction of requests < 200ms threshold, (3) Freshness SLI: fraction of reads returning data < 60s old, (4) Durability SLI: fraction of stored data retrievable. Implement: SLI calculator (reads from Prometheus), multi-window compliance (last 1h, 24h, 7d, 30d), SLI configuration as code (YAML: which metrics map to which SLI). Show: availability SLI drops from 99.95% to 99.85% during incident, error budget burn rate spikes. Auto-generate SLO compliance report." },
 
  { id: 368, title: "Hybrid Cloud Connectivity", cat: "Cloud Native", stack: "Python + FastAPI + WireGuard concepts", prompt: "Build a hybrid cloud connectivity layer connecting on-premises systems with cloud services. Components: (1) VPN tunnel (WireGuard-lite: key exchange, encrypted tunnel, peer management), (2) Service bridge (expose on-prem service to cloud + vice versa, with name resolution), (3) Traffic routing (which requests go to cloud vs on-prem based on policy), (4) Health failover (if cloud service unhealthy, route to on-prem fallback). Show: cloud service calls on-prem DB through encrypted tunnel, latency vs public internet, failover when cloud returns 500. Implement: tunnel monitoring, automatic reconnect, bandwidth metering." },
 
  { id: 369, title: "Database Multi-Master Replication", cat: "Database", stack: "Python + PostgreSQL + custom replication", prompt: "Implement multi-master (active-active) replication to understand its challenges. Setup: 2 PostgreSQL nodes, both accept writes. Replication: bidirectional row-based replication (changes on A flow to B, and vice versa). Conflict detection: same row modified on both masters before sync → conflict. Resolution strategies: (1) Last-write-wins (timestamp), (2) Highest-version wins, (3) Application-defined merge, (4) Custom conflict tables for manual resolution. Show: concurrent write conflict, each resolution strategy produces different result, explain which is appropriate when. This shows why multi-master is hard and why CockroachDB/Spanner use consensus instead." },
 
  { id: 370, title: "Traffic Shadowing & Dark Launch", cat: "Infrastructure", stack: "Python + FastAPI + asyncio", prompt: "Build traffic shadowing to test new code against production traffic without risk. Shadowing proxy: receives real request, forwards to production backend (synchronously) + forwards copy to shadow backend (async, fire-and-forget). Response comparison: log diff between production and shadow responses (body, latency, status code). Analysis: what % of requests have different responses? Use cases: (1) Database migration validation (is new schema producing same results?), (2) Rewrite validation (is refactored code equivalent?), (3) Performance comparison. Show: shadow new FastAPI rewrite against old Flask app, compare responses, find 3 behavioral differences." },
 
  { id: 371, title: "Observability for Microservices Cold Start", cat: "Observability", stack: "Python + FastAPI + Kubernetes", prompt: "Instrument and optimize microservice cold start time. Measure: time from pod scheduled → first successful request. Phases: container pull (image size), container start (init time), application start (framework init, DB connection pool warm), readiness check. Instrument each phase with OpenTelemetry spans. Optimizations: (1) Slim base image (reduce pull time), (2) Lazy initialization (don't connect to DB until first request), (3) Pre-warmed connection pool, (4) Readiness tuning (don't mark ready until pool warmed). Show: cold start before: 30s, after optimizations: 5s. Show flame chart of startup phases." },
 
  { id: 372, title: "Request Context Propagation (Full Stack)", cat: "Observability", stack: "Python + FastAPI + React / Vanilla JS", prompt: "Implement end-to-end request context propagation from browser to database. Browser: generate request_id on page load, inject into all API calls as header. API Gateway: extract request_id, add to logging context, propagate to downstream services. Each service: log with request_id, propagate to DB (SET LOCAL request.id = '...'), DB: pg_audit logs include request_id (via SET LOCAL). Show: user reports bug at time T → find all logs (browser console, API, service A, service B, DB query) linked by single request_id. Implement: browser DevTools extension that shows request_id per API call." },
 
  { id: 373, title: "Predictive Autoscaling", cat: "Infrastructure", stack: "Python + Prometheus + Kubernetes API", prompt: "Build a predictive autoscaler that scales before load arrives (not reactive). Methods: (1) Time-series forecasting: ARIMA on past week's traffic, predict next hour's load, scale proactively, (2) Event-based pre-scaling: scale up 15 min before scheduled batch jobs or marketing campaigns, (3) Anomaly-triggered scale: unusual traffic pattern → scale up as precaution. Implement: forecast model training (weekly data), confidence intervals (scale to upper bound of forecast), cost-aware scaling (don't scale if predicted savings < scale-up cost). Show: Monday morning traffic spike predicted → pods ready before spike. Compare: reactive (p99 spikes) vs predictive (smooth)." },
 
  { id: 374, title: "API Idempotency at Scale", cat: "API Design", stack: "Python + FastAPI + Redis + PostgreSQL", prompt: "Build enterprise-grade API idempotency handling all edge cases. Store: idempotency key → (request hash, response, status, created_at). Edge cases: (1) In-flight deduplication (concurrent requests with same key — only one proceeds, others wait and return same result), (2) Partial failure (request in PROCESSING state for 5 min → assume failed, allow retry), (3) Request body mismatch (same key, different body → 422 Unprocessable), (4) Key expiry and cleanup, (5) Cross-region idempotency (key stored in replicated Redis Cluster). Show: payment flow with network timeout → retry with same key → idempotent, charged exactly once. Benchmark: overhead < 2ms." },
 
  { id: 375, title: "Consistent Snapshot Isolation", cat: "Database", stack: "Python + PostgreSQL", prompt: "Deep dive into PostgreSQL's snapshot isolation and how it prevents read anomalies. Demonstrate all isolation levels: READ UNCOMMITTED (dirty reads), READ COMMITTED (non-repeatable reads), REPEATABLE READ (phantom reads), SERIALIZABLE (full isolation). For each: write a test that demonstrates the anomaly at lower levels, show it's prevented at higher levels. Implement: serializable snapshot isolation (SSI) — optimistic concurrency, detect read-write conflicts, abort conflicting transactions. Show: bank transfer scenario — serializable isolation prevents phantom that allows overdraft. Measure: throughput vs contention per isolation level." },
 
  { id: 376, title: "WebSocket Scalability (Multi-Node)", cat: "Messaging", stack: "Python + FastAPI + Redis + WebSocket", prompt: "Build a horizontally scalable WebSocket server where any node can send to any connected client. Problem: client connects to Node A, but message sent to Node B — how to deliver? Solution: Redis Pub/Sub fan-out. Message to user → publish to Redis channel user:{id} → all nodes subscribed → node with connected client delivers. Implement: (1) Connection registry (which node has which client), (2) Sticky sessions at load balancer for connection efficiency, (3) Heartbeat + reconnect (detect dead connections), (4) Message ordering (sequence numbers), (5) Offline queuing (store messages while client disconnected). Show: 3 nodes, client on node 1, message sent via node 3 → delivered." },
 
  { id: 377, title: "Infrastructure Drift Detection", cat: "Infrastructure", stack: "Python + Terraform + AWS/GCP API or Localstack", prompt: "Build a system that detects when live infrastructure drifts from Terraform state. Drift types: (1) Manual change (someone edited security group in console), (2) External automation (cloud provider updated instance type), (3) Terraform bug (state file shows different from actual). Detection: query cloud APIs, compare with Terraform state file, report diff. Remediation options: re-apply Terraform (bring back to desired state), update state file (accept drift as new desired state), alert for manual review. Schedule: run drift detection every 15 minutes, alert on any drift. Show: manually change AWS security group → drift detector finds it within 15 minutes." },
 
  { id: 378, title: "Cross-Service Data Consistency Patterns", cat: "Distributed Systems", stack: "Python + FastAPI + Kafka + PostgreSQL", prompt: "Implement 4 patterns for maintaining data consistency across microservices. (1) Saga (choreography): events trigger compensating transactions on failure. (2) Saga (orchestration): central coordinator manages flow and compensation. (3) Transactional outbox: atomically write to DB + outbox, poller publishes. (4) Change Data Capture: DB triggers feed downstream services. For each pattern: implement the happy path + compensation path + failure scenarios. Show: order service creates order → payment service charges → inventory reduces. Payment fails → saga compensates → order cancelled. Compare: coupling, complexity, consistency guarantees." },
 
  { id: 379, title: "Memory Leak Detection in Production", cat: "Observability", stack: "Python + FastAPI + tracemalloc + Prometheus", prompt: "Build a memory leak detector for Python services. Tools: (1) tracemalloc: snapshot heap, compare snapshots over time, find top growing allocations. (2) gc module: track object counts by type, find unexpected growth. (3) Memory profiling endpoint: /debug/memory returns top 20 allocation sites. (4) Prometheus metric: process_resident_memory_bytes, alert if growing > 10MB/hour. Implement: periodic snapshot comparison, automatic heap dump on OOM-near condition, leak detection report (file:line that's growing). Show: inject a leak (store objects in a global dict), detector finds it within 60 seconds, report points to the exact line." },
 
  { id: 380, title: "GDPR Compliance Toolkit", cat: "Auth & Security", stack: "Python + FastAPI + PostgreSQL", prompt: "Build a GDPR compliance toolkit. Features: (1) Right to access: export all personal data for a user (traverse all tables with user_id FK), (2) Right to erasure: anonymize user data (replace with pseudonymous tokens, not delete — preserve referential integrity), (3) Data inventory: catalog which tables hold PII (tag columns), (4) Consent management: store granular consents with timestamp/version, check consent before processing, (5) Data retention enforcement: auto-delete data past retention period. Show: user requests deletion → all PII replaced with anon token → audit log proves erasure. Consent withdrawn → processing halted. Compliance report: data inventory + retention status." },
 
  { id: 381, title: "Time-Series Forecasting for Capacity Planning", cat: "ML Systems", stack: "Python + Prophet + FastAPI", prompt: "Build a capacity planning forecasting system using time-series analysis. Data: historical infrastructure metrics (CPU, memory, storage, request rate). Models: (1) Linear trend + seasonal decomposition (simple), (2) Facebook Prophet (handles seasonality, holidays, changepoints), (3) Holt-Winters exponential smoothing. Forecast: predict resource usage for next 30/60/90 days with confidence intervals. Alerts: predicted storage full in 14 days → alert with 2-week runway. Output: capacity plan report (when to buy more resources). Show: predict traffic growth, identify when current capacity will be exceeded, quantify confidence. Compare model accuracy on holdout data." },
 
  { id: 382, title: "Dynamic Configuration with Validation", cat: "Infrastructure", stack: "Python + FastAPI + etcd + Pydantic", prompt: "Build a dynamic configuration system with schema validation and safe deployment. Config schema: Pydantic model (typed, validated, defaults). Push config update: validate schema, check for breaking changes (new required field without default = breaking), run config tests (start service with new config in dry-run mode), staged rollout (apply to 10% of instances, verify, then 100%). Rollback: revert to previous config version in < 5s. Config diff: show exactly what changed between versions. Audit log: who changed what, when. Show: invalid config (wrong type) rejected at write time, breaking change blocked, config applied live without restart." },
 
  { id: 383, title: "SQL Query Builder (Type-Safe)", cat: "API Design", stack: "Python + SQLAlchemy core", prompt: "Build a type-safe SQL query builder from scratch. DSL: Python objects compose SQL (Table('users').select('id', 'email').where(age > 18).join(Table('orders'), 'user_id').orderBy('created_at', 'DESC').limit(10)). Features: (1) Parameterized queries (prevent SQL injection by default), (2) Schema inference (auto-complete knows which columns exist), (3) Query plan preview (run EXPLAIN before execute), (4) Dialect support (PostgreSQL vs SQLite syntax differences). Show: build complex query with join + subquery using only Python objects (no string SQL). Compare: ORM (hides SQL), query builder (SQL control + type safety), raw SQL (most flexible, least safe)." },
 
  { id: 384, title: "Kubernetes RBAC Policy Generator", cat: "Cloud Native", stack: "Python + Kubernetes API", prompt: "Build a Kubernetes RBAC policy generator and auditor. Principle of least privilege: analyze what a service actually does (which API groups/resources/verbs appear in audit logs) → generate minimal Role/ClusterRole. Audit: scan existing RBAC, find over-privileged service accounts (has wildcard permissions but only needs get/list), find unused permissions (never exercised in 30 days). Policy generator: input service name → output minimal YAML. Show: existing service account with cluster-admin (terrible) → audit finds it only needs read pods → generate scoped Role. Simulate: privilege escalation attack using over-privileged SA, then show it's blocked after least-privilege policy." },
 
  { id: 385, title: "API Observability: Business Metrics from HTTP", cat: "Observability", stack: "Python + FastAPI + Prometheus", prompt: "Extract business metrics automatically from API traffic without code changes. Pattern: HTTP middleware that maps URL patterns to business events. Rules: POST /orders → order_created++, PATCH /orders/{id}/cancel → order_cancelled++, POST /payments → revenue_tracked += body.amount. Implement: URL pattern matching, request/response body sampling for amount extraction, derived metrics (conversion rate = orders / sessions), funnel tracking (view → add_to_cart → checkout → purchase). Dashboard: business KPIs from pure HTTP traffic. Show: zero code change in handlers, business dashboard appears. Alert: conversion rate drops 20% → investigate HTTP errors in checkout flow." },
 
  { id: 386, title: "Geospatial Indexing & Queries", cat: "Database", stack: "Python + PostgreSQL + PostGIS", prompt: "Build a geospatial service using PostGIS. Data: 1M points of interest worldwide. Indexes: GIST index on geography column. Queries: (1) Nearest N points to a location (k-NN), (2) Points within a polygon (geofencing), (3) Distance between two points (great circle), (4) Clustering nearby points (ST_ClusterDBSCAN). Geohash: encode lat/lon as string prefix for shard key. Show: find 5 nearest restaurants to current location < 5ms. Geofence: alert when user enters a polygon. Cluster 1M points into 1000 regional clusters. Compare: PostGIS vs H3 (Uber's hexagonal grid) for different use cases." },
 
  { id: 387, title: "Event Schema Migration", cat: "Messaging", stack: "Python + Kafka + Avro + Schema Registry", prompt: "Manage Kafka event schema evolution without breaking producers or consumers. Compatibility types: BACKWARD (new schema reads old events), FORWARD (old schema reads new events), FULL (both). Implement: schema migration for all compatibility levels. Scenarios: (1) Add optional field (backward compatible), (2) Remove field (forward compatible), (3) Rename field (breaking — requires two-phase: add new + keep old), (4) Change type (breaking). Schema migration tool: generate migration plan (which services need updates first), staged migration (producer migrates → consumers migrate → old schema deprecated). Show: live migration with 0 downtime." },
 
  { id: 388, title: "SaaS Tenant Isolation Testing", cat: "Testing & Reliability", stack: "Python + FastAPI + PostgreSQL + pytest", prompt: "Build a test suite specifically for multi-tenant SaaS data isolation. Tests: (1) Cross-tenant data leakage (tenant A cannot read tenant B's data), (2) Tenant impersonation (JWT with tenant_id tampered), (3) Shared resource exhaustion (tenant A fills connection pool, tenant B still gets connections), (4) Noisy neighbor (tenant A's heavy query doesn't block tenant B), (5) RLS bypass attempts (raw SQL injection bypassing row-level security). Automate: run isolation tests on every PR. Show: detect a real data leakage bug (missing tenant_id filter) that passes normal tests but fails isolation tests." },
 
  { id: 389, title: "GitOps Secret Management", cat: "Auth & Security", stack: "Python + SealedSecrets / SOPS concepts", prompt: "Implement GitOps-compatible secret management (store secrets in git safely). Approaches: (1) SealedSecrets: encrypt with cluster public key, only cluster can decrypt, store ciphertext in git, (2) SOPS: encrypt with KMS key, store encrypted YAML in git, decrypt at deploy time, (3) External Secrets Operator: reference to Vault/SSM in git (no secret values in git). Implement a simplified version of each. Show: commit encrypted secret to git → cluster decrypts and creates Kubernetes Secret. Key rotation: re-encrypt all secrets with new key. Audit: detect unencrypted secrets accidentally committed (pre-commit hook). Compare: which approach for which team size/maturity." },
 
  { id: 390, title: "Distributed Locking Patterns Compared", cat: "Distributed Systems", stack: "Python + Redis + PostgreSQL + etcd", prompt: "Implement distributed locking with 4 different backends and compare. (1) Redis single-node: SET NX PX (simple, not safe in failover), (2) Redlock: acquire from 3 of 5 Redis nodes (quorum), (3) PostgreSQL advisory locks: SELECT pg_try_advisory_lock(key) (ACID, but requires DB), (4) etcd lease: create lease, lock is held until lease expires (safe, distributed). For each: measure latency, implement lock renewal, simulate lock holder crash, verify lock released. Show: each backend's behavior during network partition. Recommendation matrix: when to use which approach." },
 
  { id: 391, title: "ML Model Versioning & Rollback", cat: "ML Systems", stack: "Python + MLflow + FastAPI", prompt: "Build an ML model registry with safe deployment and rollback. Features: (1) Model versioning (every trained model gets version + metrics snapshot), (2) Champion/challenger pattern (new model serves 10% traffic alongside current champion), (3) Automated promotion (if challenger wins on business metric after 7 days → promote to champion), (4) Instant rollback (champion degraded → one-click rollback to previous version), (5) Model genealogy (which training data + code produced this model). Show: train new model, deploy as challenger, A/B traffic split, compare metrics, auto-promote. Rollback in <60 seconds when model degrades." },
 
  { id: 392, title: "Service Health Score", cat: "Observability", stack: "Python + Prometheus + FastAPI", prompt: "Build a holistic service health scoring system that combines all signals into a single score. Signals: (1) Error rate (40% weight), (2) Latency p99 vs SLO (30% weight), (3) Saturation (CPU/memory vs limit) (15% weight), (4) Dependency health (are upstream services healthy?) (15% weight). Scoring: 0-100, < 60 = degraded, < 30 = critical. History: track score over time (hourly), detect trends (score declining over 24h = early warning). Alert: score drops 20 points in 1h. Dashboard: per-service score grid, drill down to contributing signals. Show: score degrading before full outage — gives 15-minute warning." },
 
  { id: 393, title: "Log Sampling & Cost Control", cat: "Observability", stack: "Python + FastAPI + OpenTelemetry", prompt: "Build an intelligent log sampling system that reduces cost while preserving signal. Strategies: (1) Level sampling (DEBUG: 1%, INFO: 10%, WARN: 100%, ERROR: 100%), (2) Endpoint sampling (health checks: 0.1%, payment: 100%, browse: 5%), (3) Adaptive sampling (if error rate spikes, temporarily increase sampling rate to 100%), (4) Head-based trace sampling with log correlation (sample logs for sampled traces). Implement: sampling decision propagated through context (don't sample some service logs but not others for same request). Show: 100x cost reduction, 99% of errors still captured. Cost calculator: estimated logging cost per strategy at 10M req/day." },
 
  { id: 394, title: "gRPC Load Balancing & Service Discovery", cat: "API Design", stack: "Python + gRPC + etcd", prompt: "Implement gRPC client-side load balancing with dynamic service discovery. Problem: gRPC multiplexes over one TCP connection → LB at L4 doesn't distribute RPCs. Solution: client-side LB (each client knows all backend addresses, distributes RPCs). Implement: (1) Name resolver: watch etcd for backend address changes, (2) Round-robin load balancer: distribute RPCs across all backends, (3) Pick-first (single backend, failover to next on failure), (4) gRPC health checking protocol (backend marks itself unhealthy → removed from LB). Show: add new backend → client starts sending traffic to it within 5s. Remove backend → traffic redistributed, zero errors." },
 
  { id: 395, title: "Database Vacuum & MVCC Bloat", cat: "Database", stack: "Python + PostgreSQL", prompt: "Deeply understand PostgreSQL MVCC bloat and vacuum. Demonstrate: INSERT/UPDATE/DELETE creates dead tuples (old row versions), no reuse until vacuumed, table grows indefinitely without vacuum. Implement: bloat calculator (estimate % dead tuples per table), vacuum monitor (track pg_stat_user_tables: n_dead_tup, last_vacuum). Simulate: high-churn table (10k updates/sec), watch bloat grow, trigger autovacuum, watch it reclaim space. Show: table with 10M rows, 80% dead tuples = 80% wasted space, post-vacuum: compacted. Implement: manual vacuum tuning (autovacuum_vacuum_cost_delay, scale_factor), identify bloat-heavy tables needing attention." },
 
  { id: 396, title: "API Throttling with Fairness", cat: "Performance", stack: "Python + Redis + FastAPI", prompt: "Build an API throttling system that ensures fairness across users. Fairness problem: 1 heavy user can consume entire rate limit budget if no fairness. Solutions: (1) Per-user quotas (simple, prevents hogging), (2) Weighted fair queuing (each user gets fair share of throughput, not just rate), (3) Deficit round-robin (variable-size request fairness), (4) Work-conserving scheduling (unused quota redistributed). Implement: fair scheduler with configurable weights (premium users 3x, free users 1x), show heavy user cannot starve light users. Compare: max-min fairness vs proportional fairness. Show: 10 users competing for 1000 req/s, resource allocation over time." },
 
  { id: 397, title: "OpenTelemetry Semantic Conventions", cat: "Observability", stack: "Python + OpenTelemetry + FastAPI + SQLAlchemy", prompt: "Implement OpenTelemetry semantic conventions for consistent observability. Conventions for: (1) HTTP (http.method, http.status_code, http.route, server.address), (2) Database (db.system, db.statement, db.name, db.operation), (3) Messaging (messaging.system, messaging.destination, messaging.operation), (4) RPC (rpc.system, rpc.service, rpc.method). Implement auto-instrumentation for FastAPI + SQLAlchemy that emits all required attributes. Show: trace from browser → API → DB, all spans follow conventions → Jaeger aggregates by convention attributes. Benefit: switch from Jaeger to Zipkin to Honeycomb, dashboards work without changes." },
 
  { id: 398, title: "Capacity Modeling & Sizing", cat: "Infrastructure", stack: "Python + Prometheus + mathematical modeling", prompt: "Build a capacity model for your services. Input: historical metrics (RPS, CPU, memory, latency per node). Model: (1) Little's Law: average requests in system = arrival rate × average time in system, (2) Amdahl's Law: max speedup with N processors and fraction p parallelizable = 1/(1-p + p/N), (3) USL (Universal Scalability Law): model throughput vs concurrency (linear + contention + coherency terms). Predict: at 2x current load, how many nodes needed? At what load does p99 > SLO? Output: capacity report with charts, sizing recommendations, cost estimate. Validate model against actual load test results." },
 
  { id: 399, title: "Developer Productivity Metrics (DORA)", cat: "Observability", stack: "Python + GitHub API + FastAPI", prompt: "Build a DORA (DevOps Research and Assessment) metrics dashboard. 4 DORA metrics: (1) Deployment Frequency (how often you deploy to production — pull from CI/CD API), (2) Lead Time for Changes (commit → production, pull from GitHub + deploy events), (3) Mean Time to Recovery (incident opened → resolved, pull from PagerDuty/OpsGenie), (4) Change Failure Rate (% deployments causing incident). Implement: GitHub API integration (PR merge times), deploy event tracking, incident correlation. Dashboard: weekly trend, benchmark against DORA elite/high/medium/low thresholds. Alert: MTTR trending up over 3 weeks." },
 
  { id: 400, title: "Full-Stack Observability: From Browser to DB", cat: "Observability", stack: "Python + FastAPI + OpenTelemetry + React + PostgreSQL", prompt: "Build end-to-end observability covering the entire request lifecycle. Browser: (1) OpenTelemetry browser SDK — trace user interactions (click → API call), measure Core Web Vitals (LCP, FID, CLS), (2) Source map integration (map JS errors to original TypeScript). API layer: (3) Distributed trace propagation (traceparent header), (4) Business context (user_id, feature_flag, experiment_id). Database: (5) Query traces (every SQL query as span), (6) Slow query logs correlated to traces. Correlate all: single incident trace goes from 'user clicks buy' → API handler → payment service → DB query → response. Show full latency breakdown per layer." },
  {
    id: 401,
    title: "Feature Flag Analytics Dashboard",
    cat: "Infrastructure",
    stack: "React + Python + FastAPI + Redis + PostgreSQL",
    prompt: "Build a full feature-flag system with built-in analytics. Part 1 — Flag engine: store flags in PostgreSQL (name, type: boolean/percentage/segment, rules). SDK (Python): on flag evaluation write an exposure event {flag_id, user_id, variant, timestamp} to a Redis stream. Part 2 — Analytics ingestion: background worker drains Redis stream into PostgreSQL events table (batches of 500). Part 3 — Stats: for each flag compute exposure count per variant, conversion rate (flag exposed + goal event within 24 h), and a z-test p-value comparing variants. Part 4 — Dashboard (React): bar chart of exposures/day, conversion funnel per variant, confidence-interval visualisation, and a 'suggest rollout' badge when p < 0.05 and treatment wins. Show: start rollout at 10% → watch exposure ratio in dashboard → promote to 100% when significant. Explain: why deterministic hashing (hash(user_id + flag_id) % 100) ensures the same user always gets the same variant."
  },
 
  // ── 402 ── originally id 202 (adjusted — was titled "Feature Flag Analytics" but different scope)
  {
    id: 402,
    title: "Pluggable Storage Layer with Runtime Swap",
    cat: "Database",
    stack: "TypeScript + Node + SQLite + S3 (MinIO)",
    prompt: "Design an abstract storage interface and three concrete backends so the application never knows which store it is talking to. Interface: get(key), put(key, value, ttl?), delete(key), list(prefix). Backend 1 — In-memory: Map<string, {value, expiresAt}>  with TTL sweep every 5 s. Backend 2 — SQLite: single-file DB, WAL mode, prepared statements, per-key TTL via expires_at column + DELETE WHERE expires_at < now(). Backend 3 — S3 (MinIO): object per key, TTL via object-lifecycle rule, list via ListObjectsV2 with prefix. Loader: read STORAGE_BACKEND env var, instantiate correct class at startup, no if/else in app code (factory + interface). Benchmark harness: 10 k sequential writes, 10 k random reads, 1 k deletes — measure ops/s and p99 latency for each backend. Show: swap from memory to SQLite by changing one env var; app code unchanged. Add: TTL correctness test (write key with 1 s TTL, read after 2 s → miss)."
  },
 
  // ── 403 ── originally id 203 (Secure File Upload)
  {
    id: 403,
    title: "Secure File Upload Service",
    cat: "Auth & Security",
    stack: "Node + Express + AWS S3 (MinIO) + ClamAV stub",
    prompt: "Build a production-grade file upload service. Step 1 — Presigned URL flow: client calls POST /uploads/init with {filename, mimeType, sizeBytes}; server validates mime (whitelist: image/*, application/pdf), checks per-user quota (PostgreSQL: used_bytes + sizeBytes <= limit), then calls S3 CreateMultipartUpload and returns presigned part URLs. Client uploads directly to S3. Step 2 — Finalize: client calls POST /uploads/complete with ETag list; server calls CompleteMultipartUpload, writes upload record. Step 3 — Virus scan: enqueue scan job (Redis queue); worker downloads object, runs ClamAV (or a stub that flags files containing the string 'EICAR'), marks object CLEAN or QUARANTINED, moves quarantined files to a separate S3 prefix and notifies user. Step 4 — Signed metadata: attach custom S3 metadata {uploader_id, upload_id, scanned_at} signed with HMAC so it cannot be tampered. Step 5 — Server-side encryption: enable SSE-S3 on bucket, show encryption header in response. Test: upload a clean file, an EICAR file, a file exceeding quota — verify each path."
  },
 
  // ── 404 ── originally id 204 (Predictive Autoscaler)
  {
    id: 404,
    title: "Predictive Autoscaler vs Reactive HPA",
    cat: "Performance",
    stack: "Python + Prometheus + Kubernetes (kind or k3d)",
    prompt: "Build a predictive autoscaler that scales before load arrives and compare it against Kubernetes HPA (reactive). Part 1 — Metric collection: scrape requests_per_second from Prometheus every 15 s, store last 7 days in a ring buffer. Part 2 — Forecast: use statsmodels SARIMAX (hourly seasonality) to predict RPS for the next 30 min; convert to pod count via pod_capacity = max_rps_per_pod (measured from a load test). Part 3 — Scale action: call Kubernetes Deployments scale API if predicted_pods != current_pods (with min/max bounds and a stabilisation window to prevent flapping). Part 4 — HPA baseline: configure standard HPA on CPU 60% target on the same deployment. Part 5 — Synthetic workload: locust ramp 0→500 RPS over 5 min, hold, ramp down. Record p99 latency and error rate for both autoscalers every 10 s. Chart: overlay scale-up timing, pod count, p99 latency — show predictive beats reactive by ≥ 30 s on ramp. Explain: why forecast error (MAPE) and stabilisation windows matter for avoiding thrashing."
  },
 
  // ── 405 ── originally id 205 (Event Sourcing Playground)
  {
    id: 405,
    title: "Event Sourcing Playground with Live UI",
    cat: "Distributed Systems",
    stack: "TypeScript + Node + SQLite + React + WebSocket",
    prompt: "Create an interactive playground that makes event sourcing tangible. Backend: append-only events table (id, stream_id, event_type, data JSONB, version INTEGER, created_at). HTTP endpoints: POST /streams/:id/events (append, with optimistic-concurrency version check), GET /streams/:id/events (full history), GET /streams/:id/state (rebuild by replaying all events through a reducer). Projections: a background worker maintains two read models — (1) account_balances table updated as MoneyDeposited/Withdrawn events arrive, (2) transaction_log table for audit. Snapshot store: after every 50 events, save current state as a snapshot so replay starts from snapshot not event 0. UI (React): left panel shows raw event stream (append-only, new events appear via WebSocket); right panel shows derived state updating in real time; controls let you append events (Deposited, Withdrawn, AccountClosed). Show: append AccountClosed event → UI shows account as closed instantly → replay from scratch produces same state. Highlight: snapshot reduces replay from 200 events to 5. Explain: why event sourcing enables time-travel debugging and audit trails that UPDATE/DELETE-based DBs cannot."
  },
 
  // ── 406 ── originally id 206 (Message Queue Explorer)
  {
    id: 406,
    title: "Message Queue Explorer UI",
    cat: "Messaging",
    stack: "React + Python + FastAPI + RabbitMQ",
    prompt: "Build a web UI for inspecting and managing RabbitMQ queues safely. Backend (FastAPI): wrap the RabbitMQ Management HTTP API and add safety guards. Endpoints: GET /queues → list all queues with depth/consumer count/message rates; GET /queues/:name/messages?count=20 → peek without ACKing (use GET /api/queues/vhost/name/get with requeue=true); POST /queues/:name/messages/:id/retry → move one message from DLQ back to its origin queue; DELETE /queues/:name/messages → purge with a confirmation token (server generates token, client must echo it back — prevents accidental purge); POST /queues/:name/replay → replay last N messages at a configurable rate (msgs/sec) to avoid overwhelming consumers. UI (React): table of queues with sparkline of depth over last 5 min, message inspector (renders JSON with syntax highlight), DLQ tab showing error reason from x-death header, action buttons with confirmation dialogs. Danger-proofing: purge and replay require typing the queue name to confirm. Show: publish 100 test messages, let 5 go to DLQ, peek them, fix consumer code, retry all 5 from UI."
  },
 
  // ── 407 ── originally id 207 (Service Dependency Map)
  {
    id: 407,
    title: "Service Dependency Map from Traces",
    cat: "Observability",
    stack: "React + Python + FastAPI + Jaeger API + D3.js",
    prompt: "Auto-discover and visualise the live service call graph by mining distributed traces. Backend: poll Jaeger's HTTP API every 60 s for all traces in the last 1 h window; parse each trace to extract (caller_service, callee_service, endpoint, status_code, duration_ms) tuples; aggregate into an edge table: {from, to, call_count, error_count, p50_ms, p99_ms}. Expose GET /graph → {nodes, edges} and GET /graph/edges/:from/:to/traces → sample of 5 recent traces for that edge. UI (React + D3 force-directed graph): nodes sized by total inbound call count; edges coloured green/yellow/red by error rate; edge thickness by call volume; click an edge → side panel shows p50/p99 latency sparkline (last 24 h) + link to sample traces in Jaeger. Alerts layer: edges whose p99 > SLO threshold shown with pulsing red glow. Architecture drift: compare today's graph with yesterday's snapshot — highlight new edges (purple) and disappeared edges (grey dashed). Show: deploy a new service that calls two existing ones → appears in graph within 60 s with no manual registration."
  },
 
  // ── 408 ── originally id 208 (REST to GraphQL Adapter)
  {
    id: 408,
    title: "REST-to-GraphQL Adapter with Batching",
    cat: "API Design",
    stack: "Node + Apollo Server + DataLoader + Redis",
    prompt: "Build an adapter that wraps multiple REST APIs into one cohesive GraphQL schema without any changes to the underlying REST services. Step 1 — Schema stitching: define GraphQL types (User, Order, Product) mapped to REST endpoints (/users/:id, /orders?userId=, /products/:id). Step 2 — Resolvers with DataLoader: instead of one HTTP call per object (N+1), batch user IDs into a single GET /users?ids=1,2,3; DataLoader caches results per request. Step 3 — Response caching: wrap resolvers with a Redis cache (TTL per type: User 60 s, Product 300 s, Order 10 s); cache key = type + arguments normalised (sorted keys). Step 4 — Error mapping: REST 404 → GraphQL null + NOT_FOUND extension, REST 429 → RATE_LIMITED extension with retryAfter. Step 5 — Schema stitching for related types: Order.user field calls User resolver — demonstrate that with DataLoader this costs 1 HTTP call for 100 orders (not 100 calls). Benchmark: query 50 orders with user details — without DataLoader: 51 HTTP calls, 800 ms; with DataLoader: 2 HTTP calls, 80 ms. Show batching proof with HTTP access log."
  },
 
  // ── 409 ── originally id 209 (Transactional Email)
  {
    id: 409,
    title: "Transactional Email Delivery Service",
    cat: "Messaging",
    stack: "Node + PostgreSQL + Redis + Nodemailer + DKIM",
    prompt: "Build a reliable transactional email service that owns the full delivery lifecycle. Part 1 — Template engine: store templates in PostgreSQL (id, subject_template, html_template, text_template); render with Handlebars; validate all required variables present before enqueue. Part 2 — Enqueue: POST /emails → insert into email_jobs table (status: PENDING, to, template_id, variables, idempotency_key); return job_id. Idempotency: if same key exists and status=SENT, return cached response immediately. Part 3 — Worker (Redis BullMQ): pick up PENDING jobs, render template, send via Nodemailer (SMTP or SES); on success set status=SENT + sent_at; on transient failure (connection timeout) retry with exponential backoff (1 s, 2 s, 4 s, max 5 attempts); on permanent failure (550 bad address) set status=FAILED, no retry. Part 4 — DKIM signing: generate RSA key pair, sign outgoing mail headers, publish public key as DNS TXT record (show the record format). Part 5 — Dashboard: table of recent jobs with status, open/click webhook receiver (update email_events table on SendGrid-style POST), bounce rate per domain, 7-day delivery funnel chart. Show: send 100 emails, inject one transient failure, verify retry succeeds."
  },
 
  // ── 410 ── originally id 210 (Immutable Data Store)
  {
    id: 410,
    title: "Immutable Versioned Data Store with Time-Travel",
    cat: "Database",
    stack: "Go + BoltDB (or bbolt)",
    prompt: "Build a key-value store where every write creates a new immutable version, enabling time-travel reads and point-in-time snapshots. Storage layout: BoltDB bucket per key named after the key; each entry is (version uint64 → serialised value bytes); a separate 'meta' bucket stores (key → latest_version). Operations: Put(key, value) → increment version, write new entry, update meta — all in one BoltDB write transaction (ACID). Get(key, version?) → if version omitted, read latest_version from meta then fetch; if version given, fetch that exact version or return ErrNotFound. List(prefix) → scan meta bucket for keys with matching prefix, return (key, latest_version) pairs. Compaction: a background goroutine runs every hour; for each key, if version_count > max_versions (configurable, default 100), delete oldest N versions (keeping the last max_versions). Snapshot API: GET /snapshot?at_version=42 → return all keys at their version ≤ 42 (consistent point-in-time view). Benchmark: 100 k writes, 100 k reads by latest version, 10 k time-travel reads — report ns/op. Show: write key 'balance' 200 times, time-travel read version 50, prove compaction removes old versions without losing the latest."
  },
 
  // ── 411 ── originally id 211 (Passwordless Login)
  {
    id: 411,
    title: "Passwordless Login: Magic Links + OTP",
    cat: "Auth & Security",
    stack: "React + Node + Express + PostgreSQL + Redis + Nodemailer",
    prompt: "Implement a complete passwordless login system with two delivery modes: email magic link and one-time code (OTP). Magic link flow: POST /auth/magic-link {email} → generate a cryptographically random 32-byte token, store {token_hash: SHA-256(token), user_id, expires_at: now+15min, used: false} in PostgreSQL; send email with link /auth/verify?token=RAW_TOKEN; GET /auth/verify → verify token hash, check not expired and not used, set used=true, issue JWT. OTP flow: POST /auth/otp {email} → generate 6-digit TOTP-style code (HMAC-SHA1 of email + current 5-min window), store attempt count in Redis; POST /auth/otp/verify {email, code} → constant-time compare, increment attempt counter, return 429 after 5 attempts. Rate limiting: POST /auth/* routes limited to 3 requests/min per email (Redis sliding window) — return 429 with Retry-After header. Device binding: on successful login, record {user_id, user_agent, ip_hash, first_seen, last_seen} — if new device detected, send 'new device login' notification email. Link expiry: tokens expire after 15 min or first use — show 'link expired' page with one-click resend. Test all paths: valid link, used link, expired link, wrong OTP, OTP rate limit."
  },
 
  // ── 412 ── originally id 212 (Image Thumbnail Service)
  {
    id: 412,
    title: "Responsive Image Thumbnail Service",
    cat: "Infrastructure",
    stack: "Node + Sharp + S3 (MinIO) + BullMQ + Redis",
    prompt: "Build a server that generates, caches, and serves responsive images at multiple sizes with format negotiation. Upload flow: POST /images → store original in S3 under originals/ prefix, enqueue thumbnail job for sizes [320, 640, 1280, 2560] in Redis via BullMQ. Thumbnail worker: pull job, use Sharp to resize (fit: 'inside', withoutEnlargement), convert to WebP (quality 80) and JPEG fallback, store both under thumbnails/{id}/{width}.webp and thumbnails/{id}/{width}.jpg in S3. Serve endpoint: GET /images/:id?w=640&format=auto → check Accept header; if image/webp supported serve .webp else .jpg; set Cache-Control: public, max-age=31536000, immutable. Pre-generation: on upload, kick off all 4 sizes immediately so first request is never a cache miss. On-demand fallback: if requested size not pre-generated, generate synchronously and cache. Cache headers: ETag based on S3 object version; handle If-None-Match → 304. Metrics: track cache hit ratio per size, generation time histogram. Show: upload a 4 MB JPEG → within 2 s all four WebP variants are available → serve 320 px version → verify Content-Type: image/webp and Cache-Control header in response."
  },
 
  // ── 413 ── originally id 213 (Connection Throttler)
  {
    id: 413,
    title: "WebSocket Connection Throttler (Noisy Neighbor)",
    cat: "Performance",
    stack: "Node + ws library + Redis + FastAPI",
    prompt: "Build a WebSocket server with per-client and per-room rate limiting to prevent noisy neighbours from starving other connections. Architecture: Node WS server; each incoming message carries {client_id, room_id, payload}. Per-client message limit: token bucket in Redis (capacity 50, refill 10/s); if bucket empty → send THROTTLED frame to that client only, drop message — other clients unaffected. Per-room message limit: separate bucket per room (capacity 200, refill 50/s); if room bucket empty → send ROOM_THROTTLED to the sender, all other room members continue receiving. Bandwidth limit: track bytes_sent per client in a 1-s sliding window (Redis sorted set); if > 100 KB/s → throttle by delaying messages (setTimeout based on excess) rather than dropping — prevents retransmission storms. Graceful disconnect: if client exceeds limit 10× in 60 s → server sends POLICY_VIOLATION close frame (code 1008) with reason 'rate limit exceeded', closes socket. Observability: expose /metrics with: throttled_messages_total{type='client'|'room'}, disconnected_clients_total, active_connections. Simulation script: 10 clients, 1 is a spammer (1000 msg/s), verify the other 9 receive their messages unaffected at normal latency."
  },
 
  // ── 414 ── originally id 214 (Data Privacy Auditor)
  {
    id: 414,
    title: "Database PII Column Auditor & Remediation Guide",
    cat: "Auth & Security",
    stack: "Python + SQLAlchemy + PostgreSQL + Rich (CLI)",
    prompt: "Build a CLI tool that scans any PostgreSQL schema for likely PII columns and generates a remediation report. Discovery: connect via SQLAlchemy reflection; iterate all columns; score each by name pattern (email, ssn, phone, address, dob, ip_address, credit_card, passport — configurable regex list) and by value sampling (sample 200 rows; run validators: is_email(), is_phone(), is_ip(), Luhn check for card numbers). Risk scoring: HIGH if name matches AND values validate; MEDIUM if name matches only; LOW if values validate only. Report (Rich table + JSON export): per-table list of risky columns with risk level, sample count, suggested remediation per level: HIGH → AES-256-GCM column encryption (show SQLAlchemy TypeDecorator snippet), MEDIUM → consider pseudonymisation, LOW → verify and document. Also detect: columns storing plaintext passwords (value contains bcrypt/argon2 prefix → already hashed ✓; short random strings → flag as plaintext risk). Retention check: flag tables with timestamp columns but no DELETE policy (infer from triggers/row counts growing monotonically). Output: summary score 0–100, prioritised remediation backlog ordered by risk × row_count. Show on a sample DB with 10 tables, 3 high-risk, 2 medium."
  },
 
  // ── 415 ── originally id 215 (Graph-based Recommendation)
  {
    id: 415,
    title: "Graph-Based Recommendation Engine",
    cat: "Database",
    stack: "Python + NetworkX + Neo4j (or Memgraph) + FastAPI",
    prompt: "Build an item-item recommendation engine using graph algorithms on a product co-purchase graph. Data loading: import 100 k order-line pairs into Neo4j as (:Product)-[:CO_PURCHASED {count}]->(:Product) relationships. Algorithm 1 — Co-purchase similarity: for item X, find all items co-purchased, rank by Jaccard similarity (shared_customers / union_customers). Algorithm 2 — PageRank: run personalised PageRank seeded on the user's recently viewed items; return top-10 by score. Algorithm 3 — Community detection: run Louvain modularity (via NetworkX after exporting subgraph) to find product categories; within a community recommend by PageRank. API: GET /recommend/{product_id}?algo=jaccard|pagerank — return top-10 with scores. GET /user/{user_id}/recommend — personalised feed using recently viewed as seed set. Neo4j Cypher: show the MATCH pattern for co-purchase traversal + CALL gds.pageRank. Offline variant: export adjacency to NetworkX for algorithms not in GDS. Benchmark: Cypher traversal vs NetworkX on 100 k nodes — show when to use each. Show: recommend items for a product with 500 co-purchases, verify result diversity (not all same category)."
  },
 
  // ── 416 ── originally id 216 (Realtime Collaborative Editor)
  {
    id: 416,
    title: "Real-Time Collaborative Editor with CRDT (Yjs)",
    cat: "Distributed Systems",
    stack: "TypeScript + Node + Yjs + WebSocket + React",
    prompt: "Build a minimal collaborative rich-text editor backed by Yjs CRDT that handles concurrent edits, presence, and offline merging. Server: Node WebSocket server using y-websocket; maintain a Y.Doc per document ID; broadcast state vectors and update messages between clients; persist doc state to disk (leveldb or SQLite via y-leveldb) every 30 s so server restart doesn't lose data. Client (React): y-codemirror.next binding for the editor; Y.Text for the shared document; show cursor positions of other users (Y.Awareness — {user, color, cursor_position}); display list of active collaborators in sidebar. Conflict-free merging demo: open two browser tabs, disconnect one from network (Chrome DevTools → offline), make edits in both tabs, reconnect — show automatic merge with no data loss and no user prompt needed. Undo/redo: per-client undo stack (Y.UndoManager) — Ctrl+Z undoes only your own changes, not collaborators'. Persistence: on server restart, reload doc from SQLite; reconnecting clients receive missed updates via state-vector sync. Metrics: track document size (bytes), number of connected clients, and conflict merge events per minute. Show: 3 users edit simultaneously for 60 s, disconnect one mid-edit, reconnect — all see identical document."
  },
 
  // ── 417 ── originally id 217 (API Rate Limit Dashboard)
  {
    id: 417,
    title: "API Rate Limit Usage Dashboard",
    cat: "Observability",
    stack: "React + Prometheus + Python + FastAPI + Grafana",
    prompt: "Build a self-service dashboard that lets engineers and customers understand their rate-limit consumption in real time. Instrumentation: add Prometheus counters to your rate-limiter middleware — rate_limit_requests_total{client_id, endpoint, result='allowed'|'throttled'}, rate_limit_bucket_remaining{client_id, endpoint} (gauge). Recording rules: pre-aggregate to 1-min rate and throttle ratio per (client, endpoint) — avoids high-cardinality query at dashboard time. Backend API (FastAPI): GET /limits/{client_id} → current bucket state (remaining, reset_at, limit) per endpoint; GET /limits/{client_id}/history?window=24h → time-series of throttle events from Prometheus. Frontend (React): per-client summary card (current usage % of limit, sparkline of last 1 h); top-10 most throttled endpoints table; heatmap of throttle events by (hour-of-day × day-of-week) to spot patterns; 'suggest quota increase' badge when throttle rate > 5% over past 7 days. Alert integration: Prometheus alerting rule — if throttle_ratio > 0.10 for 5 min, fire Slack alert with client_id and endpoint. Show: simulate client hitting 80% of their quota → dashboard shows yellow warning → throttle_ratio crosses 10% → alert fires → dashboard turns red."
  },
 
  // ── 418 ── originally id 218 (Low-Latency KV Store)
  {
    id: 418,
    title: "Low-Latency Key-Value Store with TTL (Rust + RocksDB)",
    cat: "Performance",
    stack: "Rust + RocksDB + Tokio + HTTP (axum)",
    prompt: "Build a compact, persistent key-value store with sub-millisecond reads, TTL support, and concurrent access safety. Storage: RocksDB column family 'data' for values, column family 'ttl' for {key → expiry_timestamp_ns}. API (axum): PUT /keys/:key {value, ttl_secs?} → write to data CF, if ttl write expiry to ttl CF atomically via WriteBatch; GET /keys/:key → read data CF, check ttl CF, if expired delete both CFs and return 404; DELETE /keys/:key → delete from both CFs. TTL expiry: background Tokio task every 5 s scans ttl CF for keys with expiry < now(), deletes them from both CFs in batches of 1000 (avoid write stalls). Concurrent safety: Tokio + RocksDB's thread-safe handle, no external lock needed. Benchmarks (criterion): single-threaded sequential write 1 M keys, random read 1 M keys, 8-thread concurrent mixed (70% read 30% write) — report ops/s and p50/p99/p999 latency. Tuning knobs: block_cache size, write_buffer_size, compression (Snappy vs Zstd) — show how each affects benchmark. Show: write 1 M keys, 100 k with 1 s TTL → after 5 s those 100 k are gone, confirmed by GET returning 404 and RocksDB key count metric."
  },
 
  // ── 419 ── originally id 219 (Encrypted Search)
  {
    id: 419,
    title: "Encrypted Search with Secure Index Tokens",
    cat: "Auth & Security",
    stack: "Python + PostgreSQL + cryptography library",
    prompt: "Prototype searchable encryption so that neither the database server nor an attacker with DB access can learn the plaintext of stored or queried values. Scheme: Deterministic Encryption for Equality Search — HMAC-SHA256(field_value.lower(), search_key) as a blind index column stored alongside the encrypted ciphertext (AES-256-GCM with per-row nonce). Registration: when storing email 'alice@example.com': (a) encrypt plaintext → store in email_enc; (b) compute blind_index = HMAC(email.lower(), SEARCH_KEY) → store in email_idx. Search: to find by email → compute blind_index of query → SELECT WHERE email_idx = ? → decrypt matching rows' email_enc → return. Limitations demonstration: (1) deterministic index leaks frequency (same value → same token) — show this explicitly; (2) prefix search impossible without OPE (Order-Preserving Encryption) or full-index scan. OPE stub: explain why OPE is dangerous (reveals order, vulnerable to statistical attacks) — recommend against it, suggest bloom filter approach for prefix. Key management: SEARCH_KEY lives in application env, never in DB. Key rotation: to rotate SEARCH_KEY, re-derive all blind indexes — provide a migration script. Show: store 1000 users, search by email, verify DB dump contains only ciphertext and HMAC tokens (no plaintext), query still returns correct user."
  },
 
  // ── 420 ── originally id 220 (Cost-Aware Router)
  {
    id: 420,
    title: "Cost-Aware Request Router Across Cloud Regions",
    cat: "Infrastructure",
    stack: "Node + Express + Redis + Prometheus",
    prompt: "Build a request router that dynamically chooses between cloud regions based on a cost-latency tradeoff policy. Setup: simulate 3 'regions' as local Express servers on different ports, each reporting synthetic latency (mean ± jitter) and a cost-per-request (cheap region: $0.00001; normal: $0.00003; expensive-but-fast: $0.00007). Router policy engine: for each incoming request, collect current p99 latency (polled from /metrics of each region every 10 s) and cost; score each region as score = w_latency × p99_ms + w_cost × cost_per_req × 1000 (weights configurable); pick lowest score. Policy presets: LATENCY_FIRST (w_cost=0.1, w_latency=1), COST_FIRST (w_cost=1, w_latency=0.1), BALANCED (equal weights). Latency SLO guard: if chosen region's p99 > SLO_THRESHOLD_MS (e.g. 200 ms), escalate to next cheapest region that is within SLO — never send to out-of-SLO region regardless of cost. Simulation: inject latency spikes in cheap region → watch router migrate traffic to balanced region → spike recovers → traffic migrates back. Dashboard: real-time chart of cost_per_minute and p99 per region, region-selection pie chart, estimated monthly savings vs always-using-cheapest. Show: latency spike causes 100% traffic shift in < 30 s."
  },
 
  // ── 421 ── originally id 221 (Mobile Push Delivery)
  {
    id: 421,
    title: "Scalable Mobile Push Notification Fanout",
    cat: "Messaging",
    stack: "Node + PostgreSQL + Redis BullMQ + Firebase FCM + APNs (stub)",
    prompt: "Build a push notification service that fans out to millions of devices reliably. Data model: devices table (device_id, user_id, platform: ios|android, push_token, active boolean, last_seen). topics table (topic_id, name). device_topics junction. Flow: POST /notifications/topic/:topic_id {title, body, data} → insert notification record → enqueue fanout job. Fanout worker: fetch all active device_tokens for topic in pages of 500; enqueue one send-job per page to a high-concurrency BullMQ queue (concurrency 20). Send worker: for android → FCM HTTP v1 API (batch up to 500 tokens); for iOS → APNs HTTP/2 (stub: log instead of actual send). Error handling: FCM returns 'invalid-token' → mark device inactive in DB; 'quota-exceeded' → exponential backoff retry. Deduplication: Redis SET with 24 h TTL on (notification_id, device_id) prevents double-send on worker restart. Analytics: update delivery_events table (device_id, notification_id, status: sent|failed|invalid_token, timestamp). Throughput target: fanout 1 M devices in < 30 s using worker parallelism. Show: send to topic with 10 k subscribers, 500 invalid tokens → those marked inactive → delivery report shows 9500 sent, 500 failed."
  },
 
  // ── 422 ── originally id 222 (Fine-Grained Audit Logs)
  {
    id: 422,
    title: "Fine-Grained Immutable Audit Log System",
    cat: "Auth & Security",
    stack: "Python + PostgreSQL + FastAPI + BLAKE2",
    prompt: "Build a security-grade audit log that is tamper-evident, queryable, and append-only. Schema: audit_log (id BIGSERIAL, event_type, actor_id, actor_ip, resource_type, resource_id, old_data JSONB, new_data JSONB, diff JSONB, created_at, prev_hash TEXT, current_hash TEXT). Hash chain: current_hash = BLAKE2b(prev_hash || id || event_type || actor_id || resource_id || created_at || SHA256(payload)) — links each entry to the previous, making any tampering detectable. Append: Python context manager that wraps any DB write — on __exit__, diff old vs new state (DeepDiff), insert audit row with hash computed from previous row's hash (SELECT … FOR UPDATE on last row to prevent concurrent chain breaks). Integrity verifier: background job every 10 min replays hash chain from row 1, alerts if any row's current_hash doesn't match recomputed hash. Query interface: GET /audit?actor=alice&resource_type=invoice&from=2024-01-01 — full-text index on (actor_id, resource_type, resource_id), date-range index on created_at. Retention + archival: rows older than 90 days compressed and moved to cold_audit_log table with same schema; chain integrity maintained across tables. Role enforcement: audit table owned by audit_role; application user has INSERT only, no UPDATE/DELETE (enforced by PostgreSQL GRANT). Show: update a record, verify audit row exists, tamper with one row's data in DB, verifier detects hash mismatch within 10 min."
  },
 
  // ── 423 ── originally id 223 (Multilingual Search Index)
  {
    id: 423,
    title: "Multilingual Search Index with Language Detection",
    cat: "Database",
    stack: "Elasticsearch + Node + fasttext (language detection)",
    prompt: "Create a search service that correctly indexes and queries documents in multiple languages with per-language analysers and graceful fallback. Indexing: on document ingest, run fasttext language detection (top-3 languages + confidence); store detected_lang in document. Use a per-language sub-field: body.en (english analyser: porter stemmer, stop words), body.fr (french analyser), body.de (german analyser), body.zh (smartcn CJK analyser), body.ar (arabic analyser with diacritics removal). Query time: detect query language; search body.{lang} with boost 2.0 + body.en with boost 1.0 as fallback; use dis_max query to avoid score inflation from multi-field. Synonym support: per-language synonym files (e.g., 'car, automobile, vehicle' for EN; 'voiture, automobile' for FR) loaded as Elasticsearch synonym token filter. Fuzzy matching: add fuzziness: AUTO for typo tolerance on non-CJK fields. Relevance tuning: BM25 with custom k1=1.5, b=0.75 per language. Language fallback: if document has no French body, fall back to English body for French queries — demonstrate with a test. Show: index 1000 mixed-language documents, run English, French, and German queries, verify top results are in the query language, measure MRR (Mean Reciprocal Rank) improvement vs single-analyser baseline."
  },
 
  // ── 424 ── originally id 224 (Push-based CDC Pipeline)
  {
    id: 424,
    title: "Push-Based CDC Pipeline: PostgreSQL → Kafka → Consumers",
    cat: "Messaging",
    stack: "Python + PostgreSQL + Debezium (Docker) + Kafka + FastAPI",
    prompt: "Build a complete Change Data Capture pipeline where database changes flow to downstream consumers in real time. Step 1 — Source setup: enable PostgreSQL logical replication (wal_level=logical); create replication slot for Debezium. Step 2 — Debezium connector: configure Debezium PostgreSQL connector (Docker) targeting your DB; it streams INSERT/UPDATE/DELETE as Avro-encoded Kafka messages to topic per-table (e.g. mydb.public.orders). Step 3 — Schema registry: deploy Confluent Schema Registry; Debezium auto-registers Avro schemas; consumers use schema ID in message header to deserialise. Step 4 — Consumers: (a) Elasticsearch sync consumer — upsert/delete documents on order index; (b) Analytics consumer — maintain a materialised view in ClickHouse or a simple SQLite for aggregates (orders_by_hour, revenue_by_product). Step 5 — Exactly-once semantics where possible: consumers commit Kafka offset only after successful write to sink; on restart, replay from last committed offset (at-least-once); deduplicate in sink using (topic, partition, offset) as idempotency key. Step 6 — Lag monitoring: track consumer_group_lag via Kafka AdminClient, expose as Prometheus metric, alert if lag > 10 000 messages. Show: insert 1000 rows, update 200, delete 50 → Elasticsearch reflects changes within 2 s → analytics table updated."
  },
 
  // ── 425 ── originally id 225 (Adaptive Image CDN)
  {
    id: 425,
    title: "Adaptive Image CDN with Device-Aware Edge Serving",
    cat: "Infrastructure",
    stack: "Nginx + Lua (OpenResty) + S3 (MinIO) + Sharp (Node worker)",
    prompt: "Build an edge layer that serves images tailored to the requesting device's capabilities and connection quality without changing any origin URLs. Edge logic (OpenResty Lua): (1) Parse Accept header — if image/avif supported serve AVIF; else if image/webp serve WebP; else JPEG. (2) Parse Client-Hints headers (DPR, Viewport-Width, Save-Data) to compute target_width = viewport_width × DPR, capped at [320, 640, 1280, 2560]. (3) Construct cache key: origin_url + width + format. (4) Check Nginx proxy_cache; on hit serve immediately with Age header. (5) On miss, call Node Sharp worker via internal HTTP to resize + convert, cache result for 7 days. Vary header: set Vary: Accept, DPR, Viewport-Width on responses so CDN caches separate variants per device class. Save-Data handling: if Save-Data: on header present, halve the target_width and use quality 60 instead of 80. Origin fallback: if Sharp worker unreachable, proxy original image and set Cache-Control: no-store (don't cache degraded response). Cache purge: POST /admin/purge?url=... → Nginx proxy_cache_purge module clears all variants for that origin URL. Show: request same image from simulated mobile (Viewport-Width: 375, DPR: 2) vs desktop (1440, 1) → different file sizes served, both cached separately."
  },
 
  // ── 426 ── originally id 226 (Search Suggest with Usage)
  {
    id: 426,
    title: "Search Autocomplete with Popularity-Weighted Trie",
    cat: "Performance",
    stack: "Node + Redis + TypeScript",
    prompt: "Build a sub-10 ms search-as-you-type service backed by a weighted in-memory trie that promotes popular and recent queries. Trie structure: each TrieNode stores {children: Map<char, TrieNode>, is_end: boolean, score: number, query: string}. Insert: for each search query, traverse/create nodes for every prefix; at the terminal node update score = 0.7 × frequency_score + 0.3 × recency_score (recency_score = 1 / (days_since_last_searched + 1)). Top-k retrieval: at query prefix node, run bounded DFS collecting is_end nodes; maintain a min-heap of size k; return top-k sorted by score. Redis persistence: every 5 min, serialise trie to Redis as JSON BLOB; on startup deserialise (avoids cold-start loss). Usage ingestion: POST /search/log {query} → increment frequency counter in Redis ZSET (key=query, score=count) and update last_searched timestamp. Rebuild: nightly job reads top 100 k queries from Redis ZSET, rebuilds trie from scratch (avoids trie fragmentation). Fuzzy fallback: if exact-prefix trie returns < 3 results, fall back to Redis ZRANGEBYLEX on a sorted set of all queries (range [prefix, prefix\xff]). Benchmark: 1 M prefix lookups on 100 k query trie — target p99 < 5 ms. Show: type 'pyt' → suggestions ['python tutorial', 'pytorch install', 'python list comprehension'] ordered by score."
  },
 
  // ── 427 ── originally id 227 (Typed API Contracts)
  {
    id: 427,
    title: "Typed API Contracts: OpenAPI → SDK + Contract Tests",
    cat: "API Design",
    stack: "TypeScript + Node + openapi-generator + Jest",
    prompt: "Build a workflow where a single OpenAPI 3.1 spec is the authoritative source of truth for both client SDK and server contract tests — so drift between spec and implementation is caught in CI. Step 1 — Spec authoring: write a realistic OpenAPI spec for a bookstore API (CRUD for books, orders, authors) with request/response schemas, error schemas (4xx/5xx with problem+json format), and example values. Step 2 — SDK generation: run openapi-generator-cli (typescript-fetch template); customise generator config (modelPropertyNaming: camelCase, enumPropertyNaming: UPPERCASE_UNDERSCORES); commit generated SDK to repo. Step 3 — Contract tests (provider side): use jest + supertest; for every endpoint in the spec, auto-generate a test that calls the real Express server and validates: status code matches spec, response body matches JSON Schema (ajv), required headers present. Step 4 — Breaking change detection: add a CI step that diffs current spec vs main-branch spec using openapi-diff; fail if breaking changes detected (removed field, changed type, removed endpoint) unless a new major version is declared. Step 5 — Mock server: use prism to serve the spec as a mock; run SDK tests against mock to verify SDK and spec are consistent. Show: remove a required field from the spec → contract test fails → build blocked."
  },
 
  // ── 428 ── originally id 228 (Offline-First Sync)
  {
    id: 428,
    title: "Offline-First Mobile Sync Engine with Conflict Resolution",
    cat: "Distributed Systems",
    stack: "React Native + SQLite (expo-sqlite) + Node + PostgreSQL + WebSocket",
    prompt: "Build an offline-first task manager where edits made offline sync automatically on reconnect with deterministic conflict resolution. Local store: SQLite tables mirror server schema; every row has (id, updated_at, sync_status: SYNCED|PENDING|CONFLICT, vector_clock JSONB). Local write: update row, set sync_status=PENDING, increment local vector clock. Sync protocol (on reconnect via WebSocket): (1) Client sends all PENDING rows with their vector clocks. (2) Server compares with server-side vector clocks: if client vc > server vc → client wins, server applies; if server vc > client vc → server wins, send server version to client; if concurrent (neither dominates) → CONFLICT. (3) Conflict resolution strategies: LWW (last updated_at wins), field-level merge (merge non-conflicting fields, flag truly conflicting fields), or user-prompt (send both versions to client, user picks). Retry: failed sync retried with exponential backoff (offline, server error). Partial sync: large datasets sync in pages of 100 rows ordered by updated_at — resume from last synced row on disconnect. Show: edit task A on device 1 offline, edit same task on device 2 online, device 1 reconnects → LWW picks latest timestamp, user sees merged result."
  },
 
  // ── 429 ── originally id 229 (Token Introspection)
  {
    id: 429,
    title: "OAuth2 Token Introspection & Revocation Service",
    cat: "Auth & Security",
    stack: "Node + Express + PostgreSQL + Redis",
    prompt: "Build a standalone token introspection (RFC 7662) and revocation (RFC 7009) service that other microservices can query to validate tokens without decoding JWTs themselves. Token storage: PostgreSQL tokens table (jti UUID PK, user_id, client_id, scope, issued_at, expires_at, device_fingerprint, revoked_at, revoke_reason). Cache: on introspection, check Redis first (key=jti, value=active|revoked, TTL=token remaining life) — cache hit avoids DB round-trip. Introspection endpoint: POST /introspect {token} (Bearer auth required for caller) → decode JWT header (kid) → fetch public key from JWKS cache → verify signature → check exp → check revoked_at in Redis/DB → return RFC 7662 payload {active, scope, exp, client_id, sub, jti}. Revocation endpoint: POST /revoke {token, token_type_hint, reason} → write revoked_at + reason to DB → set Redis key to 'revoked' with same TTL → invalidate all tokens in same refresh-token family (token_family_id column). Device mapping: token row stores device_fingerprint (hash of User-Agent + IP); if new device detected on introspection, flag suspicious. Bulk revoke: POST /users/:id/revoke-all → revoke all active tokens for user (on password change or account compromise). Show: issue token → introspect (active=true) → revoke → introspect again (active=false) → verify cached revocation served in < 1 ms."
  },
 
  // ── 430 ── originally id 230 (Graph Processing Pipeline)
  {
    id: 430,
    title: "Large-Scale Graph Processing Pipeline",
    cat: "Database",
    stack: "Python + PySpark + NetworkX + GraphFrames + Matplotlib",
    prompt: "Process a large social-network graph dataset and compute graph analytics at scale. Dataset: generate or download a synthetic graph (1 M nodes, 10 M edges) representing social follows. Part 1 — Spark ingestion: load edge list CSV into Spark DataFrame, create GraphFrame (vertices + edges). Part 2 — Connected components: run GraphFrames connectedComponents(); find the giant component size and isolated nodes; visualise component size distribution. Part 3 — Shortest paths: BFS from 10 seed nodes to all reachable nodes (GraphFrames BFS); compute average shortest path length (small-world test). Part 4 — PageRank: run GraphFrames pageRank(tol=0.01); export top-100 nodes by rank; compare with degree centrality (are top-degree nodes always top-PageRank?). Part 5 — Community detection: sample 50 k nodes, export to NetworkX, run Louvain algorithm; visualise community membership with colour-coded force-directed layout (Matplotlib). Part 6 — Export & serve: write top-100 PageRank nodes + their community IDs to PostgreSQL; expose REST API for querying. Show: runtime comparison for PageRank on 10 M edges with 4 vs 8 Spark executors (parallelism scaling)."
  },
 
  // ── 431 ── originally id 231 (Time-Series Compression)
  {
    id: 431,
    title: "Time-Series Compression: Delta-of-Delta & Gorilla",
    cat: "Performance",
    stack: "Python + NumPy + BitArray",
    prompt: "Implement two time-series compression algorithms from scratch and benchmark compression ratio vs query speed. Algorithm 1 — Delta-of-Delta encoding (Gorilla timestamps): (a) store first timestamp as 64-bit absolute; (b) subsequent timestamps: compute delta = t_n − t_(n−1), then delta-of-delta = delta_n − delta_(n−1); encode using variable-length encoding (0 bit → value 0; 10 + 7 bits → -64..63; 110 + 9 bits → -256..255; 1110 + 12 bits → -2048..2047; 1111 + 32 bits → full range). Algorithm 2 — XOR float compression (Gorilla values): (a) first value stored raw 64-bit; (b) XOR current with previous; if XOR=0 write 0-bit; else encode leading/trailing zero counts + meaningful bits. Implement both as Python classes with compress(series) → BitArray and decompress(bits) → series. Verify: decompress(compress(X)) == X for 100 k random series. Benchmark: 1 M data points, 1-second intervals, values ∼Gaussian noise + upward trend. Measure: raw size (8 MB), compressed size, compression ratio, encode throughput (MB/s), decode throughput. Compare with: gzip on raw bytes, pandas Parquet (Snappy). Show: sensor data with regular 1-s intervals achieves > 90% compression; irregular intervals achieve less."
  },
 
  // ── 432 ── originally id 232 (Multi-Region Failover)
  {
    id: 432,
    title: "Multi-Region Failover with DNS Switching & RTO/RPO Measurement",
    cat: "Infrastructure",
    stack: "Docker Compose + Terraform (local) + Python + dnspython",
    prompt: "Simulate a two-region active/passive deployment with automated DNS failover and measure RTO/RPO. Setup (Docker Compose): primary region (postgres-primary + app-primary + haproxy), standby region (postgres-standby + app-standby + haproxy); PostgreSQL streaming replication primary → standby. DNS simulation: run a local PowerDNS (or use /etc/hosts + a Python resolver mock); DNS record points to primary IP. Failover script (Python): (1) Health-check primary every 5 s (HTTP GET /health); (2) On 3 consecutive failures → promote standby (pg_promote()), update DNS record to standby IP (simulate propagation delay of 60 s TTL). RPO measurement: write a record to primary every second with a sequence number; when failover completes query standby for max sequence number → compute RPO = (primary_last_sequence − standby_last_sequence) × 1 s. RTO measurement: start timer at first health-check failure; stop timer when first HTTP 200 arrives from standby address → RTO in seconds. Failback: after simulated primary recovery, re-establish replication from standby → primary, shift DNS back. Show: inject primary failure → automated failover completes in < 120 s → RTO logged → RPO typically < 5 s."
  },
 
  // ── 433 ── originally id 233 (Data Catalog & Lineage)
  {
    id: 433,
    title: "Data Catalog with Schema Discovery & Lineage Graph",
    cat: "Observability",
    stack: "React + Node + PostgreSQL + D3.js",
    prompt: "Build a data catalog that auto-discovers datasets, tracks schema changes, and visualises data lineage from sources to derived tables. Discovery agent: connect to PostgreSQL (and optionally S3 Parquet files); reflect all tables — store (table_name, column_name, data_type, is_nullable, sample_values, row_count, last_modified) in catalog DB; schedule re-scan every 6 h. Schema change detection: diff current schema snapshot vs previous; classify changes as ADDITIVE (new column), BREAKING (removed column, changed type), or METADATA (rename, comment); store change history. Lineage tracking: parse SQL views and dbt model YAML to extract (source_table → derived_table) lineage edges; store as a directed graph. Business metadata: API to add (owner_team, sensitivity_level: public|internal|confidential|restricted, description, tags); show % of tables with metadata as catalog completeness score. Frontend (React + D3): searchable table of all datasets with column explorer; lineage graph (force-directed) where clicking a table highlights its upstream sources and downstream consumers; schema change history timeline per table; completeness dashboard. Show: create a view that joins two tables → lineage graph shows three-node chain → change a source column type → catalog flags BREAKING change → downstream view owner notified."
  },
 
  // ── 434 ── originally id 234 (Streaming Windowed Aggregations)
  {
    id: 434,
    title: "Streaming Windowed Aggregations with Late-Arrival Handling",
    cat: "Messaging",
    stack: "Python + Kafka + Faust (stream processing) or custom asyncio consumer",
    prompt: "Implement tumbling and sliding window aggregations on a Kafka event stream with event-time semantics and late-arrival tolerance. Producer: emit {event_id, user_id, action, amount, event_time_ms} events at 1000/s with realistic late arrivals (5% of events arrive 10–120 s late, simulated by shuffling timestamps before publishing). Tumbling window (1 min): aggregate COUNT and SUM(amount) per user per minute using event_time, not processing time. Sliding window (5 min, slide 1 min): aggregate COUNT per user over the last 5 min, updated every minute. Watermark: track max(event_time) seen; watermark = max_event_time − 30 s (grace period). Window trigger: when watermark advances past window_end, close window and emit result; late events arriving after window close go to a LATE_EVENTS topic. State store: in-memory dict {(user_id, window_start) → {count, sum}}; checkpoint to Kafka compacted topic every 30 s for fault tolerance. Show: inject a burst of late events (100 events with event_time 45 s ago) → 20 of them fall within grace period and update the window → 80 are post-watermark and routed to late topic → final window result shown with and without late arrivals. Measure: window accuracy (% of events captured before trigger) vs latency tradeoff at different watermark delays."
  },
 
  // ── 435 ── originally id 235 (Connection Health Monitor)
  {
    id: 435,
    title: "Long-Lived TCP/WebSocket Connection Health Monitor",
    cat: "Infrastructure",
    stack: "Go + Prometheus + Grafana",
    prompt: "Build a health monitor for long-lived TCP and WebSocket connections that detects flapping, stale connections, and misbehaving clients. Connection tracker: maintain an in-memory map of {conn_id → ConnectionState{first_seen, last_activity, message_count, error_count, latency_ema, status}}. Heartbeat protocol: server sends PING every 30 s; client must respond PONG within 10 s; if no PONG → mark STALE; after 3 consecutive stale cycles → close connection with code 1001 (going away). Flapping detection: a connection that connects and disconnects > 5 times in 60 s is classified as FLAPPING; add to a temporary blocklist (Redis, 5 min TTL); return 429 on reconnect with Retry-After. Latency tracking: measure round-trip of PING/PONG, update EMA (α=0.1) per connection; if EMA > 500 ms flag as HIGH_LATENCY. Misbehaving clients: track message_rate per 10 s window; if > 100 msg/10 s and messages are empty/malformed → throttle (delay processing) and emit alert. Prometheus metrics: websocket_connections_active, websocket_connections_stale, websocket_connections_flapping, websocket_message_rate_per_conn (histogram). Grafana dashboard: connection lifecycle heatmap (connect/disconnect events on timeline), top-10 high-latency connections table, flap rate time series. Show: simulate 5 flapping clients → they get blocklisted → monitor dashboard shows event."
  },
 
  // ── 436 ── originally id 236 (Search Result Diversification)
  {
    id: 436,
    title: "Search Result Diversification to Avoid Filter Bubbles",
    cat: "Performance",
    stack: "Python + Elasticsearch + FastAPI",
    prompt: "Implement search result diversification that balances relevance with category variety so results don't all come from the same narrow segment. Baseline: standard BM25 search on a product catalogue (100 k products with category, brand, price). Problem demo: search 'running shoes' → top 10 all Nike Air Max variants (relevant but not diverse). Maximal Marginal Relevance (MMR): re-rank results using MMR = argmax[λ × sim(doc, query) − (1−λ) × max(sim(doc, selected))] where similarity is TF-IDF cosine. Tune λ: λ=1 → pure relevance, λ=0 → pure diversity; offer λ as API param with default 0.7. Category-aware slot filling: enforce that top-10 results contain ≥ 3 distinct categories; if not, replace lowest-scoring same-category item with the next highest-scoring item from an underrepresented category. Metrics: implement nDCG@10 (relevance), ILD (Intra-List Diversity = mean pairwise distance of embeddings), Coverage (% of relevant categories in results). A/B comparison endpoint: GET /search?q=...&mode=standard|mmr|slot_fill → returns results + metrics for each mode. Show: query 'running shoes' → standard: ILD=0.12, Coverage=20% → MMR (λ=0.7): ILD=0.31, Coverage=60% with < 5% nDCG loss."
  },
 
  // ── 437 ── originally id 237 (Personalization Engine)
  {
    id: 437,
    title: "Real-Time Personalization Engine with Feature Cache",
    cat: "Database",
    stack: "Python + Redis + scikit-learn + FastAPI",
    prompt: "Build a real-time personalization service that scores and ranks content for each user using a lightweight ML model with features served from a sub-millisecond cache. Features: offline batch job (runs every 6 h) computes per-user features: {avg_session_duration, top_3_categories (one-hot), days_since_last_visit, purchase_frequency, preferred_price_tier}; stores as Redis HSET user:{id}:features with 8 h TTL. Model: train a LightGBM ranking model (LambdaRank) offline on (user_features + item_features + interaction_label) pairs; export as ONNX for fast inference. Online serving: GET /personalise?user_id=X&items=[id1..id100] → fetch user features from Redis (< 1 ms) + fetch item features from Redis → run ONNX model (batch of 100 in one call) → return ranked item list. Cold start: if user features not in Redis → serve popularity-based ranking as fallback; log the cold-start event for monitoring. A/B test: route 50% of users to personalized ranking, 50% to popularity baseline; track CTR and order conversion rate per group. Show: serve 100-item ranking in < 20 ms end-to-end; cold-start user gets popularity ranking; hot user gets personalised ranking; A/B metrics update in real time."
  },
 
  // ── 438 ── originally id 238 (Access Token Scopes Manager)
  {
    id: 438,
    title: "OAuth2 Fine-Grained Token Scope Manager",
    cat: "Auth & Security",
    stack: "Node + PostgreSQL + Express + React",
    prompt: "Build a self-service scope management system for OAuth2 tokens with admin UI and scope audit tooling. Data model: scopes table (scope_id, name, description, resource, action, sensitivity: low|medium|high); clients table (client_id, name, allowed_scopes[]); tokens table (jti, client_id, granted_scopes[], issued_at, last_used_at). Scope definition: scopes follow the pattern resource:action:qualifier (e.g. orders:read:own, invoices:write:all, users:admin); validators enforce this naming scheme. Token issuance: POST /oauth/token validates requested_scopes ⊆ client.allowed_scopes; records granted_scopes. Scope audit: background job computes for each token its 'used_scopes' by parsing access logs (which endpoints were called); compare with granted_scopes; flag tokens where granted_scopes − used_scopes > {} (over-privileged). Admin UI (React): scope browser with sensitivity badges; per-client allowed-scope editor (add/remove with approval workflow for high-sensitivity scopes); token explorer (list tokens for a client, show granted vs used scopes, one-click revoke); 'least-privilege suggestion' button — shows the minimum scope set that would have satisfied that token's actual usage. Show: issue token with 10 scopes, use only 3, audit flags 7 unused → admin reduces scope set."
  },
 
  // ── 439 ── originally id 239 (Event Replay Tool)
  {
    id: 439,
    title: "Kafka Event Replay Tool with Rate Limiting & Filtering",
    cat: "Messaging",
    stack: "Node + Kafka (kafkajs) + FastAPI + Redis",
    prompt: "Build a safe, operator-facing tool to replay historical Kafka events to repaired or new consumers. Use cases: (1) consumer had a bug, messages were DLQed or offset-committed incorrectly; (2) new downstream service needs to bootstrap from history. Replay modes: TIME_RANGE — replay events between start_ts and end_ts; OFFSET_RANGE — replay from offset A to B on specific partition; KEY_FILTER — only replay events where message key matches a regex (e.g. only user_id=123 events). Rate limiting: configurable msgs/sec (default 500) using a token bucket in Redis — prevents overwhelming a repaired consumer. Progress tracking: persist {replay_id, status, source_topic, target_topic_or_consumer_group, total_messages, replayed_count, failed_count, started_at, estimated_eta} in PostgreSQL; expose GET /replays/:id. Safety: replay to a 'replay.{original_topic}' shadow topic by default; operator explicitly opts into direct replay to original topic with a confirmation step. Schema compatibility: before starting, verify consumer's registered schema is compatible with the oldest event in the replay window. Show: create consumer with a bug → messages fail → fix consumer → start replay with rate=200 msgs/s → progress bar fills → consumer processes all correctly."
  },
 
  // ── 440 ── originally id 240 (Schema Migration Visualiser)
  {
    id: 440,
    title: "Database Schema Migration Visualizer & Safety Checker",
    cat: "Database",
    stack: "React + Node + PostgreSQL + Alembic metadata",
    prompt: "Build a tool that visualises pending schema migrations, shows diffs, detects destructive operations, and provides rollback guidance before any SQL runs. Migration parser: read Alembic migration files (or raw SQL files with -- upgrade/-- downgrade markers); extract SQL statements per migration. Diff renderer: parse SQL with node-sql-parser; categorise each statement: ADD COLUMN, DROP COLUMN (🔴 destructive), ALTER COLUMN TYPE (🟡 risky), CREATE INDEX CONCURRENTLY (🟢 safe), CREATE INDEX without CONCURRENTLY (🟠 locks table). Safety scoring: each migration gets a safety score — multiple DROP or ALTER → HIGH RISK; only ADD/INDEX CONCURRENTLY → LOW RISK. Visualizer (React): migration DAG (nodes = migrations, edges = depends_on); click a node → see the diff with colour-coded SQL lines; dependency conflict detection (two migrations modifying the same column in incompatible order). Rollback guide: for each upgrade statement auto-generate the inverse downgrade SQL suggestion (DROP COLUMN for ADD COLUMN, etc.) and highlight where auto-generation is impossible (data loss, type narrowing). Dry-run estimator: for large ALTER TABLE statements, estimate lock duration based on table row count (fetch from PostgreSQL) and Postgres version-specific lock behaviour. Show: migration that drops a column → tool flags DESTRUCTIVE, shows estimated lock time, suggests online alternative (rename + keep + drop in separate migration)."
  },
 
  // ── 441 ── originally id 241 (Content Delivery Strategy)
  {
    id: 441,
    title: "Multi-Layer Cache Strategy Lab",
    cat: "Infrastructure",
    stack: "Node + Nginx + Redis + Prometheus",
    prompt: "Build an instrumented cache lab to compare multi-layer caching strategies for different content types and measure real hit/miss patterns. Architecture: three cache layers — L1: Nginx proxy_cache (in-memory, 128 MB); L2: Redis (1 GB, LRU eviction); L3: origin (Node Express server, simulates slow DB with 100 ms delay). Content types: STATIC (JS/CSS/images, long TTL), API_CACHEABLE (product listing, medium TTL), API_USER_SPECIFIC (user profile, cannot cache at L1/L2 without user key), DYNAMIC (stock price, short TTL or no-cache). Strategy configurations to compare: (a) L1-only: Nginx caches everything for 60 s; (b) L1+L2: Nginx for 10 s, Redis for 300 s (stale-while-revalidate pattern); (c) Surrogate keys: Redis entries tagged with {product_id}; on product update purge all tagged entries. Load simulation: locust sends 5000 req/min for 5 min with realistic request distribution. Metrics (Prometheus): cache_hit_ratio per layer per content_type, origin_requests_per_min (lower is better), p99_latency by strategy. Report: for each strategy show hit ratio, origin load, and p99. Show: surrogate-key purge invalidates 50 related cached pages in < 100 ms while non-purge strategy serves stale data for 300 s."
  },
 
  // ── 442 ── originally id 242 (Deduplicating Ingest Pipeline)
  {
    id: 442,
    title: "Deduplicating Event Ingest Pipeline",
    cat: "Messaging",
    stack: "Python + Redis + Kafka + FastAPI",
    prompt: "Build a high-throughput telemetry ingest pipeline that deduplicates events at multiple levels to prevent double-counting analytics. Event model: {event_id UUID, source_id, event_type, timestamp_ms, payload}. Level 1 — Client-side dedup key: clients attach event_id (UUID v7 — sortable by time); ingest endpoint rejects if event_id seen in Redis in past 24 h (SET NX with 24 h TTL — O(1) check). Level 2 — Kafka exactly-once producer: configure Kafka producer with enable.idempotence=true and transactional.id=source_id; guarantees no broker-level duplicates on retry. Level 3 — Bloom filter fast path: before Redis check, run through a rotating Bloom filter (two filters, swap every 12 h); on positive: high probability duplicate → skip Redis (reduce load by 90% for high-duplicate workloads). Level 4 — Downstream consumer dedup: consumer tracks (kafka_partition, kafka_offset) in a compact PostgreSQL table; skips already-processed offsets. Memory vs false positive tradeoff: for 10 M events/day with 1% error rate — calculate Bloom filter size (m=96 MB) vs exact Redis SET (640 MB for 10 M keys × 64 bytes). Benchmark: 50 k events/s ingest, 10% genuine duplicates — measure: unique events correctly passed through, false positives blocked (should be < 1%), throughput drop vs no-dedup pipeline."
  },
 
  // ── 443 ── originally id 243 (Policy-as-Code Engine)
  {
    id: 443,
    title: "Policy-as-Code Engine with OPA/Rego",
    cat: "Auth & Security",
    stack: "Go + OPA (rego) + FastAPI (policy service) + React",
    prompt: "Build a policy engine that externalises all access control decisions into versioned, testable Rego policies. Policy types to implement: (1) RBAC: user has role, role has permissions, permission allows action on resource type. (2) ABAC: user.department == resource.department AND resource.classification <= user.clearance_level. (3) Rate-limiting policy: deny if request_count_last_minute > user.quota. OPA integration: run OPA as a sidecar; application calls POST /v1/data/authz/allow {input: {user, action, resource, context}}; OPA evaluates matching policy and returns {allow: true/false, reason: string}. Policy CI: write Rego unit tests (rego.v1 test framework) for every policy; run in CI — a policy that breaks existing tests cannot be merged. Versioning: policies stored in git; OPA bundle server serves versioned bundles; rollback = point OPA to previous bundle. Admin UI (React): policy editor with Rego syntax highlighting; test-case runner (input JSON → expected output); diff viewer between policy versions; audit log of which policy version was active for each decision. Benchmark: OPA decision latency (should be < 5 ms for typical RBAC); compare vs SQL-based permission check at same complexity. Show: write RBAC + ABAC policies, test edge cases, deploy, call from Python middleware, verify correct allow/deny decisions."
  },
 
  // ── 444 ── originally id 244 (Mutable to Immutable Migration)
  {
    id: 444,
    title: "Mutable Table to Append-Only Migration with Compaction",
    cat: "Database",
    stack: "Python + PostgreSQL + SQLAlchemy",
    prompt: "Migrate a frequently-updated mutable table to an append-only pattern while maintaining query compatibility and measuring performance impact. Starting state: orders table (id, user_id, status, total, updated_at) with frequent UPDATE status changes. Step 1 — Dual-write: add orders_events table (event_id, order_id, event_type, data JSONB, created_at); modify application to append event AND update original table (transition period). Step 2 — Read model: create orders_current VIEW that reconstructs latest state from events using DISTINCT ON (order_id) ORDER BY created_at DESC — prove it matches the mutable table exactly. Step 3 — Cutover: stop writing to mutable table; all reads go to view. Step 4 — Compaction: background job every 6 h reads events, groups by order_id, keeps only the latest N events per order (configurable), moves older events to orders_events_archive. Step 5 — Performance benchmark: 1 M orders, 5 M events. Compare: UPDATE on mutable table (50 ms bulk update of 10 k rows) vs event append (30 ms for same 10 k rows); read latest state — mutable table: 20 ms full scan vs view: 80 ms (worse — explain why and how partial index fixes it). Show: full migration, zero downtime, view produces identical results to original table."
  },
 
  // ── 445 ── originally id 245 (Observability Sampler)
  {
    id: 445,
    title: "Dynamic Observability Sampler (Traces + Logs)",
    cat: "Observability",
    stack: "Node + OpenTelemetry + Loki + Prometheus",
    prompt: "Build a dynamic sampling layer that adjusts trace and log sampling rates in real time based on service conditions, controlling cost without losing signal. Architecture: a sampling decision service that other services poll every 30 s for their current sampling config. Sampling rules engine: rules stored in PostgreSQL with priority order — rule: {service_name, endpoint_pattern, condition, sample_rate}. Conditions: ALWAYS_SAMPLE if status_code >= 500; SAMPLE_AT(0.1) if endpoint matches /health; SAMPLE_AT(1.0) if user.tier == 'enterprise'; SAMPLE_AT(0.01) if service error_rate < 0.1%. Dynamic adjustment: Prometheus scraper runs every 60 s; if service error_rate crosses 1% → automatically escalate sampling to 100% for that service (write to DB); when error_rate recovers below 0.5% for 5 min → restore base rate. Log sampling: same engine for structured logs — each log line includes sample_rate metadata so downstream aggregations can correct for sampling (COUNT(*) × (1/sample_rate)). Show: inject errors into a service → sampling auto-escalates to 100% within 60 s → error traces captured in full → recovery → rate returns to 1%. Cost estimate: 100% sampling = $X/day; 1% base sampling = $0.15X/day; dynamic = $0.20X/day (best signal per dollar)."
  },
 
  // ── 446 ── originally id 246 (Service Configuration Store)
  {
    id: 446,
    title: "Central Config Store with Dynamic Reload via Consul",
    cat: "Infrastructure",
    stack: "Go + Consul + React + PostgreSQL",
    prompt: "Build a centralised configuration store where services receive config updates within seconds of an operator change, with no restart required. Backend: Consul KV as the primary store; each service's config lives at config/{env}/{service_name}/{key}. Config schema: JSON Schema per service registered in PostgreSQL (service_name, schema JSONB, version); every write is validated against the schema before Consul write — invalid config is rejected with a detailed error. Watch & apply: each service runs a Go goroutine that calls Consul's blocking GET (index-based long-polling); on change, deserialise new config, validate types, call registered OnConfigChange handler — no restart needed. Rollback: every Consul write first snapshots the current value to PostgreSQL config_history (service, key, old_value, new_value, changed_by, changed_at); rollback = write previous value back to Consul. Environment promotion: POST /promote?service=X&from=staging&to=production → diff staging vs production config, show diff, require approval (two-step: diff review + confirm), then copy staging values to production. UI (React): config editor with JSON Schema form validation (react-jsonschema-form); live diff view between environments; history timeline per key; approval workflow for promotions. Show: change a config value → service logs 'config reloaded' within 2 s → behaviour changes without restart."
  },
 
  // ── 447 ── originally id 247 (DB Sharding Strategy Demo)
  {
    id: 447,
    title: "Horizontal DB Sharding Strategy Comparison",
    cat: "Database",
    stack: "Python + PostgreSQL (3 instances via Docker) + FastAPI",
    prompt: "Compare three horizontal sharding strategies on the same workload to show their distribution, query, and maintenance tradeoffs. Setup: 3 PostgreSQL instances (shard-0, shard-1, shard-2) running in Docker. Proxy layer: Python FastAPI app with a shard router. Strategy 1 — Range sharding: shard by user_id ranges (0–33 M → shard-0, 33–66 M → shard-1, 66–99 M → shard-2); routing table in a config file. Strategy 2 — Hash sharding: shard = SHA256(user_id) % 3; deterministic, no config table. Strategy 3 — Directory sharding: lookup table in Redis (user_id → shard_id); supports custom placement but adds a lookup hop. For each strategy demonstrate: Single-key query (goes to one shard — show SQL only runs on correct shard via logging). Cross-shard aggregation (COUNT(*) — must query all 3 shards and merge). Hotspot: with range sharding, insert sequential IDs → all go to shard-0 (hotspot); hash sharding distributes evenly. Rebalancing: add shard-3 to hash strategy — show which % of keys must migrate (≈25%). Benchmark: 1 M inserts, 1 M random reads — measure throughput and p99 per strategy. Show: range sharding hotspot vs hash sharding even distribution visualised as bar chart."
  },
 
  // ── 448 ── originally id 248 (Real-Time Analytics Dashboard)
  {
    id: 448,
    title: "Real-Time Analytics Dashboard with Top-K Streaming Metrics",
    cat: "Observability",
    stack: "Node + WebSocket + Redis Streams + React + Chart.js",
    prompt: "Build a streaming analytics dashboard that computes and displays top-K metrics (top pages, top errors, top users) with < 2 s end-to-end latency. Event producer: Node process emits {event_type, page, user_id, status_code, duration_ms, region} events at 5000/s to a Redis Stream (XADD). Consumer: reads from stream with XREADGROUP (consumer group for fault tolerance); for each event: increment Redis HINCRBY counters (page_views:{page}, error_counts:{status_code}, user_activity:{user_id}); maintain sorted sets (ZADD) for real-time ranking. Top-K computation: every 2 s, a worker calls ZREVRANGE on each sorted set to get top-10; computes rolling 1-min window by subtracting a snapshot taken 60 s ago from current values (two sorted sets, sliding window). WebSocket push: server pushes updated top-K JSON to all connected dashboard clients every 2 s. Dashboard (React + Chart.js): live-updating bar chart for top-10 pages; error rate time series (p99 duration per endpoint); regional heatmap; active user count (HLL via PFCOUNT). Show: inject a traffic spike to one page → it rises to #1 in top-10 within 4 s → spike subsides → drops back. Measure: event-to-dashboard latency (target < 2 s end-to-end)."
  },
 
  // ── 449 ── originally id 249 (Distributed Rate Limit Service)
  {
    id: 449,
    title: "Global Distributed Rate Limit Service with Fairness",
    cat: "Performance",
    stack: "Go + Redis Cluster + gRPC + Prometheus",
    prompt: "Build a centralised rate-limiting service that API nodes call via gRPC to enforce global quotas with fairness guarantees. Architecture: standalone Go service exposing gRPC CheckQuota(client_id, resource, cost) → {allowed: bool, remaining: int, reset_at: int64}. Algorithms implemented: (1) Token bucket (Redis Lua script: atomic DECRBY + refill on timestamp delta). (2) Sliding window log (Redis sorted set: trim old timestamps, count, check). (3) Fixed window (Redis INCR + EXPIRE). Fairness: Deficit Round Robin — each client accumulates a credit when under-limit; unused credits up to 2× burst are carried forward; prevents bursty clients from consuming disproportionate quota during shared windows. Redis Cluster: shard client_id keys across 3 nodes; Lua scripts are node-local (no cross-slot ops in scripts — use hash tags {client_id} to co-locate token bucket keys). Quota config: stored in etcd, watched by rate-limit service, hot-reloaded without restart. Prometheus metrics: quota_checks_total{result='allowed'|'denied'}, quota_check_latency_seconds (histogram, target p99 < 2 ms), quota_remaining (gauge per client). Load test: 20 API nodes each sending 10 k requests/s for client_id 'test' with limit 50 k/s — verify exactly 50 k allowed, remainder denied, no double-spend."
  },
 
  // ── 450 ── originally id 250 (Secure Secrets Vault)
  {
    id: 450,
    title: "Minimal Secrets Vault with Sealing, Rotation & Audit",
    cat: "Auth & Security",
    stack: "Go + BoltDB + AES-256-GCM + FastAPI (management UI) + React",
    prompt: "Build a self-contained secrets vault that handles the full lifecycle of secrets. Seal/unseal: vault starts SEALED (all data encrypted at rest with a master key that is itself encrypted under a shamir secret sharing split into 3 key shards, any 2 needed to unseal); operator provides 2 shards via POST /unseal → master key reconstructed in memory only, never written to disk. Storage: BoltDB encrypted bucket; every secret value encrypted with AES-256-GCM using the unsealed master key + a per-secret random nonce stored alongside. Secret operations: POST /secrets/{path} {value, ttl?, lease_duration} → store encrypted, return {secret_id, created_at, expires_at}; GET /secrets/{path} → decrypt, check lease, return value; DELETE /secrets/{path} → overwrite with zeros then delete. Short-lived credentials: POST /dynamic/postgres → generate a temporary PostgreSQL user (CREATEUSER, GRANT SELECT ON schema), record in leases table with expiry, background job revokes when expired. Access policies: policy files grant or deny (path_pattern, operation) per identity; enforced on every request. Audit log: every operation appended to append-only log with HMAC chain (from project 422). Re-seal: POST /seal → zero master key from memory, vault goes back to sealed state. Show: unseal with 2 shards, store secret, retrieve, rotate key (re-encrypt all secrets with new master key), re-seal, unseal again."
  },
 
  // ── 451 ── originally id 251 (Efficient Full-Page Caching)
  {
    id: 451,
    title: "Full-Page Cache with Surrogate Keys & Background Revalidation",
    cat: "Infrastructure",
    stack: "Nginx + Redis + Node + Lua (OpenResty)",
    prompt: "Implement a full-page cache that handles logged-in users, surrogate-key purging, and stale-while-revalidate without serving stale content to the wrong user. Cache bypass rules (Lua): if request has session_cookie OR Authorization header → bypass cache (never cache user-specific content). For cacheable requests: cache key = method + host + path + sorted_query_params; store in Redis as {body, headers, created_at, surrogates: []} with configurable TTL. Surrogate keys: origin sets X-Surrogate-Key: product:123 category:shoes on response; Lua stores mapping {product:123 → [key1, key2, ...]} in Redis sets. Purge API: POST /cache/purge {surrogate_key} → Redis SMEMBERS → delete all matching cache entries in one operation. Stale-while-revalidate: if cached_age > max-age but < max-age + stale-while-revalidate, serve stale immediately and enqueue a background revalidation job (Redis list); worker fetches fresh from origin, updates cache. Login detection: on POST /login success, set a bypass cookie; when user logs out, clear cookie; never serve cached content to logged-in users. Metrics: Nginx log + Lua counter: cache_hit, cache_miss, cache_bypass_auth, stale_served, revalidation_queued. Show: product update → purge product:123 surrogate → all cached pages containing that product refreshed."
  },
 
  // ── 452 ── originally id 252 (Approximate Top-K)
  {
    id: 452,
    title: "Streaming Approximate Top-K with Count-Min Sketch & HyperLogLog",
    cat: "Performance",
    stack: "Python + Redis + FastAPI + NumPy",
    prompt: "Implement Count-Min Sketch for frequency estimation and HyperLogLog for cardinality estimation from scratch and use them in a real-time analytics API. Count-Min Sketch (CMS): initialise a d×w matrix of counters (d=5 hash functions, w=10000 buckets — calculate memory: 5×10000×4 bytes = 200 KB). Update: for each item, increment CMS[i][h_i(item)] for all d hash functions. Query: frequency estimate = min(CMS[i][h_i(item)]) across all rows. Error bound: with d=5, w=10000 → error ≤ ε×N with probability 1−δ (calculate ε and δ for your parameters). Top-K extraction: maintain a min-heap of size K alongside CMS; when CMS estimate for a new item exceeds min of heap, insert it. HyperLogLog from scratch: implement HLL register array (m=4096 registers), hash function, leading-zero counter, bias correction formula. Verify: cardinality estimate error < 2% for 1 M unique items using < 5 KB. Redis integration: use Redis CMS and PFCOUNT as production-grade equivalents; compare estimates with your implementation for correctness. API: POST /events {item} → update CMS; GET /top-k?k=10 → return top-10 with frequency estimates; GET /cardinality → HLL count. Benchmark: 10 M events at 100 k/s, memory of CMS vs exact counter dict, error rate comparison."
  },
 
  // ── 453 ── originally id 253 (GraphQL Rate Limiting)
  {
    id: 453,
    title: "GraphQL Query Cost Analysis & Rate Limiting",
    cat: "API Design",
    stack: "Node + Apollo Server + Redis + TypeScript",
    prompt: "Implement per-query cost analysis that prevents expensive GraphQL queries from crashing your server, with per-client rate limits based on cost consumed (not just request count). Cost model: each field has a cost (default 1); list fields have a multiplier (default 10 × parent limit arg, capped at 100); resolver that fetches from external service costs 5; deeply nested queries compound costs. Cost calculator: walk the parsed AST before execution; for each field selection sum cost + (multiplier if list) × child_cost; if total_cost > MAX_QUERY_COST (default 1000) → reject with 400 + {error: 'query too complex', cost, max}. Per-client budget: each client gets a cost budget per minute (e.g. 5000 cost points/min); deduct actual query cost from budget (Redis DECRBY); if budget exhausted → 429. Variable depth limiting: separate from cost — reject if query depth > 7 levels. Introspection rate limit: introspection queries are expensive to execute; limit to 10/min per client. Show: (1) query allUsers { orders { items { product { reviews { author } } } } } — cost=10×10×10×10×1=10000 → rejected before execution; (2) simple query — cost=5 → allowed; (3) client burns 5000 points in 30 s → throttled for remaining 30 s. Logging: log {client_id, query_hash, cost, allowed} for every request."
  },
 
  // ── 454 ── originally id 254 (Automated Canary Analysis)
  {
    id: 454,
    title: "Automated Canary Analysis: Promote or Rollback",
    cat: "Observability",
    stack: "Python + Prometheus + FastAPI + SciPy",
    prompt: "Build an automated canary analysis service that statistically decides whether a new deployment should be promoted or rolled back. Setup: deploy two versions (baseline v1, canary v2) each with Prometheus metrics. Metrics collected per version: error_rate (HTTP 5xx / total), p99_latency_ms, p50_latency_ms, cpu_usage_percent, custom business metric (e.g. checkout_success_rate). Statistical tests: for each metric, run a two-sample t-test (latency) or proportion z-test (error rate) between baseline and canary; compute p-value and confidence interval. Scoring: each metric produces a score: PASS (p > 0.05 and canary not worse), FAIL (canary significantly worse at p < 0.05), NODATA (< 100 samples). Overall decision: PROMOTE if all critical metrics PASS + no FAIL; ROLLBACK if any critical metric FAILS; CONTINUE if borderline (< 100 samples, wait longer). Analysis controller: run analysis every 5 min during the bake window (default 30 min); after bake window, emit final verdict. Alerting: on ROLLBACK decision, call Kubernetes API to scale canary to 0 and alert on-call. Report: per-metric chart showing baseline vs canary distributions, p-value, verdict, and reasoning. Show: inject a latency regression into canary → analysis catches it at 10-min mark → triggers rollback."
  },
 
  // ── 455 ── originally id 255 (Cross-Service Transaction Monitor)
  {
    id: 455,
    title: "Distributed Transaction Monitor via Trace Joining",
    cat: "Observability",
    stack: "Python + OpenTelemetry + Jaeger API + FastAPI + PostgreSQL",
    prompt: "Detect and alert on slow or failed distributed transactions by joining traces across service boundaries. Transaction definition: a distributed transaction starts at the API gateway and spans multiple microservices; it succeeds when all participating spans have status OK; it fails if any span errors; it is slow if total duration > SLO. Trace collection: poll Jaeger every 30 s for completed traces; parse each trace into a transaction record: {trace_id, root_service, root_endpoint, participating_services[], total_duration_ms, status, error_service?, error_message?, span_count}. Storage: PostgreSQL transactions table — index on (root_endpoint, status, started_at). Slow transaction detection: p99 of total_duration_ms per root_endpoint, compared against a per-endpoint SLO config; alert if p99 > SLO for 5 consecutive scrapes. Failed transaction analysis: group failures by (error_service, error_message) to find the most common root causes; expose as GET /analysis/failures?window=1h → ranked list. Span waterfall API: GET /transactions/:trace_id/waterfall → return all spans sorted by start_time with indent level matching parent-child depth — render as ASCII waterfall in terminal or as SVG. Alert: POST to Slack webhook when new slow transaction pattern detected (new endpoint breaches SLO). Show: simulate a DB timeout in service B → all traces involving service B fail → monitor detects pattern within 2 min."
  },
 
  // ── 456 ── originally id 256 (Data Retention Manager)
  {
    id: 456,
    title: "Data Retention Policy Manager with Archival & Deletion",
    cat: "Database",
    stack: "Python + PostgreSQL + S3 (MinIO) + Celery + FastAPI",
    prompt: "Build an automated data retention system that enforces per-table policies for archival and deletion. Policy definition: retention_policies table (table_name, retention_days, archive_before_delete: bool, archive_format: jsonl|parquet, partition_column: e.g. created_at, chunk_size: rows per job). Archive job: for tables where archive_before_delete=true, query rows older than retention_days in chunks of chunk_size; serialise to JSONL or Parquet (using pyarrow); upload to S3 bucket at path {table}/{year}/{month}/{day}/chunk_{n}.parquet; verify S3 ETag matches local checksum; update archive_log. Delete job: after archive confirmed, DELETE rows in the same chunk using the partition_column range (avoid one massive DELETE that locks table); sleep 100 ms between chunks to throttle. Dry-run mode: every job has --dry-run flag that reports what would be archived/deleted without doing it. Monitoring: Celery task status tracked in Flower; per-table metrics: rows_archived, rows_deleted, archive_size_mb, last_run_at. Compliance report: monthly report showing all tables, rows archived, rows deleted, S3 archive locations — downloadable as PDF. Show: table with 1 M rows, policy = 90 days, 100 k rows older than 90 days → archive job compresses to Parquet (∼10 MB) → upload to S3 → delete job removes in 10 chunks of 10 k."
  },
 
  // ── 457 ── originally id 257 (Multi-Model DB Connector)
  {
    id: 457,
    title: "Multi-Model DB Connector: Unified API over Postgres + Mongo",
    cat: "Database",
    stack: "Node + PostgreSQL + MongoDB + Redis + Express",
    prompt: "Build a unified data access layer that transparently routes queries to the best-fit database (relational or document) based on the entity type and query pattern. Entity routing config: YAML defines which entity type lives where: orders → PostgreSQL (relational, ACID needed), product_reviews → MongoDB (flexible schema, variable attributes), sessions → Redis (TTL, key-value). Unified interface: async get(entity_type, id), find(entity_type, filters, sort, limit), create(entity_type, data), update(entity_type, id, patch), delete(entity_type, id). Each operation delegates to the correct driver. Hot views: for the most common cross-model query (e.g. order with embedded product reviews), implement a view that fetches from PostgreSQL then enriches with MongoDB data and caches the combined result in Redis for 60 s. Schema sync: a CLI tool reads MongoDB collections' schema (via schema inference on 1000 sample docs) and generates TypeScript interfaces matching the actual stored shape — fail CI if interfaces diverge from inferred schema. Query planning: if a filter touches fields from both stores, the router issues parallel queries and merges results (fan-out pattern). Show: create order (→ PG), attach 3 reviews (→ Mongo), fetch order with reviews (→ Redis hot view) — verify only 1 DB call after first fetch."
  },
 
  // ── 458 ── originally id 258 (Alert Runbook Executor)
  {
    id: 458,
    title: "Alert Runbook Executor with Auto-Remediation",
    cat: "Observability",
    stack: "Node + Slack API + Prometheus AlertManager + PostgreSQL + Docker API",
    prompt: "Connect Prometheus alerts to structured runbooks and optionally execute automated remediation, tracking every action as part of an incident record. Runbook definition: YAML per alert rule — {alert_name, severity, description, runbook_steps: [{step, type: manual|auto, command?, check?, rollback?}]}. Example runbook for HighMemoryUsage: step 1 auto → run docker exec app python -m gc (trigger GC), check memory drops 10%within 60 s; step 2 manual → prompt on-call to check for memory leak in APM; step 3 auto → restart container if memory still high after 5 min. Alert receiver: Alertmanager webhook → POST /alerts → parse alert labels, look up matching runbook, create incident record in PostgreSQL. Execution engine: for auto steps, run the command in a sandboxed subprocess with timeout; check the success condition via Prometheus query; on failure run rollback command. Slack integration: post incident card to #incidents channel with alert details + runbook steps + current status; for manual steps, post interactive buttons (Done / Escalate / Skip); update card as steps complete. Audit trail: every auto-execution logged with {timestamp, command, exit_code, stdout, stderr, duration_ms} linked to incident. Show: trigger a high-memory alert → auto-step executes GC → memory drops → incident auto-resolved → Slack card shows 'Auto-resolved in 47 s'."
  },
 
  // ── 459 ── originally id 259 (Adaptive Circuit Breaker)
  {
    id: 459,
    title: "Adaptive Circuit Breaker with Dynamic Thresholds",
    cat: "Distributed Systems",
    stack: "Node + Redis + TypeScript + FastAPI (upstream mock)",
    prompt: "Build a circuit breaker whose failure and latency thresholds adapt to recent behaviour, avoiding both false trips during transient spikes and failure to trip during genuine degradation. Adaptive threshold computation: every 60 s, compute baseline from last 24 h of data — baseline_error_rate = P90(hourly_error_rates), baseline_p99_latency = P90(hourly_p99_latencies). Trip conditions: trip if current_error_rate > baseline_error_rate × 2.0 AND current_error_rate > 5% (absolute floor) — prevents tripping if baseline is already high; OR if current_p99 > baseline_p99 × 3.0. Recovery thresholds: half-open probes at 10% traffic; close if error_rate < baseline_error_rate × 1.2 for 30 s. State machine: CLOSED, OPEN (trips immediately, backs off with exponential delay before half-open), HALF_OPEN. Redis storage: rolling 60-s error/latency windows using sorted sets (timestamp → error_bool or latency_ms); expire entries older than 60 s. Comparison mode: run adaptive alongside a fixed-threshold breaker (5% error threshold); simulate a traffic spike causing a brief error burst — show fixed breaker trips unnecessarily while adaptive does not; then simulate real degradation — both trip, but adaptive trips 20% faster because the relative jump is larger. Metrics: circuit_state (0/1/2), error_rate, p99_latency, baseline_error_rate, trip_count."
  },
 
  // ── 460 ── originally id 260 (Vector Search Playground)
  {
    id: 460,
    title: "Vector Search Playground: FAISS Indexing & Recall/Latency Tradeoffs",
    cat: "Database",
    stack: "Python + FAISS + FastAPI + NumPy + Matplotlib",
    prompt: "Build an interactive vector search lab that shows how different FAISS index types trade recall for latency and memory. Dataset: embed 100 k short text snippets using sentence-transformers (or precomputed 128-d vectors). Index types: (1) IndexFlatL2 — exact search, 100% recall, O(n) scan. (2) IndexIVFFlat — inverted file with nlist=256 clusters, approximate (set nprobe to tune recall vs speed). (3) IndexIVFPQ — product quantisation, compressed storage (32× smaller), lower recall. (4) IndexHNSWFlat — graph-based ANN, high recall + fast query. (5) IndexIVFSQ — scalar quantisation, balance between PQ and Flat. For each index: build time (s), index size (MB), search latency p50/p99 at 1000 queries (ms), recall@10 (fraction of true top-10 from Flat that appear in results). Tune nprobe for IVF indexes: plot recall vs latency curve. API: POST /index {index_type, params} → build index; POST /search {query_vector, k, index_type} → return k nearest neighbours with distances and build stats. Playground mode: upload a custom vector dataset (CSV), rebuild all index types, compare. Show: IVFFlat at nprobe=10 → recall=92%, latency=2 ms; nprobe=50 → recall=99%, latency=8 ms — tradeoff curve plotted."
  },
 
  // ── 461 ── originally id 261 (DB Connection Diagnosis)
  {
    id: 461,
    title: "Database Connection Leak Detector & Pool Diagnostics",
    cat: "Database",
    stack: "Python + PostgreSQL + SQLAlchemy + FastAPI + Prometheus",
    prompt: "Build a diagnostic system that detects connection leaks, pool exhaustion events, and long-running connections in real time. Leak detector: monkey-patch SQLAlchemy's checkout/checkin events; track {connection_id, checked_out_at, stack_trace, request_id} in an in-memory dict; a background thread every 30 s checks for connections checked out > 60 s → log LEAK with stack trace; threshold configurable. Pool exhaustion: listen for PoolExhausted events; record timestamp, wait_duration, request_id, endpoint; if wait_duration > 100 ms emit a Prometheus counter connection_pool_exhaustion_total{endpoint}. Diagnostic endpoint: GET /debug/db — returns: {pool_size, checked_out, available, overflow, pending_waiters, oldest_checkout_age_s, top_5_longest_held_connections: [{connection_id, age_s, stack_trace_snippet}]}. Query longrunner detection: every 60 s query pg_stat_activity WHERE state='active' AND query_start < now() - interval '30 seconds'; log and emit metric for each long-running query with its query_text (truncated) and pid. Connection recycling: configure SQLAlchemy pool_recycle=1800 (recycle connections older than 30 min) to avoid stale connection errors; log each recycle event. Show: deliberately leak 5 connections by not closing cursor; leak detector fires after 60 s with stack traces pointing to the leaking code."
  },
 
  // ── 462 ── originally id 262 (Realtime Collaboration Presence)
  {
    id: 462,
    title: "Real-Time Collaboration Presence System",
    cat: "Messaging",
    stack: "Node + WebSocket + Redis + React",
    prompt: "Build a presence system for a collaborative app that tracks who is online, what they are viewing, and handles reconnection gracefully without ghost users. Presence model: when a client connects via WebSocket, write presence entry to Redis: HSET presence:{room_id} {user_id} {user_name, avatar, cursor_position, last_seen_ms, connection_id} with EXPIRE of 30 s (heartbeat keeps it alive). Heartbeat: client sends {type: 'heartbeat', cursor_position} every 10 s; server updates last_seen_ms and resets TTL; if no heartbeat for 30 s → Redis key expires → user appears offline. Presence broadcast: on join/leave/heartbeat, publish to Redis Pub/Sub channel room:{room_id}:presence; all server nodes subscribed push updated presence list to their connected clients. Reconnection: client stores its connection_id (UUID) in localStorage; on reconnect, sends {type: 'reconnect', connection_id, last_event_seq}; server checks if old presence entry exists — if yes, soft-reconnect (no flash of 'user left'). Cursor sharing: heartbeat payload includes cursor_position {x, y, element_id}; server broadcasts cursor updates to other users in room (throttled to 50 ms minimum interval). UI (React): avatar stack showing online users; each user's cursor rendered as a coloured pointer on the shared canvas; 'typing...' indicator (user sends {type: 'typing', true/false}). Show: 5 users in a room, one disconnects (network off) → after 30 s they disappear from presence list, not immediately."
  },
 
  // ── 463 ── originally id 263 (ML Feature Store)
  {
    id: 463,
    title: "Online/Offline Feature Store with Training & Serving Split",
    cat: "Database",
    stack: "Python + Redis + Parquet + DuckDB + FastAPI",
    prompt: "Build a dual-store feature store that separates training data (offline) from low-latency serving features (online) while keeping them consistent. Offline store: DuckDB over Parquet files on disk; features stored as time-stamped snapshots (user_id, feature_name, value, valid_at, created_at); point-in-time correct join — for any training label at time T, retrieve feature values that were valid at T (no future leakage). Online store: Redis HSET user:{user_id}:features {feature_name: value}; TTL = 24 h (re-computed by nightly batch). Feature ingestion pipeline: batch job reads from source DB, computes features (avg_order_value_30d, days_since_last_login, top_category), writes to both offline Parquet and online Redis. Training dataset generation: POST /dataset {label_table, feature_list, from_date, to_date} → join label table with offline store using point-in-time logic → export as Parquet for model training. Serving: GET /features/{user_id}?features=avg_order_value_30d,top_category → reads from Redis (<5 ms); if key missing → cold-start fallback computes from DB, caches in Redis. Training-serving skew detection: periodically sample online Redis values and compare with offline Parquet for same user/feature/time → alert if mean difference > threshold. Show: train model with offline features (no leakage), serve predictions using online features, detect skew when batch job is delayed."
  },
 
  // ── 464 ── originally id 264 (Query Result Cache)
  {
    id: 464,
    title: "Intelligent Query Result Cache with Tag-Based Invalidation",
    cat: "Performance",
    stack: "Node + Redis + PostgreSQL + Express",
    prompt: "Build a smart caching layer for API queries that goes beyond simple TTL: support tag-based invalidation, stale-while-revalidate, and per-endpoint cache policies. Cache entry structure: Redis HASH {body, created_at, tags: [product:123, category:shoes], ttl, etag}. Tag index: for each tag, maintain a Redis SET cache_tag:{tag} → {cache_key_1, cache_key_2, ...}. Cache key: normalised URL + query-param hash + relevant request headers (Accept-Language for localised responses). Per-endpoint policy config (YAML): GET /products/:id → ttl=300s, stale_while_revalidate=60s, tags=[product:{id}]; GET /products → ttl=60s, tags=[product_list]; POST/PUT/DELETE /products/:id → invalidate tags [product:{id}, product_list]. Invalidation on write: middleware intercepts mutation responses; on success, reads invalidation policy, calls SMEMBERS on each tag's SET, DEL all returned cache keys, cleans up tag SETs. Stale-while-revalidate: if cache_age > ttl but < ttl + stale_while_revalidate, serve stale immediately and enqueue async revalidation to a background queue. Cache-Control pass-through: respect upstream Cache-Control: no-cache (bypass cache), no-store (don't cache response). Metrics: cache_hit_ratio per endpoint, stale_served_count, invalidation_count_per_tag. Show: update product 123 → GET /products/123 cache invalidated → GET /products (list) also invalidated — 2 DEL operations for 1 write."
  },
 
  // ── 465 ── originally id 265 (Hierarchical Rate Limits)
  {
    id: 465,
    title: "Hierarchical Rate Limits: User → Org → Global",
    cat: "Performance",
    stack: "Go + Redis + gRPC",
    prompt: "Implement rate limits that apply at multiple hierarchical scopes simultaneously — individual user, organisation, and global — with proper quota deduction at each level and clear precedence rules. Hierarchy: request consumes quota from all three levels (user AND org AND global must have remaining quota). Quota deduction: atomic Lua script in Redis — decrement user_quota, org_quota, global_quota in one call; if any is exhausted, increment all back (rollback) and return denied. Scope definitions: Global: 1 M req/min across all users; Org: configured per org (e.g. Org A: 50 k req/min); User: configured per user (e.g. free tier: 100 req/min, pro: 1000 req/min). Soft vs hard limits: soft limit triggers warning header X-RateLimit-Warning: approaching-limit at 80%; hard limit returns 429. Quota inheritance: if user has no explicit limit, inherit from their org's per-user-default. Quota sharing: org admin can redistribute quota between users (transfer 200 req/min from UserA to UserB within org's budget). Burst handling: user has base rate + burst pool (e.g. 100 req/min + burst of 50 req that refills at 10 req/min). gRPC API: CheckQuota(user_id, org_id, cost) → AllowResult; GetQuotaStatus(user_id) → {user_remaining, org_remaining, global_remaining, next_reset_at}. Show: user at 100 req/min exhausts personal quota → 429; org still has quota → doesn't help individual user; org quota exhausted → all users in org get 429."
  },
 
  // ── 466 ── originally id 266 (Service Mocking Platform)
  {
    id: 466,
    title: "Service Mocking Platform for Integration Tests",
    cat: "Infrastructure",
    stack: "Node + Express + Docker + React",
    prompt: "Build a lightweight service mocking platform where teams register mock HTTP endpoints and behaviour, replacing real downstream services in integration test environments. Mock registration: POST /mocks → {service_name, method, path_pattern, response_status, response_body, response_delay_ms?, fault_mode?: 'timeout'|'connection_reset'|'random_5xx'}; stored in-memory (per-session) and optionally persisted to PostgreSQL for shared environments. Path matching: support exact match, wildcard (/users/*), and regex patterns; precedence: exact > wildcard > regex. Request recording: every call to a registered mock records {timestamp, method, path, request_headers, request_body, matched_mock_id, response_sent} in an in-memory ring buffer (last 1000 calls per service). Assertion API: GET /mocks/{service_name}/calls → list recorded calls; POST /mocks/{service_name}/assert → {method, path, times_called: {min, max}, request_body_contains: {}} → returns PASS/FAIL (enables test assertions without accessing the real service). Fault injection: set fault_mode=random_5xx with fault_rate=0.2 → 20% of requests return 500 → test client's resilience. Docker sidecar: package the mock server as a Docker image; teams add it to docker-compose alongside their service under test. UI (React): live feed of incoming mock calls, mock configuration form, assertion builder. Show: configure mock for payment service, run integration test, assert payment mock was called exactly once with correct request body."
  },
 
  // ── 467 ── originally id 267 (Privacy-Preserving Analytics)
  {
    id: 467,
    title: "Privacy-Preserving Analytics with Differential Privacy",
    cat: "Auth & Security",
    stack: "Python + Google Differential Privacy library (or manual DP) + FastAPI",
    prompt: "Implement differential privacy mechanisms over aggregated metrics and rigorously evaluate noise vs utility tradeoffs. Core mechanisms: (1) Laplace mechanism: add Laplace noise calibrated to sensitivity/ε for numeric queries (COUNT, SUM, MEAN); sensitivity of COUNT = 1, SUM = max_value. (2) Gaussian mechanism: add Gaussian noise for (ε, δ)-DP; appropriate for high-dimensional queries. (3) Randomised response: for categorical data, flip each response with probability p = 1/(e^ε + 1) — each individual can plausibly deny their true value. Dataset: 10 k users with {age, income, has_condition} attributes. Queries: (a) COUNT of users with income > 50 k; (b) MEAN age of users with has_condition=true; (c) histogram of income buckets. For each query at ε ∈ {0.1, 0.5, 1.0, 2.0, 10.0}: add DP noise, run 1000 trials, compute mean absolute error and 95% CI. Show: at ε=0.1 (strong privacy), COUNT error ∼ ±50; at ε=10 (weak privacy), error ∼ ±0.5. Privacy budget tracking: track cumulative ε spent across all queries on the same dataset (composition theorem); alert when budget exhausted. API: POST /query {type, filters, epsilon} → returns noisy result + confidence interval + privacy_cost. Demonstrate: single user's has_condition cannot be inferred even from 100 repeated queries at ε=1.0."
  },
 
  // ── 468 ── originally id 268 (Data Quality Checks)
  {
    id: 468,
    title: "Automated Data Quality Checks with Alerting Dashboard",
    cat: "Database",
    stack: "Python + Great Expectations + PostgreSQL + FastAPI + React",
    prompt: "Build an automated data quality monitoring system that runs expectations on every data load and surfaces failures to an alert dashboard. Expectation suite setup: define suites per table — orders table expectations: expect_column_values_to_not_be_null(order_id), expect_column_values_to_be_between(total_amount, 0, 1000000), expect_column_values_to_match_regex(email, r'^[\\w.+-]+@[\\w-]+\\.[\\w.]+$'), expect_table_row_count_to_be_between(min=1000, max=10000000), expect_column_pair_values_A_to_be_greater_than_B(shipped_at, ordered_at). Checkpoint runner: triggered after every ETL load via Celery; runs Great Expectations checkpoint; persists results (table, suite, run_id, passed, failed_expectations: [{expectation, column, unexpected_count, unexpected_percent}], run_at) to PostgreSQL. Alert rules: if failed_expectations contains any with unexpected_percent > 1% AND column in critical_columns → send Slack alert with details; if row_count expectation fails → P1 alert (possible load failure). Dashboard (React): timeline of quality runs per table (green/yellow/red dots); click a run → see all expectations with pass/fail status + unexpected value samples; trend chart of unexpected_percent per column over 30 days (detect gradual degradation). Data docs: auto-generate Great Expectations HTML data docs and serve from /docs endpoint. Show: inject 5% null order_ids into a load → checkpoint fails → alert fires → dashboard shows the failure with sample rows."
  },
 
  // ── 469 ── originally id 269 (Multi-Protocol API Gateway)
  {
    id: 469,
    title: "Multi-Protocol API Gateway: HTTP/1.1, HTTP/2, gRPC",
    cat: "API Design",
    stack: "Node + Envoy (Docker) + Python gRPC service + Express REST service",
    prompt: "Build a gateway that accepts HTTP/1.1, HTTP/2, and gRPC requests from clients and routes to appropriate backend services with unified auth and observability. Gateway layer (Envoy): configure listeners for port 443 (HTTP/1.1 + HTTP/2 via ALPN), port 8080 (gRPC/HTTP2). Route config: /api/v1/* → REST backend (Node Express); /grpc.* → gRPC backend (Python); /api/v2/* → gRPC backend (with transcoding: HTTP/JSON ↔ gRPC Protobuf via Envoy's grpc_json_transcoder). Auth filter: Envoy ext_authz filter calls a Python auth service for every request — validates JWT, extracts claims, injects x-user-id and x-user-role headers for backends. gRPC transcoding: define proto file for UserService; Envoy transcodes GET /api/v2/users/{id} → gRPC GetUser(GetUserRequest{id}) — backends only speak gRPC, REST clients are none the wiser. Observability: Envoy access logs → stdout (structured JSON); Prometheus metrics via /stats/prometheus (request_count, latency histogram, upstream_cx per backend). HTTP/2 push: configure a demo endpoint where server pushes a CSS resource alongside an HTML response (show push promise in browser DevTools). Show: curl REST endpoint → Envoy routes to Node; grpcurl gRPC endpoint → routes to Python; curl /api/v2 JSON endpoint → Envoy transcodes to gRPC, returns JSON. Verify auth filter blocks unauthenticated requests to all three."
  },
 
  // ── 470 ── originally id 270 (Edge Compute Scheduler)
  {
    id: 470,
    title: "Edge Compute Function Scheduler",
    cat: "Infrastructure",
    stack: "Go + Docker + Redis + FastAPI",
    prompt: "Build a scheduler that places lightweight compute functions close to users by selecting the optimal edge node based on latency and resource constraints. Edge nodes: simulate 5 nodes in different regions (us-east, us-west, eu-west, ap-south, ap-east) each running as a Docker container with a fake resource profile (cpu_cores, memory_mb, current_load). Function registration: POST /functions {name, image, cpu_required, memory_required, max_latency_ms, affinity_regions?} → stored in Redis. Request routing: POST /invoke {function_name, client_region} → scheduler selects best node. Selection algorithm: filter to nodes with affinity_regions match (or all if not set), then filter to nodes with available resources (cpu_cores - current_load_cpu >= cpu_required), then rank remaining by simulated latency (latency_table[client_region][node_region]) and pick lowest; on tie, pick node with lowest current_load. Execution: scheduler sends invoke request to selected node's Docker API (docker run --rm --cpus --memory), streams stdout back to caller. Autoplacement: if no node in affinity_regions can satisfy resource requirements, fall back to nearest node with capacity (log a placement_override event). Metrics: invocation_count per node, placement_failure_count, mean_latency_ms by client_region. Show: invoke a function from 'ap-east' client → scheduler picks ap-east or ap-south node → latency ∼ 10 ms vs us-east ∼ 150 ms."
  },
 
  // ── 471 ── originally id 271 (Schema Evolution Tools)
  {
    id: 471,
    title: "Schema Evolution Compatibility Checker for Avro & Protobuf",
    cat: "Database",
    stack: "Python + Avro + Protobuf + FastAPI + React",
    prompt: "Build a tool that automatically checks whether a new schema version is compatible with existing producers/consumers and simulates all evolution scenarios. Supported formats: Avro (full/backward/forward compatibility) and Protobuf (field-number-based evolution). Compatibility rules implemented: BACKWARD — new schema can read data written with old schema (consumer upgrades first); FORWARD — old schema can read data written with new schema (producer upgrades first); FULL — both directions work. Compatibility checker engine: for Avro, implement the comparison logic manually (field added with default → backward compatible; field removed → forward compatible; field renamed → breaking; type widened int→long → compatible; type narrowed → breaking). For Protobuf: field number unchanged = compatible; removed field (but not reserved) = forward compatible with risk; changed type for same field number = breaking. Scenario simulator: POST /check {old_schema, new_schema, format, compatibility_level} → COMPATIBLE/BREAKING with per-field reasoning. Migration playbook generator: for each detected breaking change, generate a migration plan (e.g. two-phase: add new field alongside old, deprecate old, remove after migration window). Versioned schema registry: store schema versions with compatibility history; visualise compatibility matrix (version N vs N-1, N-2, N-3). CI integration: GitHub Action that runs compatibility check on every schema file change — fails PR if BREAKING. Show: add a required field to Avro schema → tool flags BREAKING, suggests adding default value to make it backward compatible."
  },
 
  // ── 472 ── originally id 272 (Progressive Profiling)
  {
    id: 472,
    title: "Production Continuous Profiling & Regression Detection",
    cat: "Performance",
    stack: "Node + Clinic.js / 0x + Pyroscope client + FastAPI + React",
    prompt: "Build a system that collects low-overhead CPU profiles from production services, aggregates them into flame graphs, and automatically alerts on performance regressions between deploys. Profiler agent: Node.js service uses 0x (or clinic flame) in sampling mode (1 ms sample interval, < 1% CPU overhead); samples collected for 60-s windows, serialised as folded stack format, sent to profiling server via HTTP. Profiling server: receives folded stacks, stores in PostgreSQL (profile_id, service, version, git_sha, collected_at, folded_stacks_gz). Flame graph renderer: convert folded stacks to d3-flame-graph format; serve interactive flame graph at GET /profiles/:id. Baseline comparison: on each deploy, tag profiles with git_sha; after 10 min of new-version traffic, compare new-version flame graph against previous version's. Regression detection: compare top-20 hottest functions by self_time_percent; if any function increased by > 20% of total CPU → flag as regression; alert to Slack with before/after flame graph links. Continuous profiling mode (Pyroscope-compatible): instead of windows, ingest continuous 10 s samples, aggregate with sliding window; enable drill-down by time range. Show: deploy a function with an accidentally quadratic loop → profiling server detects it in next flame graph → regression alert fires with the specific function name highlighted."
  },
 
  // ── 473 ── originally id 273 (Feature Usage Heatmap)
  {
    id: 473,
    title: "Feature Usage Heatmap & Engagement Analytics",
    cat: "Observability",
    stack: "React + D3.js + Node + PostgreSQL + ClickHouse",
    prompt: "Build an analytics system that visualises which features are used across the app, helping product teams find underused areas and prioritise improvement. Event tracking: frontend SDK emits {user_id, session_id, event_type: 'feature_used', feature_name, page, timestamp_ms} events on every significant interaction (button click, form submit, section expand); events batched and sent to POST /events every 30 s or on page unload. Ingestion: Node receiver buffers in Redis, batch-inserts into ClickHouse every 5 s. Analytics queries (ClickHouse): (a) feature usage count and unique user count per feature per day; (b) funnel: % of users who used Feature A who also used Feature B; (c) retention by feature: users who used Feature X on day 0 — what % return on day 7?; (d) session depth: average number of features used per session by user cohort. Heatmap visualisation (React + D3): sitemap-style treemap where each node is a feature/page, sized by usage count, coloured by engagement score (dark green = highly used, red = barely used); click to drill into daily usage chart. Unused feature detector: features with < 0.5% of DAU in past 30 days → flagged as 'low engagement' in a report. Show: identify 3 features with < 1% usage → product team decides to deprecate → feature is removed → next report shows its entry gone from heatmap."
  },
 
  // ── 474 ── originally id 274 (Replayable Test Harness)
  {
    id: 474,
    title: "Production Traffic Replay Test Harness",
    cat: "Infrastructure",
    stack: "Node + Playwright + PostgreSQL + Express",
    prompt: "Build a system that records real production user flows and replays them deterministically in CI to catch regressions before they reach users. Traffic recorder: a middleware on the API server that captures (with sampling rate 1%) full HTTP request/response pairs — {timestamp, method, path, query_params, request_headers_sanitised, request_body_sanitised, response_status, response_body_schema_hash, duration_ms} — PII fields replaced with pseudonyms via a field-config map. Flow construction: group requests by session_id within a 30-min window; order by timestamp; stitch into a replay flow YAML file {flow_id, steps: [{delay_ms_from_prev, ...request_fields}]}. Replay engine (Node): read flow YAML, execute steps sequentially with the original delay_ms (or compressed at 10× speed); compare response status + response_body_schema_hash against recorded baseline; flag any step where status changed or schema hash changed. Playwright integration: for UI-touching flows, replay via Playwright browser automation; take screenshots at each step; diff screenshots against baseline (pixel diff < 1%). CI integration: on each PR, run the top-100 most-used recorded flows against the PR's deployed preview environment; fail build if > 5% of flows produce schema-hash mismatches. Show: break an API response schema in a PR → replay test fails on 23 of 100 flows → build blocked → schema fix applied → flows pass."
  },
 
  // ── 475 ── originally id 275 (Adaptive Query Planner)
  {
    id: 475,
    title: "Adaptive Query Planner Simulation",
    cat: "Database",
    stack: "Python + SQLite + FastAPI",
    prompt: "Simulate an adaptive query planner that learns from runtime statistics to choose better execution strategies. Query model: simple SQL subset — SELECT ... FROM table WHERE col op value [AND ...] [ORDER BY col] [LIMIT n]. Execution strategies: (1) Full scan — iterate all rows, filter in Python. (2) Index scan — binary search on a sorted index list. (3) Hash join — build hash map for one table, probe with other. (4) Nested loop join — for each row in left, scan right. Cost model: initial cost estimates based on table statistics (row count, column cardinality, index existence). Adaptive feedback: after each query execution, record actual_rows_scanned, actual_time_ms; update a statistics store (column value histograms, join cardinality estimates). Plan reuse: cache (query_template → plan) with staleness check — replan if underlying stats changed > 20%. Adaptive join reordering: if initial plan chose nested loop but actual cardinality was 100× higher than estimated → switch to hash join for similar queries. Show: run 1000 queries; first 100 use default plan (often suboptimal); by query 500 the planner has collected enough runtime stats to choose better plans; measure mean execution time of first 100 vs last 100. Visualise: plan chosen per query over time (scatter plot), highlighting plan switches."
  },
 
  // ── 476 ── originally id 276 (Zero-Trust Gateway)
  {
    id: 476,
    title: "Zero-Trust Gateway with mTLS and Short-Lived Certs",
    cat: "Auth & Security",
    stack: "Go + mTLS + FastAPI + cfssl or step-ca",
    prompt: "Implement a zero-trust API gateway where every service must present a valid, short-lived client certificate for every request — no implicit network trust. Certificate authority: set up a mini CA using cfssl or step-ca (self-signed root for the demo); issue service certificates with 24-h validity (CommonName = service name, SAN = service SPIFFE URI spiffe://cluster/ns/default/sa/service-name). mTLS enforcement: Go gateway uses tls.Config{ClientAuth: tls.RequireAndVerifyClientCert, ClientCAs: ca_cert_pool}; rejects any request without a valid client cert signed by the CA. Per-service trust policy: YAML file maps (caller_service, target_service) → allowed_paths pattern; gateway checks after cert validation — even a valid cert doesn't grant access to all paths. Short-lived cert rotation: each service runs a sidecar that calls step-ca ACME endpoint every 12 h to renew its cert; gateway reloads CRL/OCSP on each request (or uses a short TTL so CRL is rarely needed). Audit log: every request logged with {timestamp, caller_cn, target_service, path, allowed: bool, cert_expiry}. SPIFFE simulation: encode workload identity as SPIFFE URI in cert SAN; gateway extracts and logs workload identity. Show: service A with valid cert calls service B's allowed path → succeeds; service A tries service B's denied path → 403; service C with expired cert → TLS handshake rejected; no cert → TLS rejected."
  },
 
  // ── 477 ── originally id 277 (Hybrid Cloud Data Sync)
  {
    id: 477,
    title: "Hybrid Cloud Data Sync: S3 ↔ GCS with Conflict Resolution",
    cat: "Infrastructure",
    stack: "Node + AWS S3 (MinIO) + GCS (fake-gcs-server) + PostgreSQL",
    prompt: "Build a bidirectional data sync engine between two cloud object stores with bandwidth-aware scheduling and conflict resolution. Object model: each object tracked in PostgreSQL sync_state table — {key, s3_etag, gcs_etag, s3_modified_at, gcs_modified_at, sync_status: SYNCED|S3_NEWER|GCS_NEWER|CONFLICT, last_sync_at}. Change detection: poll both stores every 5 min via list-all-objects API; compare ETags and last-modified against sync_state; classify each object into a sync action. Conflict detection: object modified in both stores since last sync → CONFLICT status; resolution strategies configurable per bucket prefix: S3_WINS, GCS_WINS, NEWEST_WINS, or MANUAL_REVIEW (write to conflict_queue table for operator inspection). Bandwidth scheduling: configurable sync_window (e.g. 02:00–06:00 UTC) for large transfers; estimate transfer size (sum of S3_NEWER + GCS_NEWER object sizes); if estimated_transfer_gb > daily_budget_gb → defer large objects, sync small ones first. Checksum verification: after copy, re-download MD5 and compare to source ETag — retry if mismatch. Metrics: objects_synced_total, bytes_transferred_total, conflicts_detected_total, sync_lag_seconds (max(s3_modified_at - last_sync_at) across objects). Show: create object in MinIO (S3) → sync job copies to GCS within 5 min → verify ETags match; create conflict → resolution policy applies correctly."
  },
 
  // ── 478 ── originally id 278 (Adaptive Sampling for Logs)
  {
    id: 478,
    title: "Adaptive Log Sampling with Error-Rate Triggers",
    cat: "Observability",
    stack: "Node + Loki + Prometheus + FastAPI",
    prompt: "Build an adaptive log sampling system that dynamically adjusts sampling rates to control cost while always preserving error signals regardless of budget. Sampling configuration: per-service, per-level sampling rates stored in Redis — {service: 'api', DEBUG: 0.01, INFO: 0.05, WARN: 0.5, ERROR: 1.0}. Log emitter middleware: for each log line, perform sampling decision using deterministic hashing (hash(trace_id + level) % 100 < threshold × 100) — same trace_id always makes the same decision (important: all lines from an erroring trace are sampled together). Error-rate trigger: Prometheus scrape every 30 s; if error_rate > 2% for a service → escalate INFO to 1.0 and DEBUG to 0.1 (write to Redis) → captures full context during incident; when error_rate < 0.5% for 5 consecutive minutes → restore base rates. Sample-rate metadata: every log line includes sample_rate field; Loki label sample_rate set per log stream. Downstream correction: analytics queries divide count by sample_rate to estimate true count (COUNT(*) / AVG(sample_rate) for sampled streams). Show: cost dashboard — base sampling saves 85% of log volume; during error spike, sampling escalates → Loki receives 100% of error logs → 20% of INFO → after recovery → back to 5%. Demonstrate: without correction, sampled COUNT underestimates true count by 20×; with correction, estimate is within 3%."
  },
 
  // ── 479 ── originally id 279 (GraphQL Subscriptions Hub)
  {
    id: 479,
    title: "Scalable GraphQL Subscriptions Hub with Redis Fanout",
    cat: "API Design",
    stack: "Node + Apollo Server + WebSocket + Redis + TypeScript",
    prompt: "Build a multi-node GraphQL subscriptions system where any server can publish events and all subscribed clients across all nodes receive them. Subscription definitions: schema includes Subscription type — onOrderUpdated(user_id: ID!): Order, onInventoryChange(product_id: ID!): InventoryEvent, onGlobalAnnouncement: Announcement. PubSub backend: replace Apollo's in-memory PubSub with a Redis PubSub adapter (ioredis SUBSCRIBE/PUBLISH); channel naming convention: subscription:{subscription_name}:{filter_key} (e.g. subscription:onOrderUpdated:user_123). Client connection: WebSocket handshake per client; server stores {ws_connection_id, subscriptions: [{subscription_name, filter_key, subscription_id}]}. Publish flow: mutation resolves → server publishes to Redis channel → all server nodes receive → each server filters its own connected clients → pushes to matching WebSocket connections. Presence tracking: server updates a Redis HSET of connected subscription counts per channel; used to decide whether to publish (skip if no subscribers). Client filtering: for onOrderUpdated, filter at subscription resolver: only push if event.user_id matches the subscription argument. Backpressure: if WebSocket send buffer is full, drop the oldest pending message and emit a dropped_message metric. Show: 3 server nodes, 10 clients spread across them, publish onOrderUpdated for user_123 from node 1 → only the client on node 3 subscribed to user_123 receives it."
  },
 
  // ── 480 ── originally id 280 (Data Lake Partitioning)
  {
    id: 480,
    title: "Data Lake Partitioning Strategy Benchmark",
    cat: "Database",
    stack: "Python + PySpark + Parquet + DuckDB + Matplotlib",
    prompt: "Experiment with different data lake partitioning schemes on a large event dataset and measure query performance tradeoffs for common analytic patterns. Dataset: generate 1 B synthetic events (user_id, event_type, region, timestamp, value) written as Parquet files. Partitioning schemes to compare: (1) No partitioning — single large Parquet file. (2) Date partitioning — Hive-style /year=2024/month=01/day=15/. (3) Region + date partitioning — /region=us-east/year=2024/month=01/. (4) Hash partitioning — /bucket=42/ (user_id % 256). (5) Z-ordering (Delta Lake concept) — co-locate data sorted by multiple columns simultaneously (implement as sorted Parquet files by (region, timestamp)). Benchmark queries (using DuckDB for fast local analytics): Q1: SELECT COUNT(*) WHERE date='2024-01-15' (date-filtered); Q2: SELECT SUM(value) WHERE region='us-east' AND date BETWEEN ... (multi-predicate); Q3: SELECT * WHERE user_id=12345 (point lookup); Q4: GROUP BY event_type (full scan). For each scheme × query: measure files_scanned (partition pruning efficacy), bytes_read, query_time_s. Show: date partitioning reduces Q1 from scanning 1 B rows to 3 M; hash partitioning makes Q3 fast but Q1/Q2 slow; Z-ordering helps Q2 most. Plot: 5×4 heatmap of relative performance."
  },
 
  // ── 481 ── originally id 281 (Automated DB Index Advisor)
  {
    id: 481,
    title: "Automated DB Index Advisor with Impact Estimation",
    cat: "Database",
    stack: "Python + PostgreSQL + FastAPI + Rich (CLI)",
    prompt: "Build a tool that analyses slow queries from pg_stat_statements, identifies missing indexes, and estimates their impact before you create them. Step 1 — Query collection: connect to PostgreSQL, read pg_stat_statements (top 50 by total_time); normalise query templates (strip literals). Step 2 — EXPLAIN analysis: run EXPLAIN (FORMAT JSON) on each template with representative sample values; parse JSON plan tree; detect: Seq Scan on tables > 10 k rows (potential index needed), Nested Loop with high inner_rows_estimate (possible missing join index), Sort on non-indexed columns with large row count. Step 3 — Index candidate generation: for each Seq Scan node, extract filter columns; propose CREATE INDEX CONCURRENTLY ON table (columns); for composite filters (a AND b) propose composite index. Step 4 — Impact estimation: use pg_stats (n_distinct, correlation) + row count to estimate new index cost (% reduction in rows scanned); simulate with EXPLAIN (hypothetical index using hypopg extension — CREATE HYPOTHETICAL INDEX); compare actual_cost before vs after. Step 5 — Report: table sorted by estimated_speedup × query_frequency; show: query template, current plan cost, estimated plan cost with index, index DDL to create, storage cost estimate. Safety checks: flag indexes that duplicate existing ones. Show: find 3 missing indexes, estimate 10×/5×/2× speedup, create the best one, re-run EXPLAIN to confirm."
  },
 
  // ── 482 ── originally id 282 (Resilient Pub/Sub with Backpressure)
  {
    id: 482,
    title: "Resilient Pub/Sub with Consumer Backpressure & Flow Control",
    cat: "Messaging",
    stack: "Go + NATS + Prometheus",
    prompt: "Build a pub/sub system on NATS that prevents slow consumers from causing OOM or starvation of other consumers. Setup: NATS server (Docker); Go publisher and 3 consumers. Backpressure mechanism: use NATS JetStream's pull-based consumer — consumer requests N messages at a time (FetchBatch), processes them, ACKs, then requests more; if processing is slow it simply doesn't request more. Flow control per consumer: track in-flight message count per consumer; if in_flight > max_in_flight (configurable, e.g. 1000) → pause fetching (wait for ACKs). Multi-topic flow control: one slow consumer on topic A should not block consumers on topic B — verify via separate JetStream streams. Slow consumer detection: Prometheus gauge nats_consumer_pending_messages; alert if any consumer's pending > 10 000. Consumer crash recovery: JetStream retains unACKed messages; on restart, consumer re-fetches from last ACK position (at-least-once delivery). Message TTL: messages not ACKed within 60 s (MaxAckPending timeout) are re-delivered (configure MaxDeliver=3 then send to dead-letter subject). OOM simulation: start a consumer that sleeps 10 s per message (simulates slow DB write); verify publisher keeps publishing and other consumers are unaffected; slow consumer's pending grows but is bounded by max_in_flight. Show: in-flight limit prevents OOM; fast consumer processes at 1000 msg/s unaffected by slow consumer at 6 msg/s."
  },
 
  // ── 483 ── originally id 283 (Feature Flag SDKs)
  {
    id: 483,
    title: "Lightweight Feature Flag SDKs with Local Cache & Offline Safety",
    cat: "Infrastructure",
    stack: "TypeScript + Python + Redis + FastAPI",
    prompt: "Build production-grade feature flag SDKs for TypeScript and Python that evaluate flags locally with < 1 ms latency and behave safely when the flag server is unreachable. Flag server (FastAPI): stores flags in PostgreSQL (id, name, type: boolean/percentage/segment, rules JSONB, version); exposes GET /flags (returns all flags + version hash); SSE endpoint /flags/stream that pushes updates when flags change. TypeScript SDK: on init, fetch all flags, store in Map; subscribe to SSE stream for live updates; evaluate locally (no network call per evaluation); if SSE disconnects, poll every 30 s. evaluate(flagName, userContext) → applies rules: boolean (on/off), percentage (deterministic hash of user_id + flag_name → 0-100), segment (user.attributes match rule conditions). Python SDK: same design using threading.Event for background poll; evaluate() is thread-safe. Offline safety: bundle a default_values.json file in the SDK package (last known good values committed to git); if flag server unreachable at init → use defaults + log warning; never throw on flag evaluation (return default). Version-aware cache: if server version hash unchanged → skip re-fetch. Telemetry: SDK tracks flag_evaluation_count{flag, variant} and emits to OpenTelemetry if configured. Show: start app with flag server down → SDK uses defaults → flag server comes up → SDK fetches live flags → evaluations switch → SSE update changes a flag → all app instances pick it up within 1 s."
  },
 
  // ── 484 ── originally id 284 (Multi-Tenancy Billing)
  {
    id: 484,
    title: "Multi-Tenant Usage-Based Billing System",
    cat: "Infrastructure",
    stack: "Node + PostgreSQL + Redis + Stripe API (test mode) + React",
    prompt: "Build a metered billing system that attributes resource usage to tenants, applies tiered pricing, and generates invoices via Stripe. Usage tracking: every API request middleware increments a Redis counter {tenant_id}:{metric}:{YYYY-MM} for metrics: api_calls, storage_gb_hours, compute_minutes. Batch aggregator: every 5 min, flush Redis counters to PostgreSQL usage_ledger (tenant_id, metric, period, quantity, unit_cost). Pricing tiers: YAML pricing config — api_calls: [{from: 0, to: 10000, unit_price: 0}, {from: 10001, to: 100000, unit_price: 0.0001}, {from: 100001, unit_price: 0.00005}] (tiered pricing, not flat). Invoice generation: monthly Celery job per tenant: reads usage_ledger, applies tier pricing, computes line items, creates Stripe Invoice via API (addInvoiceItem per metric, then finaliseInvoice, then sendInvoice). Promo credits: credits table (tenant_id, amount, expires_at, reason); deducted from invoice before charging. Cost explorer UI (React): stacked bar chart of monthly spend by metric; current-month live estimate (from Redis counters + pricing); usage alert — notify if projected monthly cost > budget_threshold. Show: tenant makes 150 000 API calls → tier 1 (0–10 k) = $0 + tier 2 (10–100 k) = $9 + tier 3 (100–150 k) = $2.50 = $11.50 invoice → Stripe invoice created → PDF downloadable."
  },
 
  // ── 485 ── originally id 285 (Model Explainability Dashboard)
  {
    id: 485,
    title: "ML Model Explainability Dashboard with SHAP & Bias Audit",
    cat: "Observability",
    stack: "Python + scikit-learn + SHAP + FastAPI + React",
    prompt: "Build an explainability dashboard that shows per-request feature importance and audits models for demographic bias. Model: train a loan approval classifier (LightGBM) on a synthetic dataset with features: income, age, credit_score, employment_years, loan_amount, region, gender (include gender to demonstrate bias). SHAP explainer: compute SHAP values using TreeExplainer (fast for tree models). Per-prediction explanation endpoint: POST /explain {features} → run model → compute SHAP values → return {prediction, probability, top_5_features: [{name, shap_value, contribution_direction}]} sorted by |shap_value|. Explanation visualisation (React): waterfall chart showing how each feature pushes the prediction from the base value toward the final probability; positive contributions (increase approval chance) in green, negative in red. Global feature importance: GET /model/feature-importance → aggregate SHAP values over 1000 test samples → bar chart of mean |SHAP| per feature. Bias audit: for protected attributes (gender, region), compute: approval_rate by group; SHAP contribution of protected attribute (should be near 0 in a fair model); show: gender has SHAP contribution of +0.12 for male → model is biased. Counterfactual: given a denied application, what is the minimum change to income that would flip the decision? (binary search on income with prediction). Show: run audit on biased model → dashboard flags gender as significant contributor → retrain without gender feature → re-audit shows near-zero gender contribution."
  },
 
  // ── 486 ── originally id 286 (Optimistic Concurrency Demo)
  {
    id: 486,
    title: "Optimistic Concurrency Control with Conflict UI",
    cat: "Database",
    stack: "Python + PostgreSQL + FastAPI + React",
    prompt: "Implement optimistic locking for a concurrent editing scenario and build a UI that helps users resolve conflicts. Database: add version INTEGER column to any shared resource table (e.g. documents); increment on every write. API: GET /documents/:id → returns {data, version}; PUT /documents/:id {data, expected_version} → UPDATE WHERE version = expected_version (atomic); if 0 rows updated → conflict (someone else updated since you fetched). Conflict response: 409 Conflict with body {your_version: 5, current_version: 7, current_data: {...}, your_changes: {...}} — enough info for the client to show a diff. UI (React): document editor that fetches current version; on save, sends expected_version; on 409 → show three-panel diff (base, yours, theirs) with per-field conflict highlighting; user can choose: 'take mine', 'take theirs', 'merge manually'. Retry logic: after merge, re-submit with current_version from conflict response; if another 409 (very rare) → repeat. Simulation: two browser tabs edit the same document; tab 1 saves first → tab 2's save gets 409 → tab 2 sees conflict UI → user merges. Metrics: track conflict_rate per document over time (high conflict rate signals a document with too many concurrent editors — suggest operational locking or CRDT). Show: simulate 50 concurrent savers on one document; conflict rate, resolution time per conflict."
  },
 
  // ── 487 ── originally id 287 (Realtime Metrics Pipeline)
  {
    id: 487,
    title: "High-Cardinality Metrics Pipeline with Client-Side Aggregation",
    cat: "Observability",
    stack: "Python + Prometheus Pushgateway + FastAPI + Grafana",
    prompt: "Handle high-cardinality event streams by aggregating metrics client-side before pushing to Prometheus, preventing series explosion. Problem demonstration: naive approach — emit a metric per {user_id, action, region} — with 1 M users, 20 actions, 10 regions this creates 200 M time-series (Prometheus limit ∼ 10 M). Solution: client-side pre-aggregation. Aggregation agent (Python): maintain in-memory counters and histograms keyed by low-cardinality labels only ({action, region}); every 15 s, compute aggregates and push to Pushgateway as Prometheus metrics. High-cardinality data routing: events that need per-user granularity → write to ClickHouse (OLAP, handles high-cardinality well); events needing only aggregate monitoring → Prometheus. Cardinality guardrail: a Prometheus recording rule computes cardinality per metric; Alertmanager fires if any metric exceeds 100 k series. Client SDK: expose track_event(action, region, user_id, value) → internally routes to correct store (Prometheus aggregator vs ClickHouse). Show: emit 1 M user-level events → Prometheus receives only 200 series (20 × 10); ClickHouse receives full per-user detail; Grafana dashboard queries Prometheus for aggregate charts and ClickHouse for per-user drill-down. Measure: push latency (Pushgateway < 100 ms), ClickHouse insert throughput."
  },
 
  // ── 488 ── originally id 288 (Pluggable Auth Middleware)
  {
    id: 488,
    title: "Pluggable Authentication Middleware with Runtime Strategy Swap",
    cat: "API Design",
    stack: "Node + Express + TypeScript + PostgreSQL + Redis",
    prompt: "Build a middleware architecture that supports multiple authentication strategies as interchangeable plugins, swappable via configuration without code changes. Auth strategy interface: { name: string; authenticate(req): Promise<AuthResult> } where AuthResult = {user_id, scopes, metadata} | null. Strategies implemented: (1) JWT Bearer — verify signature, check exp, extract claims from payload. (2) API Key — SHA-256 hash of key from header, lookup in PostgreSQL api_keys table, check scopes. (3) OAuth2 Introspection — call configured introspection endpoint, cache result in Redis for remaining token lifetime. (4) Session Cookie — decrypt signed cookie (using iron-webcrypto), load session from Redis, extend TTL. Strategy selector: reads AUTH_STRATEGIES env var (e.g. 'jwt,api_key') as ordered list; tries each strategy in order; first non-null result wins (fail-open across strategies, fail-closed if all return null). Per-route override: route metadata can specify required_strategies (e.g. /admin routes: jwt only, no api_key). Strategy hot-swap: POST /admin/auth-config {strategies: ['jwt']} → updates Redis config key → middleware reloads on next request cycle (no restart). Metrics: auth_attempts_total{strategy, result: success|fail|error}. Show: configure JWT + API Key → hit endpoint with JWT → succeeds via JWT strategy; hit with API key → succeeds via API Key; hit with neither → 401; swap to JWT-only → API key now rejected."
  },
 
  // ── 489 ── originally id 289 (API Usage Quotas)
  {
    id: 489,
    title: "Monthly API Usage Quotas with Soft Limits & Notifications",
    cat: "Performance",
    stack: "Node + Redis + PostgreSQL + Stripe (billing) + SendGrid",
    prompt: "Build a production-grade API quota system with soft limits, overage handling, real-time usage visibility, and automated notifications. Quota definition: plans table (plan_id, name, monthly_api_calls, monthly_compute_seconds, overage_allowed: bool, overage_rate_per_1k). Customer quotas: customer_quotas table (customer_id, plan_id, custom_overrides JSONB). Usage tracking: Redis monthly counter {customer_id}:api_calls:{YYYY-MM} incremented atomically per request; flush to PostgreSQL usage_history every 5 min. Limit enforcement: on each request, check Redis counter vs quota; at 80% → add X-Quota-Warning: approaching-limit header; at 100% with overage_allowed=false → 429 with Retry-After: {seconds_until_next_month}; at 100% with overage_allowed=true → allow but increment overage_calls counter. Notifications: daily Celery job checks % usage; send email at 80%, 95%, 100%, 110% (overage) — each email sent only once per threshold crossing per month (track in PostgreSQL quota_notifications). Overage billing: monthly Celery job computes overage_calls × overage_rate for each customer, creates Stripe InvoiceItem, triggers invoice. Self-service dashboard: GET /billing/usage → current month usage, daily breakdown chart, projected month-end usage (linear extrapolation), quota limits. Show: customer exhausts quota at day 20 → 429 returned → overage notification sent → next month resets."
  },
 
  // ── 490 ── originally id 290 (Data Backfill Tool)
  {
    id: 490,
    title: "Safe Large-Scale Data Backfill Tool with Chunking & Resume",
    cat: "Database",
    stack: "Python + PostgreSQL + Celery + Redis + FastAPI",
    prompt: "Build a tool for running large database backfills safely — without locking tables, overwhelming the DB, or losing progress on failure. Backfill definition: POST /backfills {table, id_column, chunk_size: 1000, query_template, target_table?, transform_fn?, throttle_ms: 100} → creates backfill_job record with status=PENDING. Chunk strategy: SELECT MIN(id), MAX(id) from source table → divide into chunks of chunk_size using id range (e.g. id BETWEEN 1000 AND 1999); chunk boundaries stored in backfill_chunks table (job_id, chunk_start, chunk_end, status, processed_at, rows_affected). Celery workers: each chunk is a Celery task; worker executes the query_template for its chunk range, optionally applies transform_fn (Python lambda), writes to target. Throttling: after each chunk, sleep throttle_ms to avoid DB saturation; also monitor pg_stat_activity — if active_queries > threshold, increase throttle adaptively. Resume on failure: completed chunks are marked DONE in PostgreSQL; on restart, only PENDING and FAILED chunks are re-enqueued. Progress API: GET /backfills/:id → {total_chunks, done_chunks, failed_chunks, rows_processed, estimated_eta, current_throughput_rows_per_sec}. Dry-run mode: estimate rows affected and time to complete without executing. Show: 10 M row backfill, kill worker mid-way, restart → resumes from last completed chunk, final count matches source."
  },
 
  // ── 491 ── originally id 291 (API Contract Diff Tool)
  {
    id: 491,
    title: "API Contract Diff & Breaking Change Detector",
    cat: "API Design",
    stack: "Node + OpenAPI + TypeScript + GitHub Actions",
    prompt: "Build a tool that compares two OpenAPI 3.1 specs and produces a structured, actionable diff report distinguishing breaking from non-breaking changes. Diff categories: BREAKING (removed endpoint, removed required field, changed field type to incompatible type, changed HTTP method, changed path, new required request field, narrowed response field type, removed enum value); NON_BREAKING_ADDITIVE (new optional field, new endpoint, new enum value, loosened field constraint); NON_BREAKING_EDITORIAL (description change, example change, title change). Diff engine: recursively compare two parsed OpenAPI spec objects; for each endpoint/schema/field: classify the change using the above rules; collect all changes into a structured list. Output formats: (1) human-readable Markdown report (collapsible sections per change type, code diffs); (2) JSON for machine consumption; (3) GitHub PR comment (post to PR via GitHub API if run in CI). Severity scoring: BREAKING → fail with exit code 1; NON_BREAKING → warn; EDITORIAL → info. Versioning guidance: if breaking changes detected but new path has /v2/ prefix → downgrade to WARNING (versioned breaking change is expected). GitHub Action: runs on every PR that touches openapi.yaml; posts comment and fails check if BREAKING changes detected. Show: PR removes a required field from response → action fails → comment shows the exact breaking change; PR adds optional field → action passes with INFO comment."
  },
 
  // ── 492 ── originally id 292 (Realtime Geo-Fencing)
  {
    id: 492,
    title: "Real-Time Geo-Fencing with Enter/Exit Event Triggers",
    cat: "Infrastructure",
    stack: "Node + Redis + PostGIS + FastAPI + WebSocket",
    prompt: "Build a geo-fencing system that tracks moving entities (vehicles, mobile devices) and fires events when they enter or leave defined geographic zones. Zone management: POST /zones {name, polygon_geojson, trigger_on: enter|exit|both, webhook_url?} → store polygon in PostGIS geography column; index with GIST. Entity tracking: POST /entities/:id/location {lat, lon, timestamp} → update entity's current position in Redis GEOADD (key: 'entities', score: lon/lat, member: entity_id); also update PostgreSQL entity_positions for history. Geo-fence check: on each position update, query PostGIS: SELECT zone_id FROM zones WHERE ST_Contains(polygon, ST_SetSRID(ST_MakePoint(lon, lat), 4326)); compare result set with previous zones (stored in Redis SMEMBERS entity:{id}:zones); if new zones → ENTER event; if departed zones → EXIT event. Event dispatch: push enter/exit events to a Redis Stream; event worker reads stream, calls zone's webhook_url (with retry), pushes to WebSocket clients subscribed to zone updates. Dwell time: track when entity entered each zone (Redis HSET entity:{id}:zone_entry_time); on EXIT emit dwell_duration_seconds. Efficiency: for entities outside any zone bounding box, skip PostGIS query (Redis GEORADIUS pre-filter). Show: track 100 simulated vehicles; define 5 geo-fences; vehicles enter/exit zones; WebSocket client receives enter/exit events in real time; verify zero false positives."
  },
 
  // ── 493 ── originally id 293 (Content Moderation Pipeline)
  {
    id: 493,
    title: "Content Moderation Pipeline with Human-in-the-Loop Review",
    cat: "Auth & Security",
    stack: "Python + FastAPI + Celery + PostgreSQL + scikit-learn + React",
    prompt: "Build a multi-stage content moderation pipeline combining automated ML scoring, rules-based filtering, and a human review queue. Stage 1 — Synchronous rules filter (< 5 ms): check against blocklist (Redis SET), detect known CSAM hash (SHA-256 list), max content length, spam pattern regex. If any match → REJECT immediately, log reason. Stage 2 — Async ML scoring (< 500 ms): enqueue Celery task; text classifier (TF-IDF + LogisticRegression trained on public toxicity dataset) scores content as {toxicity: 0–1, spam: 0–1, hate: 0–1}. If any score > HIGH_THRESHOLD (0.9) → AUTO_REJECT; if score in (0.5, 0.9) → HUMAN_REVIEW queue. Stage 3 — Human review: ReviewTask model (content_id, scores, content_preview, created_at, assigned_to, decision, decision_reason, reviewed_at). Review UI (React): task queue ordered by score DESC; reviewer sees content + ML scores; actions: APPROVE / REJECT / ESCALATE; keyboard shortcuts for speed. Feedback loop: reviewer decisions stored in labelled_examples table; weekly batch job retrains classifier on accumulated labels; track model accuracy over time (improving precision/recall). Metrics: auto_reject_rate, human_review_rate, review_queue_depth, median_review_time_s, reviewer_agreement_rate (inter-rater). Show: submit 100 test items (mix of clean, borderline, toxic) → pipeline routes correctly, human queue gets borderline items, model retrains and improves on week-2 data."
  },
 
  // ── 494 ── originally id 294 (Stream-First ETL)
  {
    id: 494,
    title: "Stream-First ETL with ksqlDB and Materialised Views",
    cat: "Messaging",
    stack: "Python + Kafka + ksqlDB + PostgreSQL",
    prompt: "Build a streaming ETL that transforms events as they arrive using ksqlDB and materialises continuously-updated views for analytics — no batch jobs needed. Source streams: Kafka topics — orders (order_id, user_id, product_id, amount, status, created_at), users (user_id, name, region, tier), products (product_id, name, category, price). ksqlDB setup: create streams and tables (orders stream, users table keyed by user_id, products table keyed by product_id). Transformations (ksqlDB SQL): (1) Enrich orders with user region: CREATE STREAM enriched_orders AS SELECT o.*, u.region, u.tier FROM orders o JOIN users u WITHIN 1 HOUR ON o.user_id = u.user_id. (2) Filter high-value orders: CREATE STREAM high_value AS SELECT * FROM enriched_orders WHERE amount > 1000. (3) Windowed aggregate: CREATE TABLE hourly_revenue AS SELECT region, WINDOWSTART as window_start, SUM(amount) as revenue FROM enriched_orders WINDOW TUMBLING (SIZE 1 HOUR) GROUP BY region. (4) Materialised view: push hourly_revenue to PostgreSQL via Kafka Connect JDBC sink connector. (5) Alert: CREATE STREAM failed_orders AS SELECT * FROM orders WHERE status='FAILED' — wire to Slack via Kafka Connect. Show: insert 10 k orders → ksqlDB enriches in real time → hourly_revenue table in PostgreSQL updates within 60 s → failed_orders alerts fire within 2 s."
  },
 
  // ── 495 ── originally id 295 (Multilingual UI i18n)
  {
    id: 495,
    title: "Multilingual UI with Runtime Language Switching & Pluralisation",
    cat: "API Design",
    stack: "React + i18next + Node + PostgreSQL",
    prompt: "Implement a production-grade internationalisation system in React that supports runtime language switching, pluralisation rules, content fallback, and a translation management workflow. i18next setup: namespace-based translations (common, dashboard, errors); translations stored as JSON per locale (en, fr, de, zh, ar); loaded lazily (fetch only the locale needed). Runtime switch: language selector in UI; switch triggers i18n.changeLanguage(code) → fetches new locale JSON from backend if not cached → re-renders entire app in < 100 ms (no page reload). Pluralisation: implement ICU message format for complex plural rules — English: {count, plural, one {# item} other {# items}}; Arabic has 6 plural forms — i18next-icu handles all. RTL support: Arabic/Hebrew → inject dir='rtl' on <html>; CSS logical properties (margin-inline-start instead of margin-left) — show layout flips correctly. Content fallback chain: missing zh translation → falls back to en → logs missing key for translation team. Backend: Node serves translations from PostgreSQL translations table (locale, namespace, key, value); admin API for CRUD; cache translations in Redis for 1 h. Translation workflow: GET /admin/translations/missing → lists keys present in 'en' but missing in other locales; POST /admin/translations/export → exports all locales as ZIP of JSON files for sending to translators. Show: switch from English to French → UI re-renders in French; switch to Arabic → RTL layout with correct plural form for counts."
  },
 
  // ── 496 ── originally id 296 (High-Availability Leader Election)
  {
    id: 496,
    title: "HA Leader Election & Worker Failover with etcd",
    cat: "Distributed Systems",
    stack: "Go + etcd + Docker",
    prompt: "Use etcd to implement robust leader election for a set of background workers where exactly one worker is active at a time and failover is automatic and fast. Worker setup: 5 Go worker processes each running as separate goroutines (or Docker containers). Election: each worker calls etcd's PUT with lease — PUT /workers/leader {node_id} IF NOT EXISTS with 10 s TTL; the first to succeed becomes leader; others watch the key. Leader duties: only the leader runs the critical background job (e.g. polling an external API every 5 s, processing a queue). Heartbeat: leader renews lease every 3 s (lease.KeepAlive); if leader process crashes, etcd lease expires after 10 s → key deleted → watchers fire. Failover: non-leaders watch etcd key with Watch API; when key deleted → race to acquire lease; exactly one wins → new leader within 10–15 s. Safety properties: verify with testing — (1) kill leader → time until new leader elected; (2) network partition (SIGSTOP leader process for 20 s → lease expires → new leader elected → SIGCONT original leader → it discovers it is no longer leader via failed KeepAlive); (3) split-brain test (two workers both think they are leader → impossible with etcd lease, prove it). Metrics: leader_election_count, leader_tenure_seconds, failover_latency_seconds. Show: kill leader at random times 10 times, measure average failover latency (target < 15 s)."
  },
 
  // ── 497 ── originally id 297 (Low-Latency Notification Fanout)
  {
    id: 497,
    title: "Low-Latency Notification Fanout to Millions of Connected Clients",
    cat: "Messaging",
    stack: "Node + WebSocket + Redis Streams + PostgreSQL",
    prompt: "Build a notification fanout architecture that can push a message to millions of WebSocket connections with < 100 ms latency using a sharded fanout design. Architecture: multiple fanout nodes each holding a shard of client connections (e.g. 10 nodes × 100 k connections = 1 M clients). Publisher: POST /notify {topic, payload} → publish to Redis Stream notifications:{topic}. Fanout worker: each node runs a Redis Stream consumer (XREAD with COUNT=1000 BLOCK=10); for each message, look up subscribers from Redis SSUBSCRIBE or from a local in-memory Map<topic, Set<ws_connection_id>>; push payload to each local WebSocket. Subscription routing: client connects to WebSocket → client subscribes to topic via {type: 'subscribe', topics: ['news', 'alerts']} → server adds to local Map + stores {client_id, node_id, topics} in Redis Hash for message routing. Cross-node fanout: when a node receives a notification from Redis Stream, it only delivers to locally connected subscribers; nodes consume the same Redis Stream so all get the event — O(nodes) Redis reads per notification, not O(clients). Local fanout: within a node, deliver to all matching local WebSockets using a single loop (no network hop). Performance targets: 1 M clients, 1 notification/s to all → measure end-to-end latency (p50, p99); 10 notifications/s to 100 k clients → measure throughput. Show: 1 M simulated WebSocket connections (using goroutines or virtual connections), publish 1 notification → measure time until last client receives it."
  },
 
  // ── 498 ── originally id 298 (Content Personalisation A/B)
  {
    id: 498,
    title: "Content Personalisation A/B Testing Framework",
    cat: "Performance",
    stack: "Node + Redis + PostgreSQL + React + SciPy (Python analysis service)",
    prompt: "Build a full personalisation A/B testing framework with user bucketing, event collection, and statistical significance analysis. Experiment definition: POST /experiments {name, hypothesis, variants: [{id, content_config, traffic_weight}], primary_metric, secondary_metrics, min_sample_size, max_duration_days} → stored in PostgreSQL. Bucketing: deterministic — bucket = parseInt(SHA256(user_id + experiment_id).slice(0, 8), 16) % 100; user always gets same variant (sticky); traffic_weight controls threshold (e.g. weight=50 → buckets 0–49 = control, 50–99 = treatment). Exposure logging: on first bucket assignment, log impression event {user_id, experiment_id, variant_id, timestamp} to Kafka → PostgreSQL. Conversion tracking: POST /events {user_id, event_type} → log to PostgreSQL; analysis service joins impressions with conversions (matching within experiment duration window). Statistical analysis (Python service): for conversion rate metrics: two-proportion z-test; for continuous metrics: Welch's t-test; also compute 95% CI, relative uplift, minimum detectable effect (given current sample size). Sequential testing: run analysis daily; stop early if significant and direction is clear (O'Brien-Fleming α spending); continue if not. Dashboard (React): experiment list with status (RUNNING/SIGNIFICANT/INCONCLUSIVE/ENDED); per-experiment: daily conversion chart per variant, p-value history, confidence intervals, recommended action. Show: run experiment 7 days with 1000 users, inject 15% uplift in treatment → framework detects significance at day 5."
  },
 
  // ── 499 ── originally id 299 (Observability Health Score)
  {
    id: 499,
    title: "Composite Service Health Score from Logs, Metrics & Traces",
    cat: "Observability",
    stack: "Node + Prometheus + Loki + Jaeger + React + Chart.js",
    prompt: "Build a unified health scoring system that aggregates signals from all three observability pillars into a single actionable score per service, with trend detection and suggested remediations. Signal collection (every 60 s): Metrics — query Prometheus: error_rate = rate(http_requests_total{status=~'5..'}[5m]) / rate(http_requests_total[5m]), p99_latency from histogram, saturation = max(cpu_usage / cpu_limit). Logs — query Loki: count of ERROR lines in last 5 min per service, count of WARN lines. Traces — query Jaeger API: mean trace duration, error span rate per service. Score computation: health_score = 100 − penalties; penalties: error_rate > 1% → −30, error_rate > 5% → −60; p99 > SLO → −20, p99 > 2×SLO → −40; cpu_saturation > 80% → −10; log_error_count > 100/min → −15. Cap at 0 (never negative). Trend detection: compare current score vs score 1 h ago and 24 h ago; classify as IMPROVING, STABLE, DEGRADING. Remediation suggestions: rule-based — if error_rate high → 'Check recent deploy, review error logs'; if cpu_saturation → 'Scale horizontally or profile for CPU hotspot'; if p99 high → 'Check DB query latency, review slow traces'. Dashboard (React): service grid showing score 0–100 with colour coding; click service → 30-day score history chart + breakdown of penalties + remediation suggestions. Alert: if score drops > 20 points in 15 min → PagerDuty/Slack alert. Show: inject DB timeout → error rate rises → health score drops from 95 to 35 → remediation suggestion appears."
  },
 
  // ── 500 ── a brand-new capstone combining themes from all 100 projects
  {
    id: 500,
    title: "Full-Platform Observability Capstone: End-to-End Service Health",
    cat: "Observability",
    stack: "Python + FastAPI + React + Prometheus + Loki + Jaeger + PostgreSQL + Redis + OpenTelemetry",
    prompt: "Build a capstone observability platform that ties together every signal and system from projects 401–499 into a cohesive, production-grade view of a multi-service application. Architecture: deploy 4 microservices (API Gateway, Order Service, Payment Service, Notification Service) each instrumented with OpenTelemetry — emitting traces to Jaeger, metrics to Prometheus, structured logs to Loki. Dashboard platform (React): (1) Service Map (project 407 technique) — live force-directed graph of service dependencies mined from traces, edges coloured by error rate. (2) Health Score Grid (project 499 technique) — composite 0–100 score per service updated every 60 s with penalty breakdown. (3) SLO Burn Rate Monitor — error budget remaining per service with multi-window burn rate alerts (1 h + 6 h windows). (4) Feature Flag Status — live flag states and exposure counts (project 401 technique). (5) Quota Dashboard — per-tenant API quota usage with overage projections (project 489 technique). (6) Canary Deployment Panel — current canary error rate vs baseline with PROMOTE / ROLLBACK buttons (project 454 technique). Incident workflow: when health score drops > 30 points → auto-create incident record, link the causal Jaeger trace, fetch related Loki error logs, suggest runbook from project 458 system. Show: trigger a cascading failure (payment service times out → order service error rate rises → health scores degrade → SLO burn rate alert fires → incident created with all correlated signals attached) — from first failure to full incident card: < 3 minutes."
  },
  {
    id: 501,
    title: "Service Mesh implementation from scratch",
    cat: "Cloud Native",
    stack: "Node.js + Redis",
    prompt: "Comprehensive project: Service Mesh implementation from scratch. You will design the architecture focusing on Cloud Native principles. Build the core components using Node.js + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 502,
    title: "Distributed lock manager using ZooKeeper",
    cat: "Distributed Systems",
    stack: "Rust + WebAssembly",
    prompt: "Comprehensive project: Distributed lock manager using ZooKeeper. You will design the architecture focusing on Distributed Systems principles. Build the core components using Rust + WebAssembly. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 503,
    title: "Time-series database engine based on LSM trees",
    cat: "Database",
    stack: "Java + Spring Boot",
    prompt: "Comprehensive project: Time-series database engine based on LSM trees. You will design the architecture focusing on Database principles. Build the core components using Java + Spring Boot. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 504,
    title: "Real-time collaborative whiteboard using WebSockets and CRDTs",
    cat: "Distributed Systems",
    stack: "Python + FastAPI",
    prompt: "Comprehensive project: Real-time collaborative whiteboard using WebSockets and CRDTs. You will design the architecture focusing on Distributed Systems principles. Build the core components using Python + FastAPI. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 505,
    title: "Distributed secret manager backed by Shamir's Secret Sharing",
    cat: "Auth & Security",
    stack: "Python + FastAPI",
    prompt: "Comprehensive project: Distributed secret manager backed by Shamir's Secret Sharing. You will design the architecture focusing on Auth & Security principles. Build the core components using Python + FastAPI. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 506,
    title: "GraphQL API gateway with schema stitching",
    cat: "API Design",
    stack: "Python + FastAPI",
    prompt: "Comprehensive project: GraphQL API gateway with schema stitching. You will design the architecture focusing on API Design principles. Build the core components using Python + FastAPI. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 507,
    title: "Feature flag service with percentage rollouts",
    cat: "Infrastructure",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Feature flag service with percentage rollouts. You will design the architecture focusing on Infrastructure principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 508,
    title: "Chaos engineering tool for fault injection in K8s",
    cat: "Testing & Reliability",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Chaos engineering tool for fault injection in K8s. You will design the architecture focusing on Testing & Reliability principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 509,
    title: "Reverse proxy with dynamic health checking",
    cat: "Networking",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Reverse proxy with dynamic health checking. You will design the architecture focusing on Networking principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 510,
    title: "Distributed task scheduler using consistent hashing",
    cat: "Infrastructure",
    stack: "Kafka + ClickHouse",
    prompt: "Comprehensive project: Distributed task scheduler using consistent hashing. You will design the architecture focusing on Infrastructure principles. Build the core components using Kafka + ClickHouse. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 511,
    title: "Log aggregation pipeline with fluentd and Elasticsearch",
    cat: "Observability",
    stack: "Java + Spring Boot",
    prompt: "Comprehensive project: Log aggregation pipeline with fluentd and Elasticsearch. You will design the architecture focusing on Observability principles. Build the core components using Java + Spring Boot. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 512,
    title: "Database connection pooler with query routing",
    cat: "Database",
    stack: "Kafka + ClickHouse",
    prompt: "Comprehensive project: Database connection pooler with query routing. You will design the architecture focusing on Database principles. Build the core components using Kafka + ClickHouse. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 513,
    title: "OAuth2 Authorization Server implementation",
    cat: "Auth & Security",
    stack: "Kafka + ClickHouse",
    prompt: "Comprehensive project: OAuth2 Authorization Server implementation. You will design the architecture focusing on Auth & Security principles. Build the core components using Kafka + ClickHouse. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 514,
    title: "Rate-limiting service using sliding log algorithm",
    cat: "Performance",
    stack: "Node.js + Redis",
    prompt: "Comprehensive project: Rate-limiting service using sliding log algorithm. You will design the architecture focusing on Performance principles. Build the core components using Node.js + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 515,
    title: "Serverless function execution engine using containerd",
    cat: "Cloud Native",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Serverless function execution engine using containerd. You will design the architecture focusing on Cloud Native principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 516,
    title: "Change Data Capture (CDC) pipeline using Debezium",
    cat: "Data Engineering",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Change Data Capture (CDC) pipeline using Debezium. You will design the architecture focusing on Data Engineering principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 517,
    title: "Idempotency key middleware for payment APIs",
    cat: "API Design",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Idempotency key middleware for payment APIs. You will design the architecture focusing on API Design principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 518,
    title: "Distributed Pub/Sub message broker",
    cat: "Messaging",
    stack: "Kafka + ClickHouse",
    prompt: "Comprehensive project: Distributed Pub/Sub message broker. You will design the architecture focusing on Messaging principles. Build the core components using Kafka + ClickHouse. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 519,
    title: "Data warehouse star schema design tool",
    cat: "Database",
    stack: "Java + Spring Boot",
    prompt: "Comprehensive project: Data warehouse star schema design tool. You will design the architecture focusing on Database principles. Build the core components using Java + Spring Boot. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 520,
    title: "CDN edge caching server with stale-while-revalidate",
    cat: "Performance",
    stack: "Terraform + AWS",
    prompt: "Comprehensive project: CDN edge caching server with stale-while-revalidate. You will design the architecture focusing on Performance principles. Build the core components using Terraform + AWS. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 521,
    title: "Consensus algorithm visualizer (Raft)",
    cat: "Distributed Systems",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Consensus algorithm visualizer (Raft). You will design the architecture focusing on Distributed Systems principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 522,
    title: "WebAuthn/Passkeys authentication server",
    cat: "Auth & Security",
    stack: "Terraform + AWS",
    prompt: "Comprehensive project: WebAuthn/Passkeys authentication server. You will design the architecture focusing on Auth & Security principles. Build the core components using Terraform + AWS. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 523,
    title: "API monetization layer with Stripe integration",
    cat: "Infrastructure",
    stack: "Node.js + Redis",
    prompt: "Comprehensive project: API monetization layer with Stripe integration. You will design the architecture focusing on Infrastructure principles. Build the core components using Node.js + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 524,
    title: "Distributed tracing agent with OpenTelemetry",
    cat: "Observability",
    stack: "Node.js + Redis",
    prompt: "Comprehensive project: Distributed tracing agent with OpenTelemetry. You will design the architecture focusing on Observability principles. Build the core components using Node.js + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 525,
    title: "Circuit breaker library with Half-Open state",
    cat: "Performance",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Circuit breaker library with Half-Open state. You will design the architecture focusing on Performance principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 526,
    title: "Geospatial index using Quadtrees",
    cat: "Database",
    stack: "Kubernetes + Prometheus",
    prompt: "Comprehensive project: Geospatial index using Quadtrees. You will design the architecture focusing on Database principles. Build the core components using Kubernetes + Prometheus. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 527,
    title: "Webhook delivery system with exponential backoff",
    cat: "Messaging",
    stack: "Terraform + AWS",
    prompt: "Comprehensive project: Webhook delivery system with exponential backoff. You will design the architecture focusing on Messaging principles. Build the core components using Terraform + AWS. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 528,
    title: "In-memory cache with LFU eviction policy",
    cat: "Storage Systems",
    stack: "PostgreSQL + Redis",
    prompt: "Comprehensive project: In-memory cache with LFU eviction policy. You will design the architecture focusing on Storage Systems principles. Build the core components using PostgreSQL + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 529,
    title: "Container orchestration engine (Mini-K8s)",
    cat: "Cloud Native",
    stack: "Node.js + Redis",
    prompt: "Comprehensive project: Container orchestration engine (Mini-K8s). You will design the architecture focusing on Cloud Native principles. Build the core components using Node.js + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 530,
    title: "Batch processing framework for large datasets",
    cat: "Data Engineering",
    stack: "Java + Spring Boot",
    prompt: "Comprehensive project: Batch processing framework for large datasets. You will design the architecture focusing on Data Engineering principles. Build the core components using Java + Spring Boot. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 531,
    title: "A/B testing framework with statistical significance calculation",
    cat: "ML Systems",
    stack: "PostgreSQL + Redis",
    prompt: "Comprehensive project: A/B testing framework with statistical significance calculation. You will design the architecture focusing on ML Systems principles. Build the core components using PostgreSQL + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 532,
    title: "gRPC to REST transcoder proxy",
    cat: "API Design",
    stack: "PostgreSQL + Redis",
    prompt: "Comprehensive project: gRPC to REST transcoder proxy. You will design the architecture focusing on API Design principles. Build the core components using PostgreSQL + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 533,
    title: "Identity and Access Management (IAM) service with RBAC",
    cat: "Auth & Security",
    stack: "Kafka + ClickHouse",
    prompt: "Comprehensive project: Identity and Access Management (IAM) service with RBAC. You will design the architecture focusing on Auth & Security principles. Build the core components using Kafka + ClickHouse. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 534,
    title: "Search engine autocomplete using Tries",
    cat: "Performance",
    stack: "Python + FastAPI",
    prompt: "Comprehensive project: Search engine autocomplete using Tries. You will design the architecture focusing on Performance principles. Build the core components using Python + FastAPI. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 535,
    title: "Distributed file system using content-addressable storage",
    cat: "Storage Systems",
    stack: "Terraform + AWS",
    prompt: "Comprehensive project: Distributed file system using content-addressable storage. You will design the architecture focusing on Storage Systems principles. Build the core components using Terraform + AWS. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 536,
    title: "Dead-letter queue (DLQ) processor and replay tool",
    cat: "Messaging",
    stack: "Rust + WebAssembly",
    prompt: "Comprehensive project: Dead-letter queue (DLQ) processor and replay tool. You will design the architecture focusing on Messaging principles. Build the core components using Rust + WebAssembly. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 537,
    title: "Database migration framework (like Alembic)",
    cat: "Database",
    stack: "Elasticsearch + Logstash",
    prompt: "Comprehensive project: Database migration framework (like Alembic). You will design the architecture focusing on Database principles. Build the core components using Elasticsearch + Logstash. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 538,
    title: "Metrics aggregation service using exponential decay",
    cat: "Observability",
    stack: "Kubernetes + Prometheus",
    prompt: "Comprehensive project: Metrics aggregation service using exponential decay. You will design the architecture focusing on Observability principles. Build the core components using Kubernetes + Prometheus. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 539,
    title: "TCP load balancer with consistent hashing",
    cat: "Networking",
    stack: "PostgreSQL + Redis",
    prompt: "Comprehensive project: TCP load balancer with consistent hashing. You will design the architecture focusing on Networking principles. Build the core components using PostgreSQL + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 540,
    title: "State machine workflow engine (like AWS Step Functions)",
    cat: "Distributed Systems",
    stack: "Rust + WebAssembly",
    prompt: "Comprehensive project: State machine workflow engine (like AWS Step Functions). You will design the architecture focusing on Distributed Systems principles. Build the core components using Rust + WebAssembly. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 541,
    title: "Deploy a distributed fraud detection system",
    cat: "Cloud Native",
    stack: "Kubernetes + Prometheus",
    prompt: "Comprehensive project: Deploy a distributed fraud detection system. You will design the architecture focusing on Cloud Native principles. Build the core components using Kubernetes + Prometheus. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 542,
    title: "Architect a secure payment gateway",
    cat: "Infrastructure",
    stack: "Elasticsearch + Logstash",
    prompt: "Comprehensive project: Architect a secure payment gateway. You will design the architecture focusing on Infrastructure principles. Build the core components using Elasticsearch + Logstash. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 543,
    title: "Deploy a globally scaled IoT ingestion pipeline",
    cat: "Messaging",
    stack: "Node.js + Redis",
    prompt: "Comprehensive project: Deploy a globally scaled IoT ingestion pipeline. You will design the architecture focusing on Messaging principles. Build the core components using Node.js + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 544,
    title: "Deploy a secure ride-sharing dispatcher",
    cat: "ML Systems",
    stack: "Elasticsearch + Logstash",
    prompt: "Comprehensive project: Deploy a secure ride-sharing dispatcher. You will design the architecture focusing on ML Systems principles. Build the core components using Elasticsearch + Logstash. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 545,
    title: "Implement an observable ad-serving platform",
    cat: "Messaging",
    stack: "Terraform + AWS",
    prompt: "Comprehensive project: Implement an observable ad-serving platform. You will design the architecture focusing on Messaging principles. Build the core components using Terraform + AWS. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 546,
    title: "Design a high-performance fraud detection system",
    cat: "Storage Systems",
    stack: "Terraform + AWS",
    prompt: "Comprehensive project: Design a high-performance fraud detection system. You will design the architecture focusing on Storage Systems principles. Build the core components using Terraform + AWS. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 547,
    title: "Architect a globally scaled chat application",
    cat: "Storage Systems",
    stack: "Python + FastAPI",
    prompt: "Comprehensive project: Architect a globally scaled chat application. You will design the architecture focusing on Storage Systems principles. Build the core components using Python + FastAPI. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 548,
    title: "Architect a secure chat application",
    cat: "Auth & Security",
    stack: "Rust + WebAssembly",
    prompt: "Comprehensive project: Architect a secure chat application. You will design the architecture focusing on Auth & Security principles. Build the core components using Rust + WebAssembly. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 549,
    title: "Create a distributed fraud detection system",
    cat: "ML Systems",
    stack: "Rust + WebAssembly",
    prompt: "Comprehensive project: Create a distributed fraud detection system. You will design the architecture focusing on ML Systems principles. Build the core components using Rust + WebAssembly. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 550,
    title: "Deploy an observable stock trading platform",
    cat: "Data Engineering",
    stack: "Node.js + Redis",
    prompt: "Comprehensive project: Deploy an observable stock trading platform. You will design the architecture focusing on Data Engineering principles. Build the core components using Node.js + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 551,
    title: "Implement a globally scaled recommendation engine",
    cat: "Compiler & Runtimes",
    stack: "Rust + WebAssembly",
    prompt: "Comprehensive project: Implement a globally scaled recommendation engine. You will design the architecture focusing on Compiler & Runtimes principles. Build the core components using Rust + WebAssembly. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 552,
    title: "Build a secure stock trading platform",
    cat: "ML Systems",
    stack: "Node.js + Redis",
    prompt: "Comprehensive project: Build a secure stock trading platform. You will design the architecture focusing on ML Systems principles. Build the core components using Node.js + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 553,
    title: "Create a distributed fraud detection system",
    cat: "Compiler & Runtimes",
    stack: "Python + FastAPI",
    prompt: "Comprehensive project: Create a distributed fraud detection system. You will design the architecture focusing on Compiler & Runtimes principles. Build the core components using Python + FastAPI. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 554,
    title: "Deploy an observable fraud detection system",
    cat: "Messaging",
    stack: "PostgreSQL + Redis",
    prompt: "Comprehensive project: Deploy an observable fraud detection system. You will design the architecture focusing on Messaging principles. Build the core components using PostgreSQL + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 555,
    title: "Build a high-performance IoT ingestion pipeline",
    cat: "Cloud Native",
    stack: "Kafka + ClickHouse",
    prompt: "Comprehensive project: Build a high-performance IoT ingestion pipeline. You will design the architecture focusing on Cloud Native principles. Build the core components using Kafka + ClickHouse. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 556,
    title: "Design a globally scaled video transcoding service",
    cat: "Networking",
    stack: "Terraform + AWS",
    prompt: "Comprehensive project: Design a globally scaled video transcoding service. You will design the architecture focusing on Networking principles. Build the core components using Terraform + AWS. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 557,
    title: "Implement a distributed content delivery network",
    cat: "Data Engineering",
    stack: "Python + FastAPI",
    prompt: "Comprehensive project: Implement a distributed content delivery network. You will design the architecture focusing on Data Engineering principles. Build the core components using Python + FastAPI. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 558,
    title: "Build a high-performance chat application",
    cat: "Auth & Security",
    stack: "Elasticsearch + Logstash",
    prompt: "Comprehensive project: Build a high-performance chat application. You will design the architecture focusing on Auth & Security principles. Build the core components using Elasticsearch + Logstash. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 559,
    title: "Deploy a high-performance chat application",
    cat: "Database",
    stack: "Terraform + AWS",
    prompt: "Comprehensive project: Deploy a high-performance chat application. You will design the architecture focusing on Database principles. Build the core components using Terraform + AWS. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 560,
    title: "Create a high-performance ride-sharing dispatcher",
    cat: "Observability",
    stack: "Java + Spring Boot",
    prompt: "Comprehensive project: Create a high-performance ride-sharing dispatcher. You will design the architecture focusing on Observability principles. Build the core components using Java + Spring Boot. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 561,
    title: "Design a distributed fraud detection system",
    cat: "Database",
    stack: "Terraform + AWS",
    prompt: "Comprehensive project: Design a distributed fraud detection system. You will design the architecture focusing on Database principles. Build the core components using Terraform + AWS. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 562,
    title: "Deploy a secure fraud detection system",
    cat: "Distributed Systems",
    stack: "Rust + WebAssembly",
    prompt: "Comprehensive project: Deploy a secure fraud detection system. You will design the architecture focusing on Distributed Systems principles. Build the core components using Rust + WebAssembly. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 563,
    title: "Design a secure IoT ingestion pipeline",
    cat: "Distributed Systems",
    stack: "Rust + WebAssembly",
    prompt: "Comprehensive project: Design a secure IoT ingestion pipeline. You will design the architecture focusing on Distributed Systems principles. Build the core components using Rust + WebAssembly. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 564,
    title: "Deploy a distributed IoT ingestion pipeline",
    cat: "Infrastructure",
    stack: "Rust + WebAssembly",
    prompt: "Comprehensive project: Deploy a distributed IoT ingestion pipeline. You will design the architecture focusing on Infrastructure principles. Build the core components using Rust + WebAssembly. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 565,
    title: "Design a globally scaled content delivery network",
    cat: "Auth & Security",
    stack: "Java + Spring Boot",
    prompt: "Comprehensive project: Design a globally scaled content delivery network. You will design the architecture focusing on Auth & Security principles. Build the core components using Java + Spring Boot. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 566,
    title: "Build a globally scaled content delivery network",
    cat: "Infrastructure",
    stack: "Java + Spring Boot",
    prompt: "Comprehensive project: Build a globally scaled content delivery network. You will design the architecture focusing on Infrastructure principles. Build the core components using Java + Spring Boot. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 567,
    title: "Design a secure stock trading platform",
    cat: "Observability",
    stack: "Elasticsearch + Logstash",
    prompt: "Comprehensive project: Design a secure stock trading platform. You will design the architecture focusing on Observability principles. Build the core components using Elasticsearch + Logstash. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 568,
    title: "Implement an observable ride-sharing dispatcher",
    cat: "Storage Systems",
    stack: "PostgreSQL + Redis",
    prompt: "Comprehensive project: Implement an observable ride-sharing dispatcher. You will design the architecture focusing on Storage Systems principles. Build the core components using PostgreSQL + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 569,
    title: "Create a distributed recommendation engine",
    cat: "Data Engineering",
    stack: "Kubernetes + Prometheus",
    prompt: "Comprehensive project: Create a distributed recommendation engine. You will design the architecture focusing on Data Engineering principles. Build the core components using Kubernetes + Prometheus. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 570,
    title: "Architect a secure recommendation engine",
    cat: "ML Systems",
    stack: "Elasticsearch + Logstash",
    prompt: "Comprehensive project: Architect a secure recommendation engine. You will design the architecture focusing on ML Systems principles. Build the core components using Elasticsearch + Logstash. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 571,
    title: "Design a distributed video transcoding service",
    cat: "Testing & Reliability",
    stack: "Kafka + ClickHouse",
    prompt: "Comprehensive project: Design a distributed video transcoding service. You will design the architecture focusing on Testing & Reliability principles. Build the core components using Kafka + ClickHouse. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 572,
    title: "Deploy a globally scaled fraud detection system",
    cat: "Database",
    stack: "Terraform + AWS",
    prompt: "Comprehensive project: Deploy a globally scaled fraud detection system. You will design the architecture focusing on Database principles. Build the core components using Terraform + AWS. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 573,
    title: "Create a distributed social feed",
    cat: "Performance",
    stack: "Kafka + ClickHouse",
    prompt: "Comprehensive project: Create a distributed social feed. You will design the architecture focusing on Performance principles. Build the core components using Kafka + ClickHouse. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 574,
    title: "Deploy a globally scaled fraud detection system",
    cat: "Messaging",
    stack: "Rust + WebAssembly",
    prompt: "Comprehensive project: Deploy a globally scaled fraud detection system. You will design the architecture focusing on Messaging principles. Build the core components using Rust + WebAssembly. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 575,
    title: "Build an observable content delivery network",
    cat: "Database",
    stack: "Rust + WebAssembly",
    prompt: "Comprehensive project: Build an observable content delivery network. You will design the architecture focusing on Database principles. Build the core components using Rust + WebAssembly. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 576,
    title: "Implement a globally scaled ride-sharing dispatcher",
    cat: "Infrastructure",
    stack: "Node.js + Redis",
    prompt: "Comprehensive project: Implement a globally scaled ride-sharing dispatcher. You will design the architecture focusing on Infrastructure principles. Build the core components using Node.js + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 577,
    title: "Design a globally scaled recommendation engine",
    cat: "Infrastructure",
    stack: "Python + FastAPI",
    prompt: "Comprehensive project: Design a globally scaled recommendation engine. You will design the architecture focusing on Infrastructure principles. Build the core components using Python + FastAPI. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 578,
    title: "Implement a distributed social feed",
    cat: "Testing & Reliability",
    stack: "Java + Spring Boot",
    prompt: "Comprehensive project: Implement a distributed social feed. You will design the architecture focusing on Testing & Reliability principles. Build the core components using Java + Spring Boot. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 579,
    title: "Deploy a globally scaled recommendation engine",
    cat: "Compiler & Runtimes",
    stack: "Node.js + Redis",
    prompt: "Comprehensive project: Deploy a globally scaled recommendation engine. You will design the architecture focusing on Compiler & Runtimes principles. Build the core components using Node.js + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 580,
    title: "Design a secure recommendation engine",
    cat: "Data Engineering",
    stack: "Terraform + AWS",
    prompt: "Comprehensive project: Design a secure recommendation engine. You will design the architecture focusing on Data Engineering principles. Build the core components using Terraform + AWS. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 581,
    title: "Build a distributed payment gateway",
    cat: "Distributed Systems",
    stack: "Python + FastAPI",
    prompt: "Comprehensive project: Build a distributed payment gateway. You will design the architecture focusing on Distributed Systems principles. Build the core components using Python + FastAPI. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 582,
    title: "Deploy an observable ride-sharing dispatcher",
    cat: "Performance",
    stack: "PostgreSQL + Redis",
    prompt: "Comprehensive project: Deploy an observable ride-sharing dispatcher. You will design the architecture focusing on Performance principles. Build the core components using PostgreSQL + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 583,
    title: "Design a distributed ad-serving platform",
    cat: "Compiler & Runtimes",
    stack: "Rust + WebAssembly",
    prompt: "Comprehensive project: Design a distributed ad-serving platform. You will design the architecture focusing on Compiler & Runtimes principles. Build the core components using Rust + WebAssembly. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 584,
    title: "Implement a high-performance ad-serving platform",
    cat: "Cloud Native",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Implement a high-performance ad-serving platform. You will design the architecture focusing on Cloud Native principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 585,
    title: "Create a globally scaled ad-serving platform",
    cat: "Testing & Reliability",
    stack: "Elasticsearch + Logstash",
    prompt: "Comprehensive project: Create a globally scaled ad-serving platform. You will design the architecture focusing on Testing & Reliability principles. Build the core components using Elasticsearch + Logstash. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 586,
    title: "Deploy a globally scaled stock trading platform",
    cat: "Cloud Native",
    stack: "Python + FastAPI",
    prompt: "Comprehensive project: Deploy a globally scaled stock trading platform. You will design the architecture focusing on Cloud Native principles. Build the core components using Python + FastAPI. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 587,
    title: "Implement a distributed analytics dashboard",
    cat: "Auth & Security",
    stack: "Python + FastAPI",
    prompt: "Comprehensive project: Implement a distributed analytics dashboard. You will design the architecture focusing on Auth & Security principles. Build the core components using Python + FastAPI. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 588,
    title: "Architect an observable ad-serving platform",
    cat: "Storage Systems",
    stack: "Java + Spring Boot",
    prompt: "Comprehensive project: Architect an observable ad-serving platform. You will design the architecture focusing on Storage Systems principles. Build the core components using Java + Spring Boot. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 589,
    title: "Build a globally scaled IoT ingestion pipeline",
    cat: "Data Engineering",
    stack: "Elasticsearch + Logstash",
    prompt: "Comprehensive project: Build a globally scaled IoT ingestion pipeline. You will design the architecture focusing on Data Engineering principles. Build the core components using Elasticsearch + Logstash. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 590,
    title: "Implement a high-performance fraud detection system",
    cat: "Cloud Native",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Implement a high-performance fraud detection system. You will design the architecture focusing on Cloud Native principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 591,
    title: "Create a high-performance stock trading platform",
    cat: "Testing & Reliability",
    stack: "Terraform + AWS",
    prompt: "Comprehensive project: Create a high-performance stock trading platform. You will design the architecture focusing on Testing & Reliability principles. Build the core components using Terraform + AWS. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 592,
    title: "Implement a distributed social feed",
    cat: "Performance",
    stack: "Kubernetes + Prometheus",
    prompt: "Comprehensive project: Implement a distributed social feed. You will design the architecture focusing on Performance principles. Build the core components using Kubernetes + Prometheus. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 593,
    title: "Implement a globally scaled payment gateway",
    cat: "API Design",
    stack: "Python + FastAPI",
    prompt: "Comprehensive project: Implement a globally scaled payment gateway. You will design the architecture focusing on API Design principles. Build the core components using Python + FastAPI. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 594,
    title: "Implement an observable ad-serving platform",
    cat: "Performance",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Implement an observable ad-serving platform. You will design the architecture focusing on Performance principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 595,
    title: "Design a high-performance social feed",
    cat: "Auth & Security",
    stack: "PostgreSQL + Redis",
    prompt: "Comprehensive project: Design a high-performance social feed. You will design the architecture focusing on Auth & Security principles. Build the core components using PostgreSQL + Redis. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 596,
    title: "Deploy a secure content delivery network",
    cat: "Distributed Systems",
    stack: "Elasticsearch + Logstash",
    prompt: "Comprehensive project: Deploy a secure content delivery network. You will design the architecture focusing on Distributed Systems principles. Build the core components using Elasticsearch + Logstash. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 597,
    title: "Build a globally scaled payment gateway",
    cat: "Observability",
    stack: "Kubernetes + Prometheus",
    prompt: "Comprehensive project: Build a globally scaled payment gateway. You will design the architecture focusing on Observability principles. Build the core components using Kubernetes + Prometheus. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 598,
    title: "Create a secure social feed",
    cat: "API Design",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Create a secure social feed. You will design the architecture focusing on API Design principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 599,
    title: "Deploy a secure fraud detection system",
    cat: "Data Engineering",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Deploy a secure fraud detection system. You will design the architecture focusing on Data Engineering principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
  {
    id: 600,
    title: "Architect a distributed content delivery network",
    cat: "Compiler & Runtimes",
    stack: "Go + gRPC",
    prompt: "Comprehensive project: Architect a distributed content delivery network. You will design the architecture focusing on Compiler & Runtimes principles. Build the core components using Go + gRPC. Include considerations for scalability, fault tolerance, and observability. Ensure the system handles edge cases like network partitions and node failures. Document the trade-offs made in the design."
  },
];
